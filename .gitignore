# Dependencies
node_modules/

# Build / package output
dist/
build/
*.zip

# 배포 산출물 (zip·스크린샷 — 로컬 deploy 폴더에서 관리, git 제외)
deploy/
*.jpg

# Brainstorm artifacts (visual companion — local only)
.superpowers/

# Secrets / env
.env
.env.*

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Logs
*.log
npm-debug.log*

# 티어 표 빌드 캐시 (거래소 능력치 응답 원본 — 산출물만 커밋)
scripts/.cache/
