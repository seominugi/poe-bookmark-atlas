# AGENTS.md — poe-bookmark-atlas

이 저장소의 **프로젝트 지침 정본은 [`CLAUDE.md`](CLAUDE.md)** 다. 작업을 시작하기 전에 `CLAUDE.md` 를 끝까지 읽고 적용한다.

전역 지침은 `~/.codex/AGENTS.md`(Codex) · `~/.claude/CLAUDE.md`(Claude Code) 를 따르고, 충돌 시 이 저장소의 `CLAUDE.md` 가 우선한다.

> **이 파일은 포인터다. 프로젝트 규칙을 여기에 적지 않는다.**
> Claude Code 는 `CLAUDE.md` 만, Codex 는 `AGENTS.md` 만 자동 로드한다. 양쪽에 내용을 나눠 적으면 두 도구가 서로 다른 지침을 읽게 되고 조용히 갈라진다.
