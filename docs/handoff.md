---
timestamp: 2026-08-17 (Asia/Seoul)
project: poe-bookmark-atlas
---

# poe-bookmark-atlas 핸드오프

POE2 거래소(poe.kakaogames.com) 북마크·히스토리 관리 Chrome MV3 확장 (Vite + @crxjs/vite-plugin). 우측 도킹 Shadow DOM 패널. 제작 브랜드: 서미누기.

## 현재 목표

**0.9.1 후보 1건이 develop 에 있다 (2026-08-17, 미릴리즈)** — **0.9.0 은 스토어 배포 완료**(사용자 확인). 그 뒤 들어온 PoB 제보를 고쳤다.

**PoB 복사 값 오류 (`2c2083e`)** — 제보: *"pob 복사에 전체 방어도, 생명력 재생 등 일부 항목이 잘못 복사됩니다."*
- 원인: 값 추출이 KR 설명의 **모든 숫자를 순서대로** 걷어 EN 템플릿의 `#` 에 앞에서부터 채웠다. KR 문구에는 값이 아닌 숫자가 자주 박혀 있다 — `1초마다 생명력 26.8 재생` → `Regenerate **1** Life per second`.
- 해법: 라이브 KR 템플릿(`1초마다 생명력 # 재생`)의 **`#` 자리에서만** 값을 뽑는다(`valuesByKoTemplate`). 템플릿이 없거나 어긋나면 예전 방식으로 폴백.
- **영향 범위 실측**: `#` 있는 KR stat 10,487개 중 **855개**가 틀리게 뽑히고 있었고 100% 교정됐다. `1초마다`·`3초마다`·`사용 1회당`·`최근 4초 이내`·`몬스터 1마리` 계열.
- 배제한 원인(기록): 변형 선택은 233개 전수 검사 **실패 0건**, 번들 EN 맵도 Armour·Life Regen 불일치 **0건**.
- ⏳ 제보의 "전체 방어도"는 정확히 어느 줄인지 미확인 — 위 855개에 포함될 가능성이 높지만 **제보자 확인이 필요**하다.

### 🔬 조사 결과 — PoB 번역을 통째로 없앨 수 있다 (2026-08-17 실측, 미착수)

사용자 제안: *"우리 번역 데이터 말고, 거래소 아이템 id 로 영문 거래소 데이터를 가져올 순 없나?"* → **전면적으로 가능하다.**

카카오에서 만든 **매물 id 를 영문 거래소 API 에 그대로** 넣으면 인증 없이 200 이 온다.

| 확인 | 결과 |
|---|---|
| `GET www.pathofexile.com/api/trade/fetch/<카카오 매물 id>` | **200, 인증 불필요** |
| 스탯 | `1초마다 생명력 13.8 재생` → `Regenerate 13.8 Life per second` |
| 베이스 | `보병 브리간딘` → `Infantry Brigandine` |
| **유니크 이름** | `종말의 발` → `Apocalypse Span` |
| PoE2 | 동일 동작 (`견목 타지` → `Hardwood Targe`) |
| `?query=` | **없어도 200** — 검색 id 불필요 |
| rate limit | `trade-fetch-request-limit` · IP `12:4:10,16:12:300,50:300:300,1000:21600:1800` (4초당 12회) |

없앨 수 있는 것: `pobStatMap`(1,487+577KB)·`pobBaseMap`(362KB)·`pobUniqueMap`(61KB)·`pobUniqueModMap`(78KB) = **약 2.5MB** + 번역 로직 298줄. 오늘 고친 855개 값 오류·변형 선택·미변환 제보가 전부 존재 이유를 잃는다.

⚠ **대가**: ① 카카오에서는 cross-origin 이라 **`www.pathofexile.com` 호스트 권한**이 필요하다(현재 optional — 필수로 올리면 기존 사용자 확장이 승인 대기로 꺼진다) ② 즉시 복사가 **네트워크 왕복**이 된다(실패·지연 가능) ③ 매물이 팔리면 영문 조회가 빈다.

**권장 설계**: 영문 조회를 1순위로 쓰고 **실패·권한 없음이면 지금의 번역 맵으로 폴백**. 그러면 대가가 전부 "느려지거나 예전 품질로 떨어질 뿐"이 된다. 폴백이 거의 안 쓰이는 게 확인되면 그때 맵을 지운다. **바로 지우면 권한 미허용 사용자의 PoB 가 통째로 죽는다.**

**0.9.0 스토어 배포 완료 (2026-08-17) — 이하 이전 기록** — 버전 범프(`cee47ec`) → `develop` → `main` fast-forward(`29eed4b..cee47ec`) → **`v0.9.0` 태그 publish(現 `Latest`, 대상 `cee47ec`)**. `deploy/poe-bookmark-atlas-0.9.0.zip`(33파일·826KB, manifest 루트 확인) 생성. **남은 것: 사용자가 스토어 심사 제출.**

⚠ **`v0.8.0` 은 스토어에 나가지 않은 채 0.9.0 으로 대체됐다** — 태그는 GitHub 에만 남는다(`v0.6.0`·`0.6.3`·`0.6.6` 과 같은 처지). 영문 거래소 지원이 새 사용자층을 여는 기능이라 patch 로 묻지 않고 minor 를 올렸고, **이미 한 번 재생성한 v0.8.0 을 또 지우지는 않았다**(처음 게시 내용과 많이 달라져 '같은 0.8.0' 이라 보기 어렵다).

⏳ **0.9.0 의 미검증 항목 2건** — 스토어 제출 전에 확인하면 좋다.
- **영문 거래소 실동작** — 한국에서는 pathofexile 페이지가 카카오로 리다이렉트돼 확인 불가. **해외 사용자 확인 필요.**
- **'새로워진 기능' 안내 재방문 미노출** — 하네스 chrome mock 이 리로드로 초기화돼 재현 불가.

**0.8.0 릴리즈 게시 (2026-08-16, 스토어 미제출 — 위 참조)** — 버전 범프(`14877bc`) → `develop` → `main` fast-forward(`22ab48b..29eed4b`, 15커밋) → **`v0.8.0` 태그 publish(現 `Latest`, 대상 `29eed4b`)**. `deploy/poe-bookmark-atlas-0.8.0.zip`(33파일·826KB, manifest 루트 확인) 생성.

**⚠ 그 뒤 develop 에 영문 거래소(GGG 계정) 지원이 추가됐다 (2026-08-16, `a5f9795`·`0c40c2b`·`6e9edf7`) — 아직 릴리즈에 안 담겼다.**

제보: *"외국 살아서 GGG 홈페이지로 게임합니다. 확장 프로그램 추가하고 거래소 들어가면 강제로 카카오사이트로 이동하는데, GGG 계정이라 로그인이 안 됩니다."*

- **리다이렉트는 우리 탓이 아니다** — 한국 IP 에서 `www.pathofexile.com/trade/search/Standard` → `poe.kakaogames.com/login/kakao?redir=...` 로 서버가 보낸다. 우리 콘텐츠 스크립트가 **주입되지도 않은 상태**에서 일어났다. 다만 해외 IP 는 그 대상이 아닐 텐데도 같은 증상이었다면 **우리 팝업이 카카오를 하드코딩한 것**이 원인이었고 그건 고쳤다.
- **두 호스트 API 실측**: `filter`·`option`·`static` id 는 순서까지 동일, `stat` id 는 집합 동일(양쪽 배타 0건). ⚠ **`items` 만 다르다** — `type` 이 곧 로컬라이즈된 이름(`파란 진주 목걸이` ↔ `Blue Pearl Amulet`), itemMap 대상 725개 중 공통 225개뿐.
- **해결 방식**: 거래소 데이터를 **콘텐츠 스크립트가 same-origin 으로 직접** 받는다. 서비스 워커가 대행하면 호스트마다 `host_permissions` 가 필요해 권한 클릭이 강제되는데, 그 데이터는 애초에 거래소 페이지와 같은 출처였다. 서비스 워커는 이제 **진짜 cross-origin 만** 맡는다(환율·탭 열기·전환). **여기로 되돌리지 말 것.**
- manifest 가 pathofexile 의 `/trade2/*` 를 매칭조차 안 하고 있었다(PoE2 글로벌 전면 미지원) → 네 스크립트 모두에 추가.
- 팝업 '영문 거래소에서도 사용' 버튼은 **안전장치로 남겨 뒀다** — 주입 자체에 권한이 필요한 환경이면 그때 쓰인다. 한국에서는 확인 불가라 제거하지 않았다.
- **언어는 호스트를 따라간다(사용자 결정 2026-08-16)** — 영문 거래소에서 저장하면 스탯·아이템·리그가 영문으로 들어간다. 완전한 한글화는 **구조적으로 불가능**하다(items 는 영↔한 대응 키가 없어 스탯만 한글화하면 한 카드에 언어가 섞인다). 한글 요구가 실제로 들어오면 그때 설정을 검토한다.
- ⏳ **한국에서 end-to-end 검증 불가** — pathofexile 페이지가 카카오로 리다이렉트돼 글로벌 패널을 띄울 수 없다. **해외 사용자 확인이 필요하다.**

⚠ **`v0.8.0` 태그를 한 번 지웠다 다시 만들었다** — 처음 게시(`44635c3`) 뒤에 가이드 투어의 '라이브로 열기' 전용 스텝(`29eed4b`)을 넣기로 해서, **스토어 미제출 상태**를 확인하고 사용자 승인 후 `gh release delete v0.8.0 --cleanup-tag` → 같은 번호로 재게시했다. 밖에서 그 태그를 소비한 곳이 없다(`v0.6.5` 때와 같은 판단). 태그·`main`·zip 이 모두 `29eed4b` 로 일치함을 확인했다. **남은 것: 사용자가 스토어 심사 제출.** 0.7.0 은 심사 통과 완료(2026-08-16 사용자 확인).

⚠ **minor 를 올린 이유** — 기능 추가뿐 아니라 **'내 리그' 설정 제거(기능 제거)와 스키마 v3(dedupeKey 재작성)** 가 들어 있다. 0.7.0 을 minor 로 올린 기준과 같다.

⚠ **라이브 확인 완료 항목** (2026-08-16, POE 개발 브라우저)
- **라이브로 열기 ✓** — `#ba-live` 로 진입하니 버튼 라벨이 '라이브 검색 비활성화'로 뒤집히고 URL 이 `/live` 로 전환됐다. 표식(`#ba-live`)은 소비 후 제거 확인. 테스트 후 라이브는 껐다.
- **아이템 유형 이름 ✓** — 용병 소환장 검색에서 히스토리 제목·저장 제안 이름 모두 `용병 소환장 (저격수)`.
- ⏳ **'새로워진 기능' 투어** — 하네스에서 3스텝 순서·완료 시 `whatsNewSeen` 기록은 확인했으나, **재방문 시 안 뜨는 것은 미확인**(하네스 chrome mock 이 리로드로 초기화돼 재현 불가). 실제 확장에서 한 번 볼 것.

**이전 목표(참고)**: 유튜브 소개 영상 피드백 5건 중 **5건 전부**와 그 과정에서 나온 버그들을 처리했다.

담긴 것:
① **드래그로 순서를 바꿔도 제자리로 돌아가던 문제**(`0836b49`) — 정렬이 '순서'가 아니면(기본값 '최근') 저장은 되고 화면만 되돌아갔다. 드롭 시 정렬을 '순서'로 바꾸고 토스트로 알린다.
② **설정 '내 리그' 선택지가 하나뿐이던 문제**(`f00ec00`) — 목록이 비동기로 늦게 와서 '자동'만 보였다. 로딩 표시 + 도착 시 재렌더(리그 수가 바뀔 때만 — 아니면 셀렉트 포커스가 튄다).
③ **좌측 배치 시 페이지가 우측에서 밀리듯 로딩되던 문제** — `src/content/layout-preload.js`(`document_start`)로 **첫 페인트 전에** 여백을 미리 건다.
④ **간헐적으로 패널이 안 보이던 문제**(에러 로그 없음) — 원인은 preinit 이 패널을 감췄다가 `reveal()` 이 안 도는 경우였다. 감추는 메커니즘 자체를 제거했다.
⑤ **로드마다 패널이 가장자리에서 밀려 들어오던 슬라이드** — 마운트가 창 폭으로 추측해 접고 storage 가 펴면서 생겼다. `src/lib/startCollapsed.js` 로 규칙을 한 곳에 모으고, 정착 동안만 전환을 끈다.

⚠ **`layout-preload.js` 에는 import 를 넣지 말 것** — crxjs 가 import 를 발견하면 이 스크립트를 **비동기 동적 import 로더**로 바꾼다(빌드 산출물로 확인). 그러면 첫 페인트 뒤에 실행될 수 있어 존재 이유가 무너진다. 그래서 접힘 규칙이 `startCollapsed.js` 와 의도적으로 중복돼 있고, 갈라지지 않는지는 `test/startCollapsed.test.js` 가 소스를 읽어 검사한다.

⚠ **전환 억제는 클래스만 지워선 안 된다** — 브라우저는 task 끝에 스타일을 한 번 확정하므로, 같은 task 에서 '값 변경 + 억제 해제'를 하면 확정 시점엔 전환이 켜져 있어 그대로 애니메이션된다. 억제가 걸린 상태로 **강제 리플로우(`offsetHeight`)** 를 일으켜 값을 먼저 확정시킨 뒤 푼다.

⑥ **'검색 열기' 설정(현재 탭 / 새 탭)** — P1 피드백. **기본값은 현재 탭 그대로**라 기존 사용자 동작은 불변이다.
  - ⚠ 새 탭 기능은 **원래 있었다**(`a88d1e5`, Ctrl/⌘ 클릭). 아무 데도 안 적혀 있어 "없다"는 제보로 돌아온 것 — 그래서 설정과 함께 **카드 툴팁이 지금 설정을 그대로 읽어 준다**.
  - ⚠ 수식키는 값을 고정하지 않고 **설정을 뒤집는다**(`src/lib/openTarget.js`). Ctrl=새 탭으로 고정하면 기본이 새 탭인 사람에겐 죽은 키가 된다.
  - ⚠ **새 탭은 서비스 워커(`ba-open-tab`)가 연다.** `window.open` 은 사용자 제스처 창 안에서만 허용돼, 대화상자·네트워크 응답을 기다린 뒤 부르는 경로(지난 리그 '그대로 열기' `renderList.js:744`, 리그 이관 후 `renderList.js:711`)에서 **팝업 차단으로 조용히 실패**한다. 구현 전에 발견해 처음부터 SW 경유로 만들었다.

⑦ **'보기' 설정(기본 / 간략)** — P2 피드백(#1·#5 의 정보 밀도 요구). **기본값은 '기본' 그대로.**
  - 카드를 **숨기지 않고 접는다**: 두 줄(이름 줄 + 메타 줄)을 flex 로 한 줄에 나란히 놓는다. 실측 북마크 83 → 47px, 히스토리 86 → 48px(같은 목록 높이에 3장 → 6장).
  - ⚠ 옛 구현(`db73b33` `.ba-list.ba-oneline`)은 `.ba-meta-row` 를 `display:none` 했다. `931372d` 이후 ⋯ 액션 팝오버와 '차단된 링크'·'오래됨' 경고가 그 안으로 들어와서, **지금 숨기면 그것들이 함께 사라진다.** 높이 이득은 접기와 동일(둘 다 47px)이라 잃기만 한다. (그 구현은 `931372d` 재설계 때 토글이 빠졌고, 규칙 잔해는 `7ca458a` 에서 지워지며 주석 3줄만 남아 있었다 — 이번에 정리)
  - ⚠ **말줄임 대응**: 한 줄로 접으면 384px 에서 이름 글자 폭이 38px(2~3자)까지 줄어 "회오…"가 된다. ① 가격을 조건 칩 **안으로** 옮기고(필 하나 분량의 테두리·여백·간격 약 23px 회수) ② 썸네일을 접어(약 34px) **95px(7자)** 로 올렸다. 조건 아이콘은 남겼다 — 툴팁 앵커이자 '조건이 있다'는 유일한 표시라, 빼면(+17px) 칩이 그냥 숫자가 된다.
  - ⚠ 가격은 **양쪽에 렌더하고 CSS 로 전환**한다(`.ba-price-pill` ↔ `.ba-cond-price`). 부모가 달라 CSS 만으로는 못 옮기고, `data-brief` 는 재렌더 없이 켜고 끄는 표시 전용 상태여야 하기 때문. 조건 칩이 없는 히스토리(조건 0개)는 시각 칩에 얹는다 — 가격이 갈 곳이 항상 있어야 한다.
  - ⚠ **히스토리 제목에 말줄임이 없었다**(잠복 결함). 두 줄 배치에선 폭이 넉넉해 안 드러나다가, 한 줄로 접자 한 글자씩 세로로 쪼개져 **84 → 137px 로 오히려 커졌다.** 북마크 제목과 같은 `nowrap` + 말줄임을 줬다.
  - ⚠ `contain-intrinsic-size` 를 간략 모드에서 96 → 47px 로 맞춘다. 추정 높이가 실제의 2배면 스크롤바가 튄다.

⑧ **'라이브로 열기'(P3 라이브 서치)** — 북마크 ⋯ 액션 + 이름 **Shift 클릭**. **우리는 WebSocket 을 만들지 않는다.**
  - 거래소 화면엔 원래 `button.livesearch-btn`('라이브 검색 활성화')이 있고 인증·재연결·GGG 동시 연결 정책이 전부 거기 붙어 있다. 우리는 북마크에서 그 버튼까지 한 번에 데려다줄 뿐이다(기존 3단계 → 1단계). 새 권한 0.
  - 표식은 **URL 해시(`#ba-live`)** 로 싣는다. storage 플래그를 쓰면 어느 탭이 자기 것인지 가리는 장치(만료·소비·탭 추적)가 줄줄이 필요해진다. 표식은 로드 즉시 지운다 — 안 그러면 새로고침마다 다시 켜진다.
  - ⚠ **항상 새 탭**이다. 현재 탭에서 열면 라이브가 구조적으로 하나로 묶이고 보던 검색도 잃는다. '검색 열기' 설정을 따르지 않는 유일한 경로.
  - ⚠ **지난 리그면 먼저 지금 리그로 이관한 뒤** 라이브를 건다(사용자 방침). 끝난 리그에 라이브를 걸면 매물이 영영 안 떠 — 조용히 아무 일도 안 일어나는 최악의 실패다.
  - ⚠ 결과 행이 그려진 뒤에 누른다(검색 전엔 무효). **이미 켜져 있으면 누르지 않는다**(누르면 꺼진다). 누른 뒤 라벨 변화로 성공을 확인하고, 실패하면 토스트로 알린다.
  - **동시 개수 제한은 두지 않는다** — 한 번 클릭에 탭 하나라 429 를 부를 몰아치기가 없고, GGG 실제 상한은 측정하지 못했다. 모르는 숫자로 사용자를 막지 않는다.
  - ⏳ **실사용 확인 미완**: 버튼 클릭 타이밍(20초 안)·Vue 핸들러 발동·라벨 변화 판정.

⑨ **설정 정리** — 옵션 시인성(12.5px, 꺼진 쪽 대비 6.35→10.15) · 툴팁 11→12.5px · 4개 설정 전부 라벨 옆 `?` 도움말 배지(네이티브 title 제거 — 지연이 있고 눈에 안 띄어 있는 줄도 몰랐다).

⑩ **'내 리그' 수동 설정 제거** — 리그는 URL 에 있고 살아있는 리그 목록도 이미 받아온다. `resolveCurrentLeague` 가 '화면의 리그 → 최근 검색' 순으로 **살아있는 것만** 골라 스스로 정하고, 근거가 없으면 이관을 제안하지 않는다(엉뚱한 리그로 보내느니 멈춘다). 고를 게 없는 설정이었고 잘못 고르면 조용히 엉뚱한 리그로 이관되는 위험만 남았다. 저장돼 있던 `uiLeague` 값도 한 번 지운다.

**남은 피드백**: 없음 — 유튜브 피드백 5건 전부 처리. 2026-08-05 라운드 3건도 종결(위 해당 절의 2026-08-16 주석 참조).

### 0.7.1 담길 것 — 사용자 관점 요약 (릴리즈 노트 원안)

**고친 것**
- 드래그로 순서를 바꿔도 제자리로 돌아가던 문제 — 정렬이 '순서'가 아니면(기본값 '최근') 저장은 되고 화면만 되돌아갔다. 이제 드롭하면 정렬을 '순서'로 바꾸고 알린다.
- 설정의 '내 리그' 선택지가 하나뿐이던 문제 — 리그 목록이 비동기로 늦게 와서 '자동'만 보였다. (이후 이 설정 자체를 없앴다 — 아래)
- 좌측 배치 시 페이지가 우측에서 밀리듯 로딩되던 문제 — `document_start` 선반영으로 첫 페인트 전에 자리를 잡는다.
- 간헐적으로 패널이 안 보이던 문제(에러 로그 없음) — 패널을 감췄다 드러내는 메커니즘 자체를 제거했다.
- 페이지를 열 때마다 패널이 가장자리에서 밀려 들어오던 모션 제거.
- **리그가 다른 검색을 같은 북마크로 보던 문제** — 하드코어 올플레임과 올플레임의 같은 조건이 한 항목으로 뭉개졌다.
- **북마크 이름에 내부 id 가 나오던 문제** — 용병 소환장 등이 "NonEleBowRangerPhys"로 저장됐다.

**추가된 것**
- **검색 열기: 현재 탭 / 새 탭** 선택(기본 현재 탭). Ctrl 클릭은 항상 반대로.
- **보기: 기본 / 간략** — 카드를 한 줄로 접어 한 화면에 약 2배(83→47px). 기본값은 그대로.
- **라이브로 열기** — 북마크 ⋯ 또는 이름 Shift 클릭. 거래소의 라이브 검색이 켜진 채로 새 탭에서 열린다.

**정리된 것**
- 설정 옵션 시인성(꺼진 쪽 대비 6.35→10.15)·툴팁 크기(11→12.5px)·설정마다 `?` 도움말.
- '내 리그' 수동 설정 제거 — URL 과 살아있는 리그 목록으로 스스로 정한다.

⚠ **스키마 v3 — 저장된 북마크의 dedupeKey 를 한 번 다시 쓴다.** 북마크 내용은 그대로이고 키만 바뀐다(리그 칸 추가). 되돌릴 필요가 생기면 키를 다시 계산하면 되지만, 릴리즈 전에 실데이터에서 목록·중복 감지가 정상인지 한 번 보는 것을 권한다.

**미정리(알고 있는 것)**: `.ba-list.dense` 규칙(panel.css 731~733)이 `[data-density="compact"]` 와 중복인 채 아무도 안 쓴다. 이번 변경과 직접 얽히지 않아 손대지 않았다.

**0.7.0 스토어 심사 요청 완료 (2026-08-15)** — 버전 범프(`22ab48b`) → `develop` → `main` fast-forward(`56185cf..22ab48b`, 9커밋) → **`v0.7.0` 태그 publish(現 `Latest`, 대상 `22ab48b`)**. `deploy/poe-bookmark-atlas-0.7.0.zip`(31파일·803KB, manifest 루트 확인) 생성. **사용자가 스토어 심사 제출까지 완료. 남은 것: 심사 결과 회신.**

담긴 것 (0.6.6 이후 8커밋 중 사용자 대면 4건):
① **패널 폭 드래그 조절**(384~880px) — 핸들·페이지 밀기가 뒤늦게 따라오던 전환 문제 포함
② **조건 묶음을 거래소 화면에도** — 능력치 필터 그룹 바깥 위. 호버 시 조건 목록 툴팁
③ **조건 요약 말줄임 최소화** — 글자 중간이 아니라 '·' 경계에서 끊는다(라이브: 384 20/26 → 560 이상 26/26 온전)
④ **북마크 메모 기능 제거** — 카드 109 → 81px(-26%), 한 화면 북마크 +33%. ⚠ 스키마 v2 마이그레이션이 저장된 note 를 지운다(되돌릴 수 없음)
⑤ **찜 상태 확인 개별화** — 찜 61개 이상이면 일괄 확인이 항상 429 로 실패하던 버그 수정. 요청 간 700ms 강제 + 429 시 정지 시간만큼 차단

⚠ **`v0.6.6` 태그는 GitHub 에만 남는다** — 스토어에 제출하지 않은 채 0.7.0 이 나왔다(v0.6.0·0.6.1·0.6.3 과 같은 처지). **스토어로 나가는 버전은 0.7.0.**

⚠ **0.7.0 이 심사 중이다** — 지금 새 버전을 올리면 제출분이 대체된다(스토어는 한 번에 한 버전만 큐에 둔다). 0.6.1→0.6.2, 0.6.3→0.6.4 에서 겪은 그 상황이다. **버전 범프·릴리즈는 심사 결과를 보고 진행한다.**

⚠ **minor 를 올린 이유** — 기능 추가뿐 아니라 **기능 제거(메모)와 되돌릴 수 없는 데이터 삭제**가 들어 있다. patch 로 묻으면 나중에 이력에서 변화 크기를 놓친다.

**0.6.6 릴리즈 게시 완료 · 스토어 제출 대기 (2026-08-13)** — 버전 범프(`56185cf`) → `develop` → `main` fast-forward(`266392a..56185cf`) → **`v0.6.6` 태그 publish(現 `Latest`)**. `deploy/poe-bookmark-atlas-0.6.6.zip`(31파일·801KB, manifest 루트 확인) 생성. **남은 것: 사용자가 스토어 심사 제출.**
담긴 것: 북마크가 엉뚱한 검색을 저장하던 문제(원인 2개 — 0건 검색이 히스토리에 안 남던 것 / 링크로 연 검색의 dedupeKey 붕괴). 제보 링크 3개로 라이브 검증 완료.
⚠ **이번엔 태그를 옮기지 않고 번호를 올렸다** — 0.6.5가 **스토어에 실제 배포**돼 있어(2026-08-13 확인), 태그를 옮기면 태그와 배포물이 어긋난다. 미제출이던 지난번(0.6.5 재게시)과 상황이 다르다.

**0.6.5 스토어 배포 완료 (2026-08-13 확인)** — 이전 기록: 릴리즈 게시(재게시) — `develop` → `main` fast-forward(`6836a23..266392a`, 11커밋) → **`v0.6.5` 태그 publish(現 `Latest`, 대상 `266392a`)**. `deploy/poe-bookmark-atlas-0.6.5.zip`(31파일·801KB, manifest 루트 확인) 생성. **남은 것: 사용자가 스토어 심사 제출.**

⚠ **`v0.6.5` 태그를 한 번 지웠다 다시 만들었다** — 처음 게시(`42e349d`)에는 **주얼 반경 '작게' 버그가 들어 있었고**, 스토어 미제출 상태에서 수정본(`266392a`)이 나왔다. 태그가 실제 배포물과 어긋나면 안 되므로 사용자 승인 후 `gh release delete v0.6.5 --cleanup-tag` → 같은 번호로 재게시했다. **스토어에 나간 적이 없어 밖에서 그 태그를 소비한 곳은 없다.** 버전 번호를 올리지 않은 것도 사용자 결정.
담긴 것: ① **찜한 매물**(개별 매물 저장 + 생존 확인 + 썸네일·찜한 시점) ② 액션 행 줄바꿈 완결(폰트 의존성) + 회귀 가드 테스트 ③ '오래됨' 배지·'상태 확인' 버튼 찌그러짐 ④ 한↔영 전환 geo-redirect 오진 정정 ⑤ 확장 컨텍스트 무효화 처리.
**⚠ 이번엔 0.6.4가 스토어 배포 완료된 상태에서 올린다** — 0.6.1→0.6.2, 0.6.3→0.6.4처럼 심사 중 대체되는 상황이 아니다. `v0.6.4`도 스토어에 실제로 나갔다.

**0.6.4 스토어 배포 완료 (2026-08-06 확인)** — 심사 통과·게시됨. 이전 기록: 심사 요청 완료 — 버전 범프(`6836a23`) → `develop` → `main` fast-forward(`aae6e4e..6836a23`) → **`v0.6.4` 태그 즉시 publish(現 `Latest`)**. `deploy/poe-bookmark-atlas-0.6.4.zip`(31파일·797KB, manifest 루트 확인) 생성. **사용자가 스토어 심사 요청까지 완료. 남은 것: 심사 결과 회신.**

⚠ **develop에 0.6.5 후보가 이미 쌓여 있다** — 한↔영 전환 geo-redirect 오진 정정 + 챌린지 대응(`f795fb5`, 아래 완료 항목 첫 절). **버전 범프·릴리즈는 0.6.4 심사 결과를 보고 진행한다**(사용자 방침 2026-08-06) — 지금 올리면 0.6.4가 또 대체되기 때문. 0.6.1→0.6.2, 0.6.3→0.6.4에서 겪은 것과 같은 상황이다.
담긴 것: ① 패널 스크롤바 자리 확보 ② 거래소 필터 퍼지 검색(~) on/off ③ 군단 주얼 PoB(정복자 줄 유실·반경 미출력) ④ 조용한 실패 가시화(값 초과·변형 추정 경고) ⑤ 품질·소켓·영향력 출력 + 앞서 develop에 있던 유니크 전용 문구 KR 폴백.
**PoB 실제 붙여넣기 사용자 확인 완료** — 군단 주얼·품질·소켓·영향력 정상 동작.

⚠ **`v0.6.3` 태그는 GitHub에만 남는다** — 스토어 제출 전에 0.6.4가 나와 대체됐다(v0.6.0·v0.6.1과 같은 처지). 스토어로 나가는 버전은 **0.6.4**.

**남은 후속(저우선, 미착수)**: PoB가 읽지만 아직 안 만드는 필드 — `requirements`·`identified`·`synthesised`·`duplicated`·`scourgeMods`·`veiledMods`·`crucibleMods`. 수치에 직접 영향 주는 품질·소켓은 0.6.4에서 처리했다.

**0.6.2 릴리즈 게시 완료 · 스토어 제출 대기 (2026-08-02)** — 사용자 제보(소형 클러스터 주얼 인챈트가 한글로 남음)로 번들 stat 맵이 stale함을 확인하고 재생성했다(`1e9bdba`). 최신 stats API 대비 미매핑 **poe1 2,733건 / poe2 41건 → 양쪽 0건**. 아래 완료 항목 참조.
버전 범프 0.6.1 → **0.6.2**(`49fb1b4`) → `develop` → `main` FF 머지·푸시(`8bcd0b9..49fb1b4`, 4커밋) → **`v0.6.2` 태그 즉시 publish(`49fb1b4`, 現 `Latest`)**. `deploy/poe-bookmark-atlas-0.6.2.zip`(29파일·788KB, manifest.json 루트 확인) 생성 완료.
**사용자가 0.6.2 스토어 심사 요청 완료(2026-08-02). 남은 것: 심사 결과 회신.**
로컬 태그도 `git fetch origin --tags`로 v0.6.2까지 동기화 완료(`gh release create`는 태그를 원격에만 만든다 — 릴리즈 직후 fetch 습관화할 것).
**그 뒤 develop에 0.6.3 후보 1건 커밋·push 완료** — 유니크 전용 문구 KR 원문 폴백. **버전 범프·릴리즈는 사용자 요청 대기** — 0.6.2가 심사 중이라 지금 올리면 또 대체되므로, 심사 결과를 보고 묶는 선택지도 있다.

**2026-08-05 — 사용자 피드백 라운드 진행 중(미커밋).** 제보 5건 중 이 repo 소관 2건(스크롤바 자리 확보·퍼지 검색 on/off) 구현·검증 완료했으나 **아직 커밋 안 됨**. 나머지 3건은 seominugi-com 소관(1건은 의도적 보류). 상세는 완료 항목 첫 절.
⚠ **0.6.1 제출분은 0.6.2로 대체됐다** — 0.6.1은 심사 중이었고 스토어는 한 번에 한 버전만 큐에 둔다. 스토어로 나가는 버전은 0.6.2, `v0.6.1` 태그는 GitHub에만 남는다(v0.6.0과 같은 처지).
⚠ zip이 745KB → 788KB로 늘었다(stat 맵 증가분). PoB 버튼 클릭 시 lazy 로드라 초기 로드 성능엔 영향 없다.

**0.6.1 릴리즈 게시 완료 · 스토어 심사 대기 (2026-08-01)** — 조건 묶음 드래그 재배치·삭제, 카오스↔신성한 양방향 환산 칩, 하드코어 리그명 한글화, 변형 아이템 북마크명 버그 수정(v0.6.0, `cd5dbc4`) + 사용자 실사용 제보로 정비한 드래그 사용성·칩 줄 접기(0.6.1).
`develop` → `main` FF 머지·푸시(`cd5dbc4..8bcd0b9`) → **`v0.6.1` 태그 즉시 publish(`8bcd0b9`, 現 `Latest`)**. `deploy/poe-bookmark-atlas-0.6.1.zip`(29파일) 생성.
**사용자가 0.6.1 스토어 심사 요청 완료(2026-08-01). 남은 것: 심사 결과 회신.**
⚠ **0.6.0 zip은 스토어에 제출하지 않았다** — 0.6.1이 스토어로 가는 버전이고 v0.6.0 태그는 GitHub에만 남는다.
⚠ **심사 전에 태그를 게시했다**(0.5.0부터의 사용자 방침) — 반려되면 태그가 스토어보다 앞선 상태가 된다.

**미확인 1건**: 조건 묶음 칩 줄 **접힘 상태가 재로드 후 복원되는지**는 하네스가 매 로드 목 스토리지를 새로 시드해 검증하지 못했다. 저장(`uiSetsCollapsed: true`)까지는 확인 — 실제 확장에서 접어두고 새로고침해 확인 필요.

**0.5.0 릴리즈 완료 · 스토어 제출 대기 (2026-07-28)** — 조건 묶음·조건 칩 클릭·일괄 폴더 이동·환산 칩 확대 + 가이드 투어 반영·토스트 재배치를 담아 **0.5.0 릴리즈 게시 완료**.
`develop` → `main` fast-forward 머지·푸시(`00f25f2..13dd975`, 16커밋) → **`v0.5.0` 태그 게시(`13dd975`, 現 `Latest`)**. `deploy/poe-bookmark-atlas-0.5.0.zip`(29파일·726KB) 생성 완료.
**스토어 제출까지 완료(2026-07-28) — 0.5.0 사이클 종료.** 다음 작업은 미완료 목록에서 고른다.
⚠ **이번엔 심사 전에 태그를 게시했다** — 사용자 판단("심사는 거의 통과된다"). 지금까지의 "심사 통과 후 publish" 순서와 다르므로, 만약 반려되면 태그가 스토어보다 앞서 있는 상태가 된다.

**0.4.0 (2026-07-27 게시 중)** — 스토어 심사 통과. `v0.4.0` 릴리즈 publish 완료 → 태그 `9064d03`, `Latest` 표시.


**0.4.0 배포 준비 (2026-07-22)** — 리그 이관·투어 데모·내 리그 설정을 담아 **0.4.0으로 범프**(manifest·package)하고 `deploy/poe-bookmark-atlas-0.4.0.zip` 생성 완료. **남은 것: ① `develop` → `main` 머지(보호 브랜치라 사용자 확인 필요) ② GitHub 릴리즈 `v0.4.0` draft 생성 ③ 스토어 심사 제출.**
**진행 완료(2026-07-22)**: `v0.3.0` draft publish(태그 생성, `0f80dae`) → `develop`→`main` fast-forward 머지·푸시(`9064d03`, 11커밋) → `v0.4.0` draft 릴리즈 생성(target `9064d03`, 노트는 0.3.0 절 제외한 신규 버전). **다음: 사용자가 스토어에 `deploy/poe-bookmark-atlas-0.4.0.zip` 제출 → 심사 통과 후 `v0.4.0` draft를 직접 publish.**


0.2.0 스토어 심사 **통과**(2026-07-04 확인). 피드백 3건(작업1·2·3) 모두 기능 구현 완료. **사용자 방침(2026-07-04): "번역 100% 완벽 안 된 상태 인정하고, 미진한 부분은 추후 보완"** — Shift+클릭 수동 제보 기능이 그 보완 파이프라인. 이후 가이드 투어 화살표·키보드 네비·리그 노출 라운드까지 마침. **0.3.0 준비 완료** — 버전 범프(manifest·package 0.3.0)·`deploy/poe-bookmark-atlas-0.3.0.zip`·GitHub 릴리즈 3종 생성. **다음: 사용자가 스토어에 0.3.0 심사 제출 예정.**

## 완료된 작업

### 찜 상태 확인 — 일괄 → 개별, 요청 제한 대응 (2026-08-13, **미커밋 · 하네스 검증 완료**)

**질문에서 시작**: "가격확인을 브라우저 로딩 시마다 할 순 없을까? rate limit 에 걸릴까?"
→ **실측 답: 걸린다.** 그리고 **지금 코드에 이미 버그가 있었다.**

**실측한 정책** (`/api/trade/fetch` 응답 헤더, 정책명 `trade-fetch-request-limit`, 2026-08-13):

| 규칙 | 한도 | 초과 시 정지 |
|---|---|---|
| **Account** | **4초에 6요청** | 10초 |
| IP | 4초 12 / 12초 16 | 60초 |
| IP | **5분에 100** | 5분 |
| IP | 3시간에 1000 | 30분 |

**기존 버그** — `runWatchCheck` 가 10개씩 묶어 최대 10요청을 **대기 없이** 쏘았다. 계정 규칙이 6/4초라
**찜 61개를 넘으면 7번째 요청이 429**. 찜 상한이 100개(=10요청)이므로 상한까지 채운 사용자는 버튼이 항상 실패했다.
게다가 `!res.ok` 를 통째로 `throw` 해서 "상태를 확인하지 못했어요"만 떴다 — 원인을 알 수 없고, 재시도하면 정지가 더 길어진다.
⚠ 이 정지는 **엔드포인트 단위**라 확장뿐 아니라 **사용자의 실제 거래소 검색까지 멈춘다**(거래소 사이트도 같은 `/api/trade/fetch` 를 쓴다).

**변경 (사용자 결정: 찜마다 개별 확인)**
- 섹션 헤더의 일괄 '상태 확인' 버튼 제거 → **찜 행마다 `확인` 버튼**. 클릭 1회 = 요청 1회.
- 다른 거래소 매물에는 버튼을 그리지 않는다(조회하면 무조건 null 이 와서 '판매됨'으로 오판한다).
- `src/lib/tradeRate.js`(신규) — 실측 정책값에 묶인 순수 함수:
  `MIN_GAP_MS = 700`(계정 6/4초 = 667ms + 여유) · `nextDelay()` · `retryAfterMs()` · `parseRules()`
- 연타해도 요청 간 700ms 를 강제. 429 를 맞으면 정지 시간을 헤더에서 읽어 **그동안은 요청을 아예 보내지 않고** 남은 초를 알린다.

**하네스 검증**

| 확인 | 결과 |
|---|---|
| 버튼 렌더 | 24px 1줄, nowrap, 넘침 0 (CSS 누락으로 두 번 깨졌던 자리) |
| 최소폭 384 | 액션 행 필요 230 / 가용 323 → 여유 93px, 1줄 |
| 429 응답 | "제한에 걸렸어요 — 10초 뒤에" |
| 막힌 중 재클릭 | **요청 안 나감**, "9초 뒤에"(남은 시간 반영) |
| 2개 연타 | 요청 간격 **997ms** (≥700 준수) |
| 성공 | "아직 있어요." |

⚠ **측정 함정**: 처음엔 1차 fetch 가 끝나기 전에 2차를 눌러 두 클릭이 경합했고, 2차는 복원된 실제 fetch 로
나가 카운터에 안 잡혔다 — "추가 요청 없음"으로 잘못 읽을 뻔했다. **각 클릭의 async 완료를 기다린 뒤** 재야 한다.

**'로딩마다 자동 확인'은 채택하지 않았다** — 이 확장은 검색할 때마다 페이지가 이동하므로 "로딩마다"는 실질적으로
"검색할 때마다"다. 찜 100개면 로드당 10요청이라 IP 5분 한도(100)를 10번 로드에 소진하고, 그 정지가 사용자의
검색을 막는다. 나중에 자동화한다면 쿨다운(15분+) + 헤더 적응 + 429 백오프가 전제다.

파일: `src/lib/tradeRate.js`(신규), `test/tradeRate.test.js`(신규 14), `src/content/panel/renderList.js`, `panel.css`.



### 소개 영상 게시 + 대본·업로드 메타데이터 정리 (2026-08-15, **develop 반영 완료** `0041687`)

**유튜브 소개 영상이 실제로 게시됐다** (약 10분 30초, 사용자 라이브 더빙). 0.3.0 기준이던 기존 대본을 0.6.6 기준으로 개정하고, 확정된 업로드 메타데이터를 문서에 남겼다.

- `docs/영상-소개-대본.md` — 실제 더빙에 쓴 낭독용 대본(연출 표기 제외)
- `docs/영상-촬영-노트.md`(신규) — 준비 대본 2벌(2분/4분) + 촬영 체크리스트 + **확정 업로드 메타데이터**(제목·설명란 전문·챕터 14개·태그) + 다음 영상 개선점
- `README.md` — **크롬 웹스토어 링크 추가**. 그때까지 repo 어디에도 스토어 URL이 없었고 설치 안내가 로컬 개발 로드뿐이었다. 상태 표기 `v0.1 로컬 테스트 단계`도 게시 사실과 어긋나 교체.

**스토어 URL** (repo 정본): `https://chromewebstore.google.com/detail/poe-%EB%B6%81%EB%A7%88%ED%81%AC-%EC%95%84%ED%8B%80%EB%9D%BC%EC%8A%A4/kjdnpniemjhflpbgfhcjgdenngdpikeh`

⚠ **촬영 함정 (다음에 또 걸린다)**: 「내 리그로 다시 검색」은 저장 당시 리그가 **이미 끝난 리그**일 때만 카드에 뜬다(`renderList.js` — 스탠다드↔하드코어처럼 둘 다 열려 있으면 안 깨진 것으로 보고 표시 안 함). 그 컷은 **종료된 과거 리그의 북마크**가 있어야 촬영 가능하다.

⚠ **영상 대사는 "POE1과 POE2 모두"로 나갔다** — 실제 패널이 붙는 건 카카오 거래소이고 글로벌 `pathofexile.com`은 선택 권한 + 봇 챌린지 이슈에 `/trade2`엔 패널이 안 붙는다. 설명란에 "카카오 거래소 기준" 한 줄을 넣자고 제안했으나 이번 업로드엔 미반영. 유튜브 설명란은 사후 수정이 가능하므로 글로벌 유저 문의가 들어오면 그때 추가할 것. 같은 이유로 **디스코드 링크도 설명란에 없다**(PoB 미번역 제보 창구인데 빠졌다).

### 북마크 메모 기능 제거 (2026-08-13, **미커밋 · 하네스 검증 완료**)

**요청**: "북마크마다 있는 메모 기능은 삭제하자. 거의 사용하지 않는 기능 같아. 삭제하면 더 많은 북마크가 보일 것 같아."

**코드가 그 판단을 뒷받침했다** — `addBookmark`·`promoteToBookmark` 가 저장 시 `note` 를 `buildAutoNote()` 로 **자동으로 채우고 있었다.** 그래서 대부분의 메모는 사용자가 손대지 않은 자동 요약(`반지 · 타락 아니요 · 능력치 6개`)이었고, 조건 요약 칩과 내용이 겹쳤다.

**제거 범위 (사용자 결정: 데이터까지 삭제)**
- UI: `.ba-note-slot` 마크업 · 인라인 편집 · `.ba-note*` CSS 전부
- 저장: `setNote` 삭제, 자동 채움 2곳 삭제, **스키마 v1 → v2 마이그레이션으로 기존 `note` 필드 제거**
- `src/lib/autoNote.js` + 테스트: 유일한 소비자였던 store 가 안 쓰게 되어 고아 → 삭제

**검색 범위는 줄이지 않았다** — 예전 인덱스에 `note` 가 들어갔는데 그 내용이 `유형 + 비능력치 필터(label value)` 였다. 같은 재료를 원본에서 직접 넣도록 바꿔 `'반지'`·`'아이템 레벨'` 검색이 그대로 동작한다(테스트로 고정). 사라지는 건 **손으로 쓴 메모로 찾던 경우뿐**이고 그 데이터는 함께 지웠다.

**딸려 나온 정리 2건**
- **wide 밴드 폐지**: 카드 2층 병합은 '메모 줄'과 짝일 때만 조건 요약을 안 갉아먹었다. 짝이 사라지면 남은 후보(이름 줄 + 조건 요약)로 합쳐야 하는데 그건 599→600 잘림 급락(0→7건)의 재발이다. → `widthBand` 는 narrow/default 2단.
- **'간략히 / 상세히' 토글 제거**: 두 모드의 유일한 차이가 메모 줄 숨김이었다(CSS 주석에 "조건칩·⋯는 유지"라고 명시돼 있었다). 메모가 없으면 패딩 2px 차이뿐이라 눌러도 아무 일 없는 버튼이 된다. 액션 행 약 60px 회수 → **`actionRowBudget` 상한도 4항목/16자 → 3항목/11자로 함께 내렸다**(안 내리면 가드가 헐거워져 회수한 폭을 아무도 모르게 다시 채운다).

**결과 (하네스 실측, 최악 폰트)**

| 폭 | 카드 높이 | 액션 행 |
|---|---|---|
| 300 | 106px | 1줄(아이콘만) |
| 384 | **109 → 81px (-26%)** | 1줄 |
| 880 | 81px | 1줄 |

한 화면 북마크 수 **약 +33%**. 액션 행 문구는 `오래된 1 · 전체 접기 · 폴더 추가` 로 줄었다.

⚠ **되돌릴 수 없다** — v2 마이그레이션이 다음 로드 때 저장된 `note` 를 지운다. 손으로 쓴 메모가 있었다면 그 시점에 사라진다(사용자가 알고 결정).

**이어서 — 최소폭을 300 → 384 로 되돌리고 폭 밴드를 폐지 (같은 날, 사용자 요청)**
"최소 너비는 300px 로 줄이기 전 사이즈로" → 384(폭 조절 도입 전 고정폭이자 액션 행 실측 하한).
그러면 **narrow 구간(300~383)이 도달 불가능**해져 그 구간용 코드가 전부 죽는다 → 사용자 결정으로 정리:
- `widthBand()` · `data-w` 속성 · `.ba-root[data-w="narrow"]` CSS 약 40줄
  (액션 행 라벨 접기 · 제작자 칩 접기 · 폴더 액션 호버 노출 · 카드 그립/썸네일 숨김 · 가격 아랫줄)
- `.ba-btn-lbl` 래퍼와 그것을 검사하던 가드 테스트 2건
- **복원이 필요하면 커밋 `f66765d`** 에 그대로 있다.
- 남긴 것: `.ba-brand-tx b` 의 nowrap·말줄임 — 이건 narrow 전용이 아니라 **최악 폰트에서 384px 에서도**
  제품명이 2줄이 되던 기존 결함을 막는 규칙이다(하네스 실측: 필요 136 / 가용 127px).

**최종 확인 (하네스 실측)**: 300·200px 로 끌어도 **384 에서 멈춤** / 880 까지 확장 정상 /
카드 81px / 액션 행 1줄 28px(`오래된 1 · 전체 접기 · 폴더 추가`) / `data-w` 속성 없음 / 제품명 줄바꿈 없음.

**남은 것**: 확장 리로드 후 라이브 확인(마이그레이션이 실제 저장소에서 도는지, 카드 높이·검색).

### 조건 요약 말줄임 최소화 — '조건 경계'에서 끊기 (2026-08-13, **미커밋 · 하네스 검증 완료 · 라이브 미검증**)

**제보**: "말줄임표 되는 문제를 최소화하는 방법은 없을까?"

**먼저 잰 것 (사용자 북마크 26개)** — 잘림 비율:

| 폭 | 이름 | 조건 요약 |
|---|---|---|
| 300 | 69% | 69% |
| 384 | **15%** | 65% |
| 480 | 4% | 58% |
| 880 | **0%** | **46%** |

→ **이름은 384 이상에서 사실상 해결된다. 문제는 조건 요약이고, 880px 까지 넓혀도 절반이 잘린다.**

**후보안들을 계산해 전부 기각**
- **2줄 허용**: 65%→54%(384) / 46%→31%(880). 카드 +18px 를 내고 11~15%p 만 산다. wide 밴드의 '항목을 많이 보여준다'는 보상과 정면 충돌.
- **반복 단어 압축**: 요약 안에서 겹치는 단어를 다 지워도 **전체 절감 20%**. 최장 169자는 41% 줄여도 100자라 여전히 못 담는다.
- 근본 이유: 조건 요약 = 조건 수 × 스탯명 길이. 중앙값 44자인데 384px 의 가용은 약 16자다. **어떤 폭에서도 다 못 보여준다.**

**그래서 자르는 방식을 바꿨다** — 글자 중간이 아니라 `·` 경계에서 끊는다. 보이는 조건은 온전하고, 남은 개수는 이미 왼쪽 '조건 N개' 배지가, 전체는 호버 툴팁이 갖는다. CSS `text-overflow` 는 글자 단위라 불가능해 측정이 필요하다 → `src/lib/fitSummary.js`(순수 함수 `fitByParts` + DOM 적용 `fitCondSummaries`).

**결과 (하네스, 조건 요약 11개)** — '온전히 끝남':

| 폭 | 온전 | 평균 표시 조건 수 |
|---|---|---|
| 300 | 2/11 | 2.09 |
| 384 | **10/11** | 1.73 |
| 480 | **11/11** | 2.27 |
| 880 | **11/11** | 3.00 |

**라이브 검증 완료 (실제 거래소 · 북마크 26개)**

| 폭 | 개선 전 | 개선 후 |
|---|---|---|
| 384 (기본) | 잘림 65% (≈9/26 온전) | **20/26 온전** (잘림 23%) |
| 880 | 잘림 46% | **26/26 온전** (잘림 0%) |

남은 6건은 ① 조건이 하나뿐이라 끊을 경계가 없거나 ② 첫 조건 자체가 가용폭보다 긴 경우 — 설계된 폴백이다.
⚠ **드래그 중 재측정은 라이브 미검증** — 검증 시점에 탭이 `hidden` 이라 rAF 가 멈춰 있었다. 렌더 시점 계산(renderList 에서 동기 호출)은 폭을 저장하고 새로고침하는 방식으로 384·880 양쪽 다 확인했다.

**300px 만 예외이고 그건 설계된 폴백이다** — 그 폭에서 `.ba-cond-tx` 가용은 **79px** 인데 조건 하나가 이미 ~101px 이라, "첫 조건조차 안 들어가면 원문을 그대로 두고 CSS 말줄임에 맡긴다"는 규칙이 발동한다(빈 칸보다 낫다). 오늘과 같은 모습이라 회귀는 아니다.
**후속 후보**: narrow 에서는 요약 텍스트를 아예 감추고 '조건 N개' 배지만 남기는 안 — 그 폭에서 요약은 어차피 정보가 아니라 잡음이다. 정보 손실이 있어 미적용.

**구현 함정 2개**
- **줄인 뒤의 `clientWidth` 는 '가용폭'이 아니라 '줄어든 내용의 폭'이다.** 그대로 다시 재면 넓혀도 복원되지 않고 줄어든 상태에 갇힌다 → 매번 ① 전부 원문 복원 → ② 읽기 → ③ 쓰기 순서로 돈다(읽기/쓰기를 섞으면 카드마다 강제 리플로).
- **백그라운드 탭에서는 `requestAnimationFrame` 이 멈춘다.** 폭 재측정을 rAF 로 묶어 뒀는데, 검증 중 대상 탭이 뒤에 있어 "넓혀도 안 늘어난다"로 보였다 — 코드 문제가 아니었다(탭을 앞으로 가져오니 정상). **하네스 검증 시 탭이 활성인지 먼저 확인할 것.** 실사용에서는 드래그 중 탭이 항상 활성이라 문제되지 않는다.
- 재측정 실패를 `catch (_) {}` 로 삼키고 있었다 → `console.warn` 으로 바꿨다. 삼키면 "넓혀도 조건이 안 늘어난다"로만 보여 원인 추적이 불가능하다.

파일: `src/lib/fitSummary.js`(신규), `src/content/panel/renderList.js`(호출·재노출), `panel.js`(폭 변경 시 rAF 재측정), `test/fitSummary.test.js`(신규 7).

---

### 패널 폭에 '넓게/좁게의 이유' 만들기 — 폭 밴드 (2026-08-13, **미커밋 · 하네스 검증 완료 · 라이브 미검증**)

**제보**: "넓게/좁게 사용할 때의 장단점들이 있어야 할 것 같아. 지금은 패널을 그저 넓히기만 하니까."

**먼저 짚은 것**: 최소폭 = 기본폭 = 384px 이라 **'좁게'라는 선택지가 아예 없었다.** 사용자가 두 권장안을 모두 채택 — ① 넓히면 카드를 한 줄로 편다 ② 최소폭을 낮춰 좁은 쪽을 되살린다.

**설계 — 폭 밴드 3단** (`src/lib/panelWidth.js`, 순수 함수 + 테스트)

| 밴드 | 폭 | 성격 | 카드 높이 | 조건 요약 폭 | 한 화면 북마크 | 액션 행 |
|---|---|---|---|---|---|---|
| narrow | 300~383 | 거래소를 덜 가림 | 108px | 76px | 2.2 | **1줄**(아이콘만) |
| default | 384~599 | **조건 요약이 가장 길다** | 108px | 160→375px | 2.7~3.1 | 1줄(384는 2줄) |
| wide | 600~880 | **항목이 가장 많다** | **75px** | 173→453px | **4.3~4.7** | 1줄 |

전부 하네스 실측(최악 폰트 system-ui·Malgun 고정). `.ba-root[data-w]` 속성으로 CSS 가 받는다.

**최소폭 384 → 300 의 근거** — 384 는 액션 행이 최악 폰트에서 336/342px 을 쓰기 때문이었다. narrow 밴드에서 **라벨(.ba-btn-lbl)을 버리고 아이콘만** 남기면 필요폭이 409 → 219px 로 떨어진다(실측). 라벨을 안 숨기면 300px 에서 즉시 줄바꿈이다 — **둘은 한 세트**라 `actionRowBudget.dom.test.js` 에 "라벨은 전부 .ba-btn-lbl 안에 있다" 가드를 넣었다(래퍼가 빠지면 CSS 만 봐선 아무도 모른다). 뜻은 `data-tip` 툴팁이 그대로 갖는다(실측 확인).

**wide 경계를 560 → 600 으로 올린 이유 (측정이 뒤집은 판단)**
줄을 합치면 조건 요약이 **이름 줄 폭만큼(~203px) 줄어든다.** 격차가 상수라 어느 폭에서도 wide 가 같은 폭 default 보다 넓어지지 않는다. 560 으로 두면 559→560 에서 조건 요약이 335 → 132px 로 **1px 넓혔는데 글자가 짧아진다** — 버그로 읽힌다. 그래서 **기본폭(384)보다 두 축 모두 나아지는** 지점으로 옮겼다: 600 에서 조건 요약 160→173px, 카드 109→75px. 손익분기 587 + 여유.
→ 결과적으로 폭마다 성격이 달라졌고, 그게 요청받은 "장단점"이다.

**⚠ wide 밴드가 합치는 짝을 바꿨다 (2026-08-13, 라이브 검증이 내 판단을 반박)**
처음엔 **이름 줄 + 조건 요약**을 합쳤다. 라이브 스윕(북마크 26개)에서 그게 회귀임이 드러났다:

| 폭 | 밴드 | 조건 요약 온전 |
|---|---|---|
| 520 | default | 25/26 |
| **560~599** | default | **26/26** |
| **600** | **wide** | **19/26** ← 급락 |
| 640~800 | wide | 21~23 |
| 880 | wide | 26/26 |

**599 → 600 에서 잘림이 0건 → 7건.** 1px 넓혔더니 나빠지는, WIDE_MIN 을 600 으로 올릴 때 내가 피하려던 바로 그 증상이다. **기본폭 384 와만 비교해 경계를 정한 탓에**, 사용자가 실제로 지나가는 599→600 경로를 못 봤다.
원인: 줄을 합치면 조건 요약이 짝의 폭만큼 잃는다. 680px 실측 — **이름 줄 중앙값 244px vs 메모 119px**. 짝은 **좁은 쪽**이어야 한다.
→ **이름 줄 + 메모**를 합치고(`order` 0/1), **조건 요약은 단독 줄로 전체 폭**을 갖게 했다(order 2). 히스토리 카드(.ba-meta)는 메모 줄이 없고 칩도 짧아 기존대로 이름 줄과 합친다.
**수정 후 — 라이브 확인 완료(북마크 26개)**: 384 20/26 · 480 23/26 · **560·599·600·640·720·880 전부 26/26 온전** — 599→600 급락 소멸(19→26). 카드 높이 109 → 82px(-25%)로 층수 이득 유지. 300(narrow) 11/26 은 설계된 폴백.

**wide 는 grid 가 아니라 flex-wrap** — 이름이 아주 길면 메타 행이 스스로 내려가 **지금과 똑같은 레이아웃으로 되돌아간다.** grid 로 칸을 못 박으면 그 경우 이름이 찌그러진다. 폭 예산이 세 번 깨진 영역이라 '넘치면 원래대로'가 되는 쪽을 택했다.
`flex-basis` 는 0 이어야 한다 — 처음에 220px 로 뒀더니 그 값이 줄바꿈 판정에 들어가 **560px 에서도 안 붙었다**(이름 줄 자연폭 중앙값 192·최대 289px). basis 0 + `min-width: 150px`.

**측정 함정 2개 (다음에 또 걸린다)**
- `.ba-row { content-visibility: auto; contain-intrinsic-size: 96px }` — 화면 밖 카드는 **자리표시자 크기(96px)를 보고한다.** 처음 측정에서 카드 높이가 전 폭 116px 로 나와 "wide 가 안 먹는다"고 오판했다. 측정 중에는 `content-visibility: visible` 을 강제해야 한다.
- 액션 행 줄 수를 자식 `top` 의 고유값으로 세면 **아이콘 묶음의 세로 정렬 차이가 줄바꿈으로 잡힌다.** 6px 허용오차로 묶어야 한다.

**부수 수정** — `applyWidth` 가 저장값이 있을 때만 불려서 **첫 사용자는 `data-w` 없이 떴다**(밴드 CSS 전체가 죽는다). 저장값 읽기 전에 한 번 부르도록 고쳤다. `clampPanelWidth` 는 `Number(null)===0` 때문에 "값 없음"이 "0px 요청"으로 읽혀 최소폭까지 쪼그라들던 것도 테스트가 잡아 고쳤다.

**전 폭 넘침 감사**: 300/340/383/384/560/880 에서 패널 밖 삐져나감·말줄임 없는 잘림 **0건**. 300px 에서 헤더 부제('POE1 TRADE MANAGER')가 13px 잘리던 것만 나와 narrow 밴드에서 감췄다(장식 문구, 제품명은 위 줄에 남는다). `.ba-*-glint` 2건은 부모에 클립되는 장식 스윕으로 384·880 에서도 동일 — 기존 상태.

**라이브 검증 (2026-08-13, 실제 거래소 + 실제 북마크 26개)** — 합성 PointerEvent 로 리사이즈 그립을 끌어 측정.

| 항목 | 결과 |
|---|---|
| 드래그로 폭 변경 | 384 → 634 → 880(상한) → 300(하한) ✅ |
| 밴드 전환 | default → wide → narrow 정확히 ✅ |
| **드래그 중 지연 해소** | 핸들 전환 `0.26s → 0s`, 페이지 전환 `0.25s → 0s`, 여백이 폭+28 로 **즉시** 추종(484→512, 684→712) ✅ |
| 놓은 뒤 원복 | 핸들 0.26s / 페이지 0.25s 로 복귀 ✅ |
| wide 카드 압축 | 109 → **74px**, 히스토리 2층→1층, 한 화면 북마크 2.0 → **3.6개(+80%)** ✅ |
| 폭 저장·복원 | 새로고침 후 684px 복원 ✅ |
| 마운트 시 밴드 | `data-w="wide"` 즉시 부착 ✅ (초기 `applyWidth` 수정 동작) |
| 조건 묶음 줄 | 그룹 밖·바로 앞, 좌측 57=57 제목선 일치, 들여쓰기 42px ✅ |
| 칩 호버 툴팁 | 7줄(이름+요약+구분선+조건 목록), 네이티브 title 없음 ✅ |

**라이브에서 잡은 버그 — 하한을 넘겨 끌면 폭이 384로 튀어 오른다**
로그: `오른쪽 200px → 300px(하한 정상)` → `오른쪽 900px → **384px**`. 좁히려고 계속 끄는데 패널이 되레 넓어진다.
원인은 직전에 넣은 `clampPanelWidth` 의 `n > 0` 가드다. `startW + delta` 가 음수가 되면 **"값 없음"으로 오인해 기본폭으로 되돌린다.** '값 없음'(null·undefined·''·NaN)만 따로 판정하고 숫자는 음수든 0 이든 하한에 눌러 담도록 고쳤다(회귀 테스트 추가).
→ 이 수정은 **확장 리로드 후 재확인 필요**(라이브 확인 시점의 확장에는 버그가 들어 있었다).

**스크린샷이 잡은 것 — 넘침 감사가 놓친 '줄바꿈' (2026-08-13)**
300px 화면을 눈으로 보니 두 가지가 깨져 있었다. **자동 감사는 둘 다 통과시켰다** — `scrollWidth > clientWidth` 로 넘침만 봤는데, 이 둘은 **넘치는 게 아니라 줄바꿈**이라 폭이 아니라 높이가 늘어난다. 앞으로 좁은 폭 감사에는 `getBoundingClientRect().height > 한 줄 높이` 검사를 함께 넣어야 한다.

| 증상 | 원인 | 수정 |
|---|---|---|
| 제품명이 'POE 북 / 마크 아 / 틀라스' 3줄 | 헤더 우측 고정 요소(제작자 칩 ~90px·♥·단축키)가 다 먹어 제목이 50px 만 받음(필요 128) | narrow 에서 **제작자 칩 접기** + `.ba-brand-tx b` 에 nowrap·말줄임 |
| 폴더명이 안 보임 (사용자 제보) | 폴더 행에서 **액션 아이콘 3개가 86px**, 이름은 **16px** (필요 54) | narrow 에서 액션을 **호버 시에만** 노출 (`@media (hover: hover)` 안에서만 — 터치 환경에선 그대로 둔다) |

⚠ **덤으로 드러난 기존 결함**: 최악 폰트(system-ui·Malgun)에서는 **기본폭 384px 에서도** 제품명이 2줄이 됐다(필요 136 / 가용 127). CSS 807행 주석의 "아이콘 과밀로 타이틀을 줄바꿈" 이 폰트에 따라 아직 살아 있던 것. nowrap·말줄임을 narrow 전용이 아니라 **기본 규칙으로** 올려 막았다(384 에서 9px 말줄임 — 3줄 쪼개짐보다 낫다).

**세 번째 병목 — 카드 이름 (같은 방식으로 이어서 발견)**
폴더명을 고친 뒤 다시 보니 **카드 이름이 26px 만 받고 121px 잘려 있었다**("반…", 라이브 실측 / 필요 147px).
- 1차: `.ba-l1l` 의 드래그 그립(23px)·썸네일(28px)을 narrow 에서 접음 → **30px 회수에 그침**(여전히 68px 잘림).
- 2차: 남은 병목은 **가격 칩 72px(flex:none)**. 좁은 폭에서 카드의 핵심은 "어느 북마크인가" = 이름이므로 `.ba-line1` 을 narrow 에서 `flex-wrap: wrap` 시켜 **가격을 아랫줄 우측으로** 내렸다.
- 결과(하네스 실측, 이름 '반지 - 화염 저항 세팅(고급)' 161px): 이름 **61 → 161px, 잘림 0**. 대가는 카드 높이 **108 → 133px(+25)**. 좁게 쓰는 목적은 밀도가 아니라 '거래소를 덜 가림'이라 이 교환을 택했다.

**narrow 밴드에서 접는 것 — 한눈에**
| 요소 | 회수 | 이유 |
|---|---|---|
| 액션 행 라벨 | 190px | 행 전체가 줄바꿈됨 (MIN_W 300 의 전제) |
| 헤더 제작자 칩 | ~90px | 제품명이 3줄로 쪼개짐 |
| 헤더 부제 | — | 13px 잘림 |
| 폴더 액션 3개 | 86px | 폴더명이 16px 만 받음 (호버 시 노출) |
| 카드 그립·썸네일 | 51px | 카드 이름이 26px 만 받음 |
| 카드 가격 → 아랫줄 | 72px | 위 51px 로도 부족 |

수정 후 하네스 실측(최악 폰트): 300·340·480·880 에서 제품명 줄바꿈 없음·잘림 0, 폴더명 잘림 0, 카드 이름 잘림 0.
**남은 거친 부분**: 300px 에서 푸터 안내 문구가 3~4줄로 쪼개진다(장식 문구, 기능 영향 없음 — 미수정).

**하한 초과 드래그 수정 라이브 확인 완료** — 오른쪽으로 900px·2000px 넘겨 끌어도 300px 유지(예전엔 384로 튀어 올랐다).

⚠ **측정 함정 4 — `vite build` 는 로드된 확장을 끊는다.** dist/ 를 교체하는 순간 Chrome 이 언패킹 확장을 놓아버려 패널이 아예 주입되지 않는다(콘솔 오류도 없다). 라이브 검증 중 빌드를 돌렸다면 **chrome://extensions 에서 확장을 다시 로드**해야 한다 — 그 화면은 브라우저 도구로 접근할 수 없으므로 사용자에게 요청해야 한다. 대안: 하네스(localhost:5199)를 같은 Chrome 에서 열면 확장 없이 패널 레이아웃을 검증할 수 있다.

⚠ **측정 함정 3 — 창이 렌더링하지 않으면 전환이 멈춘 채로 계산된다.** 패널 `transform` 이 `translateX(906px)`(접힘 위치)인데 `collapsed` 클래스는 없고, `<html>` 인라인에 `margin-right: 712px !important` 가 박혀 있는데 computed 는 `0px` 이었다. 둘 다 `getAnimations()` 가 `running` 인 전환의 **정지된 시작값**이었다 — `a.finish()` 를 부르니 각각 `translateX(0)` / `712px` 로 정착했다. 스크린샷이 "not compositing frames" 로 실패하는 환경에서는 computed style 을 그대로 믿으면 안 된다.

파일: `src/lib/panelWidth.js`(신규), `src/content/panel/panel.js`, `panel.css`, `renderList.js`(라벨 래퍼), `test/panelWidth.test.js`(신규 10), `test/actionRowBudget.dom.test.js`(+2).

---

### 조건 묶음 — 능력치 필터만 담기 + 로드 중 토스트 (2026-08-13, **미커밋 · 라이브 미검증**)

**제보 A: "조건 묶음으로 저장할 때에는 능력치 필터에 관련된 정보만 저장되어야 해. 현재는 아이템 검색 정보도 저장된다."**

`extractConditionSet` 이 `q.type` 을 함께 담고, `mergeConditionSet` 이 `q.type = set.itemType` 으로 **대상 검색의 아이템을 덮어쓰고** 있었다. 그러면 '얹기'가 아니라 '바꾸기'다. `q.type` 은 '아이템 검색' 칸에서 고른 기본 타입이다(아이템 유형 드롭다운은 `filters.type_filters.category` 로 별개이며 애초에 담지 않는다).
- `extractConditionSet` — `itemType` 제거. 능력치가 하나도 없으면 `null`(유형만으론 묶음이 되지 않는다).
- `mergeConditionSet` — `q.type` 을 건드리지 않는다. **예전에 저장된 묶음의 `itemType` 은 여기서 무시되므로 저장분 마이그레이션이 필요 없다**(테스트로 고정).
- `conditionSetSummary` — 유형 제거. 남아 있는 `itemType` 을 보여주면 "이것도 적용된다"는 잘못된 인상을 준다.
- `panel.js addStatsToSearch` 의 `{ ...set, itemType: null }` 은 이 변경으로 고아가 돼 제거(같은 일을 공통 경로가 한다).

**제보 B: "툴팁이 페이지 새로고침되는 과정에서 이상하게 계속 보이는 문제"**

정체는 툴팁이 아니라 **패널 토스트**(`.ba-toast`, 뷰포트 42% 지점 고정)였다. 조건을 얹고 이동한 뒤 뜨는 안내다. 원인 2개:
- 우리 패널은 `document_idle` 에 뜨는데 거래소 화면은 한참 뒤에 그려진다 → **아무것도 없는 빈 화면 한가운데** 토스트만 떠 있다(제보 스크린샷이 정확히 그 상태). 게다가 그동안 메인 스레드가 막혀 자동 숨김 타이머(2.2s)도 늦게 돌아 더 오래 남는다. → **`load` 이후에 띄운다**(3초 상한 — 클릭과 안내가 너무 벌어지면 무슨 안내인지 모른다).
- `chrome.storage.local.remove('baSetApplied')` 를 **await 하지 않아서**, 거래소가 로드 중 한 번 더 이동하면 안 지워진 플래그가 다음 로드에서 또 토스트를 띄울 수 있었다. → 띄우기 **전에** await 로 소비. 15초 지난 흔적도 이제 함께 정리된다(예전엔 조기 return 이라 영구 잔류).

⚠ **반복 재현은 못 했다** — 단순 새로고침으로는 토스트가 뜨지 않았다(플래그는 소비되고 있다). 다만 폴링이 로드 8.6초 뒤에야 시작돼 초기 구간은 관측하지 못했다. 위 두 원인은 제보 스크린샷(빈 화면 + 토스트)과 코드 상 경합으로 확정한 것이며, 리로드 후 실사용 확인이 필요하다.

---

### 조건 묶음 칩을 거래소 화면에도 (2026-08-13, **미커밋 · 라이브 미검증**)

**요청**: "우선 패널에 두고 사용해보니, 눈에 띄지 않아서 사용성이 조금 아쉬웠어. DOM이 바뀌면 그때마다 우선 대응하는 걸로 하고, DOM에도 추가해보자."

**문제는 기능이 아니라 위치였다** — 조건을 만지는 동안 시선은 거래소 필터에 머무는데 칩은 화면 반대편 패널에 있었다. 그래서 **패널 칩은 그대로 두고**, 조건을 실제로 넣는 자리 바로 위(능력치 필터 그룹 맨 앞)에 같은 줄을 하나 더 놓는다.

**앵커는 실측으로 정했다** (라이브 DOM에서 능력치 필터 입력칸의 조상 사슬을 직접 읽음):
```
.filter-group (제목 "능력치 필터")
  ├ .filter-title.filter-title-clickable
  └ .filter-group-body        ← 여기 firstChild 로 삽입
      └ .filter.filter-padded  ← 첫 번째 능력치 필터
```
탐색 2경로: ① `.filter-group-body input[placeholder]` 중 `+ 능력치 필터 추가` → `closest('.filter-group-body')` ② 폴백 — 그룹 제목 텍스트. **라이브 검증: 12개 `.filter-group` 중 두 경로 모두 능력치 그룹만 골랐고 서로 일치.** 한/영 정규식(`능력치 필터` / `stat filters?`).

**유지보수 계약 (사용자 수용)** — 거래소가 마크업을 바꾸면 앵커를 못 찾는다. 그때는 던지지도, 아무 데나 붙이지도 않고 **조용히 아무것도 하지 않는다**. 패널 칩이 그대로라 기능은 사라지지 않고 화면만 예전으로 돌아간다. **앵커가 깨지면 `src/lib/pageSets.js` 하나만 고치면 된다** — 거래소 마크업 의존을 이 파일에 가뒀다.

**무한 루프 함정** — 주입 감시가 `document.body` 전역 MutationObserver 라, 매번 새로 그리면 우리 삽입이 감시를 다시 깨운다. `dataset.sig`(묶음 id+이름 JSON) 비교로 **같은 내용이면 노드를 재사용**한다. 회귀 테스트로 고정(`pageSets.dom.test.js`).

**배선**
- 렌더 트리거: `pobKick`(옵저버·100ms 디바운스) / `schedulePobInject` 타이머 / `storage.onChanged`(`conditionSets`) / 진입 직후 `[200,800,2000]ms`.
- **옵저버를 첫 fetch 가 아니라 진입 직후에 켠다** — 조건 줄은 검색 결과가 없어도(필터만 펼친 상태) 필요하다.
- 실패 문구는 패널과 **같은 사본**을 쓴다: `SET_FAIL` 을 `panel.js` 로컬 → `lib/conditionSet.js` 로 옮겨 양쪽이 import.

**과거 사고 두 건이 정확히 이 작업의 위험군이었다**
- `efa9011` 늦은 배선으로 버튼이 죽던 문제 → `panel.toast` 참조는 클릭 시점 해석(선언 순서상 안전) 확인.
- `6c96af9`·`c3b3de4` 주입 요소 CSS 누락 → 라이브 드라이런으로 computed style 실측: 배경 `rgba(167,139,250,0.13)`, 테두리 `rgba(167,139,250,0.4)`, `transition-property: background, border-color, color, transform`(**`all` 없음**), 폰트는 거래소 `FontinSmallcaps` 상속. 줄 높이 41px·칩 25px·줄바꿈 없음(740px 폭, 칩 4개).

**폭 조정 (라이브 확인 후 즉시 수정)** — 처음엔 필터 행과 같은 740px 로 늘렸더니 "벗어난 느낌"이라는 제보. 실측: **칩이 448px 만 쓰고 292px(39%)가 빈 꼬리**였다. 다른 필터 행은 테두리가 없어 폭이 안 보이는데 우리만 상자라, 내용보다 큰 상자가 겉돌았다. → `width: fit-content; max-width: 100%`, 패딩 `7px 9px`→`5px 8px`. **결과 실측: 폭 740→456px, 빈 꼬리 292→9px, 높이 41→37px**(필터 행 30px 에 근접). 좌측은 그대로 필터 행과 맞물려 정렬 유지.

**배치 — 그룹 안 → 그룹 밖 위 (제보로 두 번 옮김)**

① 처음: `.filter-group-body` 맨 앞(첫 능력치 필터 위).
② "체크박스 안쪽으로 indent" → 필터 행 내용 열(42px)에 맞춤.
③ **"능력치 필터 안에 있으면 안돼. 외부에 있어야해"** → "능력치 필터" 제목 **밑**에 들어가니 그 그룹에 속한 필터처럼 보였다. 조건 묶음은 능력치 필터의 일부가 아니라 그 위에 얹는 별개 도구다. → **`.filter-group` 의 앞 형제**로 이동(`group.parentElement.insertBefore(bar, group)`). 능력치 그룹은 우측 페인(`.search-advanced-pane.brown`)의 첫 자식이라, 결과적으로 페인 최상단이다.

- 기준선도 함께 바뀌었다: 그룹 밖으로 나온 뒤로는 **그룹 제목**이 기준선이다(`titleIndent` — `.filter-title` 과 `.filter-group` 의 left 차이). 값은 **재서** 쓰고 못 재면 실측 기본 42px. **라이브 검증: 칩 줄 left 1003 = 제목 left 1003, 칩 줄 top 278 < 제목 top 321, `group.contains(bar) === false`.**
- **자리 고정에 실제 구멍이 있었다** — 시그니처가 같으면 그냥 반환하는 조기 반환 탓에, 거래소가 Vue 로 다시 그려 우리 줄이 밀리면 영영 그 자리에 남는다. 조기 반환 경로에서도 `old.nextElementSibling !== group` 이면 다시 그룹 앞으로 옮기도록 고쳤다(테스트로 고정 — 사이에 침입 노드를 넣고 재렌더하면 노드는 재사용하되 자리는 복구).
- `findStatFilterBody` → **`findStatFilterGroup`** 으로 교체(그룹 자체를 반환).

⚠ **라이브 미리보기 함정** — 실행 중인 확장이 자기 `#ba-page-sets` 를 옵저버로 100ms 내에 원래 자리로 되돌린다. 리로드 전 새 배치를 확인하려면 **다른 id 의 노드**로 미리보고 확장 것은 CSS 로 숨겨야 한다(첫 시도의 좌표는 되돌려지기 전 틱 값이라 오해했다).

**모션**(전역 §21): 진입 애니메이션 **없음** — 페이지 로드마다 보이는 고빈도 요소라 장식 모션은 넣지 않는다. 누름 피드백 `:active { scale(0.97) }`(160ms, `cubic-bezier(0.23,1,0.32,1)`)만 둔다 — 클릭이 네트워크 왕복이라 즉시 응답이 필요하다. hover 는 `@media (hover: hover) and (pointer: fine)`, reduced-motion 시 transform 제거.

**호버 툴팁 (제보 반영)** — 칩에 이름만 있으면 무슨 조건이 담겼는지 알 수 없다. 호버 시 **이름 · 요약 · 조건 목록(값 범위 포함)** 을 보여준다. 네이티브 `title` 이 아니라 기존 커스텀 툴팁(`#ba-page-tip` + `bindPageTip`) 재사용 — PoB 버튼·환산 칩과 같은 관례이고, 둘을 같이 쓰면 툴팁이 두 번 뜬다(테스트로 `title` 부재 고정).
**툴팁 본문은 패널과 같은 함수에서 나온다** — `conditionSetTip(set)` 을 `lib/conditionSet.js` 에 두고 패널·페이지가 함께 쓴다(패널에 인라인으로 있던 사본 제거). 두 표면이 갈라지지 않게 테스트로 고정.

**남은 것**: 확장 리로드 후 라이브 확인 — 칩이 실제로 뜨는지 / 클릭 시 조건이 얹혀 이동하는지 / 이동 후 패널 토스트가 뜨는지 / 묶음 0개면 줄이 안 뜨는지 / 호버 툴팁 위치.

파일: `src/lib/pageSets.js`(신규), `src/lib/conditionSet.js`(`SET_FAIL`·`conditionSetTip` 이관), `src/content/content-main.js`, `src/content/panel/panel.js`, `test/pageSets.dom.test.js`(신규 16).

---

### 폭 드래그 중 핸들·페이지가 뒤따라오던 문제 (2026-08-13, **미커밋 · 수정 후 라이브 미검증**)

**제보**: "패널 너비를 조정하는 동안, 패널 버튼이 뒤늦게 뒤따라 오는 애니메이션 문제가 있어."

**원인 2개 — 라이브 실측으로 확정** (수정 전 확장에서 computed style 직접 읽음):

| 요소 | transition | 폭에 물림 | 결과 |
|---|---|---|---|
| 패널 `.ba-root` | `transform` 0.26s | 아니오 (`width: var(--ba-w)`) | 즉시 ✅ |
| 핸들 `.ba-handle` | **`right, left` 0.26s** | 예 (`right: 417px` = 14+403) | **260ms 지연** |
| 페이지 `<html>` | **`margin` 0.25s** | 예 (`margin-right: 431px` = 403+28) | **250ms 지연** |

두 전환 모두 **접기/펼치기용**이다 — 그때는 `right`·`margin` 이 한 번에 크게 움직여서 전환이 있어야 자연스럽다. 그런데 폭이 `--ba-w` 로 묶이면서 **드래그 매 프레임마다 새 전환이 걸리게** 됐다. `panel.js:179` 주석에 "전환 없이 반영"이라 적어 뒀지만, 그건 패널 본체 얘기였고 **핸들·페이지가 가진 별도 전환은 놓쳤다**(0.6.6 폭 조절 기능 도입 시 함께 들어온 회귀).

**수정** — 드래그 중에만 끈다(접기/펼치기 전환은 보존):
- `panel.js`: `resizing` 플래그 + `elRoot.classList.add('ba-resizing')` (pointerdown) / remove (pointerup·pointercancel)
- `applyPagePush`: `transition` 을 `resizing ? 'none' : 'margin .25s ease'` 로
- `panel.css`: `.ba-root.ba-resizing ~ .ba-handle { transition: none; }`
- 드래그 종료 시 `applyPagePush(isCollapsed())` 를 한 번 더 불러 **꺼 뒀던 페이지 전환을 되돌린다** — 안 그러면 다음 접기/펼치기가 뚝 끊긴다.

**테스트 없음(의도)** — 패널을 마운트하는 테스트 하네스가 아직 없고, jsdom 은 레이아웃 엔진이 없어 지연을 측정할 수 없다. 클래스 토글 하나를 위해 하네스를 세우는 건 비용이 크다(전역 §7.2.4). **원인은 라이브 실측으로 확정**했고 수정 후 상태는 리로드 후 확인이 필요하다.

---

### 패널 너비 드래그 조절 (2026-08-13, `adcd09a` **커밋·push 완료 · 라이브 미검증**)

**요청**: 패널 너비를 조정할 수 있으면 좋겠다. **최소는 현재 너비(384px)로 제한**(사용자 결정).

**최소를 현재 폭으로 두는 게 핵심이다** — 이번 세션 실측에서 액션 행이 최악 폰트 기준 가용 342px 중 **336px(98%)** 를 쓰고 있었다. 더 좁히면 레이아웃이 흔들린다. **넓히기만 허용하면 기존 폭 예산과 회귀 가드(`actionRowBudget.dom.test.js`)가 그대로 하한으로 유효**하다.

**핵심 설계 — 폭 결합 4곳을 CSS 변수 하나로 수렴**
예전엔 폭 파생값이 각각 하드코딩돼 있었다(`width: 384px` / 핸들 `right: 398px` / 좌측 핸들 `left: 398px` / 페이지 밀어내기 `412px`). 한 곳만 고치면 **핸들과 패널 사이에 틈이 생긴다**(CSS 주석에 그 경고가 이미 있었다).
→ `:host` 에 **`--ba-w`** 선언, 나머지는 전부 파생: `width: var(--ba-w)` / `right: calc(14px + var(--ba-w))` / 페이지 밀기는 JS 에서 `panelW + 28`.

**구현**
- `applyWidth(w)` — `MIN_W(384)` ~ `maxW()`(= `min(880, innerWidth - 160)`, 거래소를 완전히 덮지 않게)로 클램프하고 호스트 인라인으로 `--ba-w` 를 덮는다
- 그립은 패널 **안쪽** 변(우측 배치면 왼쪽). 잡는 곳이 안쪽이라 **방향이 배치에 따라 뒤집힌다** — 우측 배치는 왼쪽으로 끌수록 넓어진다
- **저장은 놓을 때 한 번만**(`pointerup`) — 드래그 중 `chrome.storage` 에 쓰면 다른 탭 `onChanged` 가 매 프레임 깨어나 재렌더된다
- 창 축소 시 `resize` 로 재클램프, `storage.onChanged` 로 탭 간 동기화
- 모션 없음(포인터를 즉시 따라가야 한다 — 조건 묶음 드래그와 같은 원칙)

검증: 테스트 **364/364**, 빌드 통과. 하네스에서 **폭 변경·콘텐츠 리플로우 확인**(386 → 482 → 642px).
**⚠ 핸들 밀착·페이지 밀어내기는 하네스에서 검증 못 함** — 이 환경 하네스는 `position: fixed` 측정이 앞뒤가 맞지 않는다(핸들에서 `--ba-w` 는 384px 로 풀리는데 computed `right` 는 0px, 접힘 클래스가 없는데 transform 은 132%). 실사이트 측정은 이번 세션 내내 정확했으므로 **거래소에서 확인할 것**: ①드래그로 넓어지는지 ②384px 미만으로 안 줄어드는지 ③접힘 핸들이 계속 붙어 있는지 ④페이지가 폭만큼 밀리는지 ⑤새로고침 후 유지되는지 ⑥좌측 배치에서 방향이 맞는지.

### 북마크가 엉뚱한 검색을 저장하던 문제 — 원인 2개 (2026-08-13, **미커밋**)

**제보(네이버 카페 #468)**
- 서로 다른 두 링크(`d808O8ZPhJ` / `q909gqpptg`)를 **북마크가 같은 조건으로 인식**
- 매물 0건 검색(`bGDj9VLKUL`, 찬란한 허영심)은 **처음엔 저장이 안 되고**, 이후 다른 검색을 저장하면 **직전 검색이 저장**됨

**먼저 확인한 것**: 세 검색의 raw query 를 API 에서 받아 `searchIdentity`·`structuralIdentity` 에 직접 넣어보니 **두 검색은 정상적으로 '다름'으로 판정된다**(enchant 필터 유무·가격 80/70·disabled 차이). 즉 식별 함수의 문제가 아니었다. → 저장 경로를 팠다.

**원인 ① — 저장이 "현재 검색"이 아니라 "히스토리 맨 위"를 집는다**
`panel.js doSave` 는 `listByKind('history', game)[0]` 을 저장한다. 그런데 히스토리는 **`fetch` 브리지 메시지가 와야만** 기록됐다(`content-main.js` 의 `d.kind === 'fetch'` 분기). **결과가 0건이면 거래소가 `/api/trade/fetch` 를 호출하지 않으므로** 그 검색은 히스토리에 영영 안 남고, 저장 버튼은 직전 검색을 집는다. 제보의 두 증상(①처음엔 저장 불가 ②직전 검색이 저장됨)이 정확히 이걸로 설명된다.
→ **`recordSearch()` 신설**: `search` 메시지 시점에 **먼저 기록**하고, `fetch` 가 오면 시세·아이콘을 얹는다. `addHistory` 가 `dedupeKey` 로 병합하므로 두 번 불러도 항목이 늘지 않는다. 선기록에는 `icon`·`snapshot` **키를 아예 넣지 않는다** — `undefined` 로 넣으면 순서가 역전됐을 때 먼저 온 시세를 지운다.

**원인 ② — 링크로 연 검색은 조건을 못 읽어 전부 한 키로 뭉갰다**
브리지는 요청 **본문**에서 query 를 꺼내는데, **링크로 열면 앱이 `GET /api/trade/search/<hash>` 를 호출해 본문이 없다** → `d.query === undefined`.
그러면 `dedupeKey(undefined)` 가 `searchIdentity({})` = **`"t:|n:"`** 로 계산돼 **링크로 연 모든 검색이 같은 키**가 되고 `addHistory` 가 한 항목으로 병합한다. **"두 링크가 같은 조건"의 직접 원인.**
→ GET **응답 본문에 query 가 그대로 들어 있다**(`{id, query}` — 실측 확인). `d.query || d.data.query` 로 폴백. query 를 끝내 못 얻으면 **기록하지 않는다**(뭉개진 키로 남기느니 안 남기는 편이 낫다).

**검증 — 제보된 세 링크로 라이브 확인 완료 (2026-08-13, 'POE 브라우저')**
- **원인 ②**: `d808O8ZPhJ` → 히스토리 `…msqkml4e`, `q909gqpptg` → `…msqkn9cr` — **서로 다른 레코드**로 남았다. 수정 전이라면 둘 다 `poe1|t:|n:` 로 병합됐다
- **병합 동작**: 매물 있는 검색에서 `선기록(…msqko0uj)` → fetch → `히스토리 저장됨(…msqko0uj)` — **같은 id**. 항목이 늘지 않고 시세만 얹혔다
- **원인 ①**: 진짜 0건 검색(`total: 0`)을 만들어 확인 — `선기록(…msqkoskt)` 로그만 남고 **fetch 메시지·저장 로그가 아예 없는데도 히스토리 맨 위에 기록**됐다. 수정 전이라면 이 검색은 히스토리에 없고 저장 버튼이 직전 검색을 집었다
- 테스트 **364/364**(신규 5건 — `addHistory` 병합 3건, 키 붕괴 고정 2건), 빌드 통과, 콘솔 오류 0

**참고**: 검증 중 사용자 계정 히스토리에 항목 3~4건이 추가됐다(제보 링크 3개 + 합성 0건 검색 1개). 히스토리는 `HISTORY_CAP`(200)으로 자동 정리되는 휘발성 데이터라 별도 조치는 하지 않았다.

### 주얼 반경 '작게'가 번역되지 않던 문제 — 추측한 매핑 정정 (2026-08-06, **미커밋**)

**제보**: 불가능한 탈출(Impossible Escape)을 PoB 복사하면 `Radius: 작게` 로 한글이 남는다.

**원인 — 내가 KR 표기를 재지 않고 추측했다.** 0.6.5에서 반경을 추가하며 `{ 대형, 중형, 소형 }` 으로 매핑을 넣었는데, **실제 값은 '작게'** 였다. 이번 세션 내내 "측정 먼저"를 지켜왔는데 여기서만 어겼다.

**실측 (거래소 API 실아이템)**
```json
{"name":"적용 반경","values":[["작게",0]],"displayMode":0,"type":24}
```
- 속성 이름은 `반경` 이 아니라 **`적용 반경`** (기존 정규식 `/반경|Radius/` 이 부분일치로 잡고 있어 이쪽은 무사했다)
- 값에 **`type: 24`** 라는 언어 무관 식별자가 붙는다
- 확인된 값 2종: **`작게`**(불가능한 탈출·진청록색 주얼) / **`대형`**(완력 해결 등·코발트색 주얼)
- ⚠ **국지화 어형이 일관되지 않다** — 같은 속성에 부사형(`작게`)과 명사형(`대형`)이 섞인다. 그래서 어형 규칙으로 유추할 수 없다

**값 목록의 출처가 실아이템뿐이다**: 거래소 `data/filters`·`data/stats` 어디에도 반경 값 열거가 **없음을 확인했다**. 주얼 카테고리·유니크 주얼로 표본을 모아도 위 2종만 나왔다(반경 속성 자체가 일부 주얼에만 있다).

**수정 방침 — 모르는 값은 추측해 채우지 않는다.** `RADIUS_EN = { 작게: 'Small', 대형: 'Large' }` 로 **실측분만** 두고, 나머지는 기존 폴백대로 **원문 그대로 내보내고 `missing` 에 `radius:<원문>` 으로 기록**한다. 제보가 오면 그 값을 추가하면 된다. (추측으로 채우는 게 이 버그의 원인이었다.)

검증: 제보 아이템 실데이터로 `Radius: Small` + `missing: []` 확인. 테스트 **359/359**(신규 2건 — `작게`→Small 실측 고정, 속성명 `적용 반경` 매칭).

**관찰(미조치)**: 이 아이템 출력 끝에 `Passage` 한 줄이 붙는다. 폴백 사전(`pobUniqueModMap`)의 원본 데이터가 그렇게 갖고 있어서인데(`…without being connected to your tree #% to all Elemental Resistances Passage`), 제보 범위 밖이라 손대지 않았다. PoB 는 모르는 줄을 무시하므로 무해하다.

### "Extension context invalidated" 콘솔 오류 + 죽은 패널 무안내 (2026-08-06, **미커밋**)

**제보**: 확장 리로드 후 `Uncaught (in promise) Error: Extension context invalidated.` × 4 (`content-main.js`).

**성격**: 확장을 리로드·업데이트하면 **이미 열려 있던 탭**의 콘텐츠 스크립트는 고아가 되고 이후 모든 `chrome.*` 호출이 던진다 — **브라우저 동작상 정상**이다. 문제는 우리가 그걸 **안 잡아서** ① 콘솔이 uncaught 로 더럽혀지고 ② **사용자는 패널이 왜 죽었는지 모른 채 남는다**는 것.

**샌 곳 2군데(자동 실행 경로라 클릭 없이도 터진다)**
- `ensureSchema()` — 최상위에서 캐치 없이 호출. 리로드 직후 바로 던진다
- `window.addEventListener('message', async …)` — **async 리스너**라 내부에서 던지면 그대로 unhandled rejection. 안에서 `addHistory`·`markUsedByUrl`·`backfillQuery`가 `chrome.storage`를 만진다. **검색할 때마다 발생**해서 오류가 여러 개였던 것도 이걸로 설명된다

**수정**
- `isCtxInvalidated()` + `noteExtensionDead()` — 무효화를 식별해 **1회만** 안내. 배너에 **[새로고침] 버튼**을 달아 복구 경로를 준다(값은 전부 `textContent`)
- `guard(p)` — 자동 실행 진입점 2곳(`ensureSchema`, bridge 핸들러)에 적용. 리스너 본문은 `handleBridgeMessage()`로 분리해 감쌌다
- **전역 안전망 `unhandledrejection` 리스너** — `renderList.js`에만 async 클릭 리스너가 10곳 넘고 전부 `chrome.storage`를 만진다. 각각 감싸는 대신 한 곳에서 잡는다(앞으로 추가될 리스너까지 자동으로 덮인다). **컨텍스트 무효화만 삼키고 나머지는 그대로 흘려보낸다** — 진짜 버그를 숨기면 안 되므로

**검증 (2026-08-06 라이브, 'POE 브라우저')**
- 새 번들 적재 확인(`content-main.js-CFsWzSGy.js`), 검색 3회 전부 `히스토리 저장됨` — **리스너를 `handleBridgeMessage()`로 분리한 리팩터가 정상 경로를 안 깼다는 직접 증거**(`addHistory`가 `chrome.storage`까지 도달). 콘솔 오류 0
- 테스트 357/357, 빌드 통과, 산출물에 `unhandledrejection`·`ba-ctx-dead`·안내 문구 포함

**⚠ 전역 `unhandledrejection` net 은 실동작 미검증** — 재현하려면 거래소 탭을 **살려둔 채** 확장만 무효화해야 하는데(비활성화가 확실한 트리거), 시도 3회 모두 탭·브라우저가 새로 뜨면서 컨텍스트가 살아 있었다(`간략히` 토글이 정상 동작 = `chrome.storage.local.set` 성공으로 확인).
합성 `PromiseRejectionEvent` 로도 못 잰다 — **콘텐츠 스크립트는 격리 월드라 페이지 컨텍스트에서 던진 이벤트가 닿지 않는다.**
**다만 신고된 오류 4건은 이 net 이 아니라 `guard()` 두 곳에서 나온 것**이고(스택의 `content-main.js`와 일치), 그쪽은 일반 Promise `.catch` 라 월드와 무관하게 확실하다. **즉 제보된 문제 자체는 해결됐다.**
net 은 "무효화 이후 사용자가 패널 버튼을 누른" 더 좁은 경우용 보강이다. 그 경우에도 uncaught 가 새면 net 대신 `renderList.js` 의 async 리스너 15곳을 직접 감싸는 방식으로 교체할 것(코드 주석에도 남김).

### 찜한 매물 보강(썸네일·날짜) + '상태 확인' 버튼 CSS 누락 + 조건부 UI 전수 점검 (2026-08-06, **미커밋**)

**⚠ '상태 확인' 버튼은 내가 만든 결함이다.** `.ba-watch-check`에 **CSS를 아예 안 써서** 브라우저 기본 버튼으로 렌더됐고, 아이콘(SVG block)이 윗줄로 줄바꿈돼 2줄로 찌그러졌다(제보 스크린샷).
**2026-07-27 '선택' 버튼과 완전히 같은 원인** — 새 버튼을 공용 버튼 스타일 목록에 넣지 않은 것. 찜 기능을 추가하며 같은 실수를 반복했다.
→ `.ba-watch-check` 규칙 신설(`inline-flex` + `align-items` + `nowrap` + `flex: none`). **높이 37px(2줄) → 27px(1줄).**
**규칙: 패널에 새 버튼을 추가하면 반드시 `inline-flex`·`align-items: center`·`white-space: nowrap`을 갖는 규칙에 편입시킨다.**

**찜 카드 보강(사용자 요청)**
- **아이템 이미지** — 북마크 카드와 같은 `.ba-thumb` 재사용(`isAllowedIconUrl`로 `web.poecdn.com`만 허용). **28px 고정이라 이미지가 안 떠도 레이아웃이 안 흔들린다**(우리가 쫓던 결함 부류가 여기선 이미 막혀 있다)
- **찜한 시점** — 기존 `ago()` 헬퍼 재사용(`2시간 전`·`3일 전`·`12일 전`). 매물은 시간이 지날수록 죽을 확률이 커져 이 값이 곧 신선도 힌트다. 액션 행 왼쪽 빈 공간에 배치해 세로 공간을 안 쓴다
- `.ba-wname`을 고정 `max-width: 190px` → **`flex: 1 1 auto; min-width: 60px`**. 썸네일이 앞에 붙은 뒤에도 배지가 밀리지 않게

**결과 행 버튼 순서 — ★ 를 PoB 앞으로**(사용자 요청). `group.append(btn, star)` → `group.append(star, btn)`.

**조건부 UI 전수 점검 — 6개 상태 전부 깨끗**
지난번 점검이 "안 띄운 요소는 측정되지 않는다"로 실패했으므로, 이번엔 조건부 상태를 **시드로 강제 노출**한 뒤 쟀다. `.ba-attn--del`('차단된 링크', 6자로 '오래됨'보다 길다)도 하네스에 상시 시드.

| 상태 | 줄바꿈 | 패널 밖 |
|---|---|---|
| 기본 화면(오래됨·**차단된 링크**·지난 리그 배지) | 0 | 0 |
| ⋯ 액션 팝오버 / 설정 모달 | 0 | 0 |
| 저장 충돌 다이얼로그 / 폴더 이동 모달 | 0 | 0 |
| 토스트 | 0 | 화면 안(`word-break: keep-all`) |

검증: 테스트 **357/357**, 빌드 통과. 찜 카드 3종(썸네일 有/無, 배지 1·2개, 긴 이름) 전부 패널 안 수납·액션 행 1줄 확인. **결과 행 ★/PoB 순서는 거래소 페이지에서만 보이므로 사용자 확인 필요**(하네스엔 결과 행이 없다).

### '오래됨' 배지가 세로로 쪼개지던 문제 + 전수 점검의 구멍 메우기 (2026-08-06, **미커밋**)

**제보(스크린샷)**: 북마크 카드의 '오래됨' 배지가 `오/래/됨` 세 줄로 쪼개짐. **2026-07-27 '선택' 버튼과 같은 실패 모드.**

**⚠ 바로 앞 커밋(`222fe12`)의 레이아웃 전수 점검이 이걸 놓쳤다.** 이유가 중요하다 —
**하네스 시드에 오래된 북마크가 없어 `.ba-attn` 배지가 한 번도 렌더된 적이 없었다.**
조건부로만 나타나는 요소는, 띄우지 않으면 측정 대상에 아예 들어오지 않는다.
→ **하네스에 30일 지난 북마크를 상시 시드**해 이 상태가 항상 화면에 뜨게 했다(`harness.mjs`).

**원인**: `.ba-attn`(panel.css:555)에 `flex: none`도 `white-space: nowrap`도 없었다.
같은 행(`.ba-meta-row`)의 `.ba-more`는 `flex: none`을 갖고 있는데 이 배지만 빠져 있었다.
그래서 옆의 조건 칩이 길면 배지만 찌그러지고 한글이 글자 단위로 세로 줄바꿈된다.
**실측: 배지 높이 53px(3줄) → 수정 후 23px(1줄).** 대신 줄어드는 `.ba-cond--summary`엔 이미 말줄임이 걸려 있다.

**패턴 전수 조사 — 같은 결함 1건 더 발견**
"flex 행에서 줄어들 수 있고(`flex-shrink: 1`) 줄바꿈이 허용된(`white-space: normal`) 짧은 한글 라벨"을 전부 뽑아 **부모를 강제로 좁혀 압축을 유발**했다. 후보 8건 중 실제로 무너진 건 하나:
- **`.ba-sort-seg`(정렬 순서/최근/이름)** — 압축 시 27px → 43px(2줄). `.ba-seg > *`에 `white-space: nowrap` 추가로 해결(설정 모달의 패널 위치·퍼지 검색 세그먼트도 같이 덮인다).
- 나머지 6건(`.ba-econ-lbl`·`.ba-watch-check`·`.ba-wopen`·`.ba-clear-hist`·`.ba-foot-guide`·`.ba-wdel`)은 압축 시뮬레이션에서 **견뎠다** → 손대지 않았다.

검증: 정렬 세그먼트 압축 시 27px 유지 / 오래됨 배지 23px 1줄 / **전체 재스캔에서 줄바꿈 중인 짧은 한글 라벨 0건**. 테스트 357/357, 빌드 통과, 콘솔 에러 0.

**교훈(다음 점검 때 반드시)**: 조건부 UI(오래됨·차단된 링크·지난 리그 배지, 오래된 정리 버튼, 각종 다이얼로그)는 **시드로 강제 노출한 뒤** 재라. 안 띄운 것은 측정되지 않는다.

### UI 레이아웃 전수 점검 — 폭 예산 감사 + 회귀 가드 (2026-08-06, **미커밋**)

액션 행이 **같은 원인으로 3번 깨진** 뒤, 같은 부류가 더 있는지 하네스에서 **측정으로** 전수 점검했다(4px 차이라 육안으로는 못 잡는다).

**① 가변 데이터는 전부 안전** — 북마크명·메모·조건칩·폴더명·조건묶음칩·찜 아이템명/메타에 **27자 공백 없는 한글**을 넣어도 넘치지 않는다. 대부분 말줄임 처리, 이름·메모는 wrap.

**② 위험한 행은 액션 행 하나뿐** — 이력과 일치한다.

| 행 | 자식 | 여유 | 판정 |
|---|---|---|---|
| **액션 행** | 4 (전부 고정폭) | **6~13px** | ⚠ 포화 |
| 섹션 헤더 3종 | 2 | 122~168px | ✓ |
| 폴더 헤더·카드 1행 | 유동 자식 有 | 자동 조정 | ✓ |
| 찜 카드 상·하단 | 2 | 215px | ✓ |
| 패널 헤더(column)·푸터(의도된 2행) | — | N/A | ✓ |

오탐 2건도 확인해 제외했다: `glint` 장식 요소는 `overflow:hidden` 부모가 자르고, 패널 헤더는 `flex-direction: column`이라 가로 예산 개념이 없다.

**③ 앞선 수정이 불완전했다 — 폰트 의존성**
`min-width: 92px`로 버튼 폭을 고정했는데, **넓은 폰트에서 고정이 풀렸다.** '전체 펼치기' 자연폭 실측: Arial·Noto Sans KR 89.7 / serif 92.5 / **system-ui·Malgun Gothic 94.9**.
**패널이 선언한 Pretendard는 번들돼 있지 않다** — 실제 사용자는 폴백으로 떨어지고, system-ui(Segoe UI)에서 92px를 넘겨 **3px 변동이 남아 있었다.** → **96px로 상향**. 이제 5개 폰트 전부에서 96px 고정 + 상태 간 행 폭 동일.

**④ 회귀 가드 신설 — `test/actionRowBudget.dom.test.js`**
3번 다 원인이 같았다: **추가할 때 아무도 폭을 재지 않았다.** jsdom은 레이아웃을 계산하지 않아 픽셀을 못 재므로, 실측 예산을 **항목 수 + 라벨 글자수**로 근사해 고정했다.
**가드가 실제로 잡는지 과거 사고 2건을 재현해 증명했다**:
- 라벨을 '모든 폴더 펼치기'로 되돌림 → `expected 8 to be less than or equal to 6` ✗
- 액션 행에 버튼 1개 추가 → `expected 5 to be less than or equal to 4` ✗

**⚠ 남은 위험 — 액션 행은 포화 상태다.** 최악 폰트(system-ui) 기준 **가용 342px 중 336px 사용(98%)**. 여기에 뭘 더하면 거의 확실히 넘친다.
**다음에 이 행에 무언가 추가해야 하면 상한을 올리지 말고 버튼을 옮겨라** — 가장 유력한 후보는 사용 빈도가 낮은 **가져오기/내보내기 묶음(72px 확보)** → 설정 모달. 이번엔 투어 스텝(`.ba-io-group`) 의존이 있고 사용자 판단이 필요해 보류했다.

검증: 테스트 **357/357**(신규 3건), 빌드 통과, 콘솔 에러 0. 5개 폰트 × 2상태 실측.

### 액션 행 — '전체 펼치기' 상태에서 줄바꿈되던 문제 (2026-08-06, **미커밋**)

**제보**: "북마크에서 '모든 폴더 펼치기' 상태가 되면 이전과 같은 문제가 발생합니다. 글자수가 늘어나서 그런것같아요." — **제보자가 원인까지 정확히 짚었다.**

**실측 (하네스, 가용폭 342px)**
| 라벨 | 행 필요폭 | 결과 |
|---|---|---|
| 모든 폴더 **접기** | 336px | 1행 (6px 여유) |
| 모든 폴더 **펼치기** | 346px | **2행 (4px 초과)** |

**단 4px 차이로 행이 바뀌었다.** 즉 토글할 때마다 레이아웃이 흔들린다.

**중요한 발견 — 줄바꿈 자체는 못 없앤다**: `오래된 정리` 버튼(staleN>0일 때 조건부 노출)까지 붙으면 **어떤 라벨로도 한 줄에 안 들어간다**(최단 후보도 37px 초과). 5개 버튼이 384px 패널에 애초에 안 맞는다. 그래서 목표를 "한 줄 유지"가 아니라 **"상태 토글로 레이아웃이 변하지 않게"**로 잡았다 — 제보의 실제 불만도 그것이다.

**수정**: 라벨을 `전체 접기`/`전체 펼치기`로 줄이고 `.ba-collapse-all`에 **`min-width: 92px` + `justify-content: center`**. 실측 폭이 접기 79.5px / 펼치기 89.7px라 92px면 양쪽 다 고정된다. 전체 설명(`모든 폴더 접기/펼치기`)은 **툴팁이 그대로 갖는다**.

검증(하네스 실측): 정리 버튼 **없을 때** 접기·펼치기 모두 1행·버튼 92px(17px 여유), **있을 때** 모두 2행·92px. 토글 동작·폴더 3개 접힘/복원·툴팁 전체 설명 유지 확인. 테스트 354/354, 콘솔 에러 0.

⚠ **같은 행이 같은 원인으로 세 번째 깨졌다**(2026-07-27 버튼 추가 → 넘침 / 이번 라벨 길이). **이 행에 버튼이나 긴 라벨을 추가할 때는 342px 예산을 먼저 재라.** 여유는 현재 17px뿐이다.

### 찜한 매물(watchlist) — 개별 매물 저장 + 생존 확인 (2026-08-06, **미커밋**)

**제보**: "특정 판매자가 판매하는 특정 아이템을 찜/즐겨찾기하는 기능이 있으면 좋겠다"

**측정이 요구의 절반을 지웠다**
- 거래소에 **`account`(판매자 계정) 필터가 이미 있다**(`/api/trade/data/filters` 실측) → "이 판매자 물건 다시 보기"는 **오늘도 된다**(판매자로 검색 → 그 검색을 북마크). **판매자 찜은 만들지 않았다**
- **매물 재조회가 `id`만으로 된다** — `/api/trade/fetch/<id>`가 `query` 파라미터 **없이도** 200을 주고, 없는 매물은 `null`로 온다(실측). 원 검색이 만료돼도 생존·현재가 확인이 가능하다는 뜻 → 단순 북마크를 넘어 **추적**이 성립

**페르소나 판정**: PS **Go(조건부)** — ①판매자 찜 제외 ②생존 확인 없으면 No-Go에 가까움. Dev Lead **승인**. Simplicity Advocate **축소안 채택**(자동 갱신 제거 → 수동 버튼, 규모 절반). Designer **승인**(색 언어 이탈 1건 발견·수정). QA+Security **PASS**. Red Team 3시나리오 방어 ✓(호출부 확인).

**설계 판단 3건**
1. **`origin`을 키의 일부로 쓴다**(`listingId+origin`) — 카카오와 글로벌은 **매물 id 공간이 다르다.** 합치면 한쪽에서 조회했을 때 멀쩡한 매물이 `null`로 와서 **"판매됨"으로 조용히 오판**한다. 이번 세션에서 반복해 나온 부류라 미리 막았다
2. **`sourceUrl`로 재검색한다** — 아이템→조건 역추출(item → query)을 만들지 않는다. `conditionSet.js`는 raw query 기반이라 item 입력을 지원하지 않아 역추출은 그 자체로 대형 작업이다. 찾았던 검색을 다시 열면 충분하다
3. **상태 갱신은 사용자가 누를 때만** — 자동 폴링 없음(rate limit). 장기 보관 용도라 목록을 여는 순간이 곧 확인 시점이라 자동화 이득이 작다. **실제로 쓰이는 게 확인되면 그때 얹는다**

**구현**
- `store.js` — `watchlist` **별도 키**(records와 생명주기가 달라 `conditionSets`·`folders`와 같은 근거). `listWatched`/`addWatch`/`removeWatch`/`isWatched`/`applyWatchStatus`, `WATCH_CAP = 100`
  - `addWatch`는 **조용히 버리지 않는다** — 중복·상한 초과 시 `{ok:false, reason}`을 돌려주고 UI가 토스트로 알린다
  - `applyWatchStatus`는 **넘긴 항목만** 갱신 — 확인 못 한 것(다른 거래소)을 `sold`로 떨어뜨리면 거짓말이 된다
- `content-main.js` — 결과 행 ★ 버튼. PoB 버튼과 **한 그룹**(`.ba-row-btns`)으로 묶어 기존 3단 배치 폴백을 그대로 상속(신규 순회 0). `listing.account.name` 캡처 추가
- `renderList.js` — '찜한 매물' 섹션(북마크와 히스토리 사이). 배치 10개 **순차** 재조회, **현재 페이지와 origin이 같은 매물만** 대상이고 나머지는 `다른 거래소` 배지로 명시
- `panel.js` — `toast`를 반환 API에 노출(페이지 표면 ★에서도 같은 토스트를 쓰기 위해)
- `panel.css` — 상태 배지. ⚠ **`--ba-drop`(라임)은 '드래그로 놓을 자리 전용' 토큰이라 재사용하지 않았다.** '있음'은 일부러 약하게 두고 **'판매됨'만 강조** — 강조를 나눠 가지면 둘 다 안 보인다

**미착수**: 자동·주기 갱신, 판매자 찜(거래소 `account` 필터로 대체), 아이템→조건 역추출, 타 origin 원격 확인(서비스 워커 경유)

**일몰 조건**: 한 리그(≈3개월) 내 찜 생존율 20% 미만 또는 사용자 5% 미만이면 축소·제거 검토

검증: 테스트 **354/354**(신규 10건 — origin 분리·상한 거부·미확인 항목 불변 포함), 빌드 통과. 하네스 실측: 배지 4종 렌더·가격 변동(`3 divine → 5 divine`)·긴 이름 말줄임·오버플로 없음·해제 동작·**실패 경로(토스트+버튼 복구+카드가 sold로 오변경되지 않음)**·콘솔 에러 0. **실제 거래소에서의 ★ 주입·상태 확인은 사용자 확인 필요**(하네스엔 결과 행·실 API가 없다)

### 한↔영 거래소 전환 — geo-redirect 전제 오진 정정 + 챌린지 대응 (2026-08-06, **미커밋**)

**결론부터: 프로젝트가 오래 믿어온 "pathofexile.com이 한국 IP를 카카오로 geo-redirect한다"는 전제가 측정으로 뒷받침되지 않는다.** 실제로 걸리는 건 Cloudflare 봇 챌린지이고, 화면이 깜박이던 원인의 상당 부분은 **우리 확장이었다.**

**측정 (KR IP, curl)**
| 대상 | 결과 |
|---|---|
| `/trade` | `403` `Cf-Mitigated: challenge`, **Location 없음**, `CF-RAY: …-ICN` |
| `/trade2` | **`/trade`와 동일** |
| `/api/trade/data/leagues` | `200` |

- **리다이렉트가 아니다** — 3xx도 `Location`도 없다. 봇 차단이다
- **`/trade`와 `/trade2`가 동일** → `cross-site-receiver.js` 주석·`CLAUDE.md`·배포 문서의 "PoE1만 접근 가능, PoE2만 geo-redirect"는 **근거 없는 서술이었다**
- **API는 통과** → 예전에 "API는 되는데 사이트는 안 된다"를 geo 근거로 읽었지만, 실제로는 **챌린지가 HTML 경로에만 걸리는 것**이다
- **사용자 확인**: 확장을 끄면 깜박임이 사라지고 pathofexile에 그대로 머문다. 단 **재현율이 매우 낮아** "geo-redirect가 없다"고 확정하진 못한다

**원인 A — 우리가 봇 점수를 올리고 있었다**
`page-bridge.js`가 MAIN world·`document_start`에서 `window.fetch`·`XMLHttpRequest.prototype`을 교체한다. 관찰 전용이라 동작엔 영향이 없지만 `String(window.fetch)`가 `[native code]`가 아니게 되어 봇 탐지에 걸린다.
- `mask()` 신설 — `name`·`length`·`toString`을 원본으로 위임하고 **`toString` 자신도 네이티브로 보이게** 한 겹 더 덮는다
- 하네스 실측: 후킹 전후 문자열 **완전 동일**(`function fetch() { [native code] }`), fetch·XHR 캡처는 정상 유지, 무관한 요청은 미캡처

**원인 B — 실패하면 복구가 불가능했다**
`cross-site-receiver.js`가 **fetch 직전에** 페이로드를 지웠다. 챌린지에 걸리면 검색 조건이 이미 사라진 뒤라 확인 절차를 마치고 새로고침해도 영영 복구가 안 됐다.
- **성공했을 때만 소비**하도록 바꾸고, 챌린지 루프로 새로고침이 반복될 때 POST가 무한히 나가지 않게 **시도 3회 상한**(`tries`)을 뒀다
- 실패 시 **화면에 보이는 안내 배너**(`notify`) — 종전엔 `console.warn`뿐이라 사용자는 영문을 몰랐고, 드물게 터지는 이 실패가 아무 흔적도 안 남겨 **원인 판정이 영영 불가능했다.** 문구에 HTTP 상태·현재 호스트·시도 횟수를 담아 **다음 발생이 곧 제보 자료**가 되게 했다
- ⚠ **`notify`를 export 하지 않는다** — 모듈이 되면 crxjs가 동적 import 로더로 바꾸는데, 챌린지 페이지의 엄격한 CSP(`script-src 'nonce-…'`)에 그 import가 막힐 수 있다. **정작 안내가 필요한 화면에서 스크립트가 안 뜨면 의미가 없다.** 빌드 산출물에서 loader가 아닌 직접 주입임을 확인했다

**manifest**: `cross-site-receiver`에 `https://www.pathofexile.com/trade2/*` 추가 — 종전엔 `/trade/*`만 매치돼 **poe2 전환은 지역과 무관하게 구조적으로 불가능**했다

**정정한 문서**: `CLAUDE.md`(프로젝트) · `docs/chrome-webstore-배포.md` · `docs/영문-pob-복사-선행조사.md`. 참조하던 메모리 `project_poe_bookmark_atlas_georedirect`는 **실재하지 않는다**

**전환 기능은 여전히 UI에서 숨김 유지** — 근거가 "한 번 안 일어났다"뿐이라 약하다. 위 안내 배너로 사례를 몇 건 모은 뒤 복원 여부를 판단한다.

검증: 테스트 **344/344**, 빌드 통과, 하네스 실측(위장·캡처·배너 렌더 560px 상한/중앙정렬/화면 안), 빌드 산출물에 핵심 문자열 포함 확인.
**미검증**: 실제 pathofexile.com에서의 동작 — 재현율이 낮아 사용자 확인 필요. 좁은 폭(420px) 배너는 판 미표시로 뷰포트 제어가 안 먹어 상한 동작만 확인했다.

### PoB 조립기 — 조용한 실패 가시화 + 미독해 필드 3종 (2026-08-05, **미커밋**)

군단 주얼 수정 뒤 "같은 부류가 더 있나" 조사에서 나온 3건을 처리했다.

**① 값 초과 경고 — 실패 처리하지 않는다(측정 근거)**
- `fillValues`는 값이 템플릿 `#`보다 많으면 초과분을 조용히 버린다 → 틀린 수치가 PoB로 간다
- ⚠ **그런데 실측하니 진짜 유실은 사실상 0건이었다.** 번들 맵의 ko/en 쌍(poe1 465·poe2 160)에서 초과 122건 중 **120건이 `#`=0인 고정 문구**("추가 **1**명" → "an additional Rogue Exile" — 한국어 어법상의 1이라 EN이 온전). 나머지 2건도 "5개 또는 6개"가 EN에 리터럴로 들어간 정상 케이스
- 따라서 **`#`가 1개 이상일 때만** 보고, **실패가 아니라 경고**로 남긴다. 실패로 만들었으면 정상 번역 120건이 한글로 떨어졌다. **이건 고장을 고친 게 아니라 앞으로를 위한 계기판이다**
- ⚠ 이 측정은 `ko`를 가진 다중변형 항목만 덮는다(전체 17,684 중 465). 단일 템플릿 항목의 실제 위험은 측정 불가 — 경고가 그걸 드러내라고 있는 것

**② 변형 추정 경고** — `pickTemplate`이 다중변형에서 ko 매칭 실패 시 조용히 `e[0]`을 썼다(다중변형 poe1 233·poe2 80건, 1.0~1.3%). `(Local)`/글로벌을 잘못 고르면 PoB 계산이 통째로 달라진다. `pickTemplateInfo(id, ko, map)` 신설 → `{en, guessed}`. **동작은 유지하고 경고만 추가**(기존 `pickTemplate`은 얇은 래퍼로 남겨 호출부·테스트 불변)

**`warnings[]`를 `missing[]`과 분리한 이유**: warnings까지 "미변환 N" 배지에 세면 정상 복사가 실패처럼 보인다. `buildPobText`가 `{text, missing, warnings}` 반환, 배지는 `missing`만, 제보 텍스트(`buildReportText` 4번째 인자)엔 '의심 항목' 절로 따로 담는다. **warnings만 있어도 제보는 생성된다**(그게 드러내려는 대상이므로)

**③ PoB가 읽는데 안 만들던 필드 3종** — Radius는 "`properties`를 안 읽는다"의 한 사례였을 뿐이었다
- **품질** `Quality: +20%` — PoB 로컬 mod 계산에 직접 영향
- **소켓·링크** `Sockets: R-G-B W` — 같은 `group`은 `-`, 다른 group은 공백
- **영향력** `Shaper Item`/`Elder Item`/… 6종
- 섹션 순서를 인게임 Ctrl+C에 맞췄다: 헤더 → 속성(품질·반경) → 소켓 → Item Level → mod → 영향력 → Corrupted
- 미착수(저우선): `requirements`·`identified`·`synthesised`·`duplicated`·`scourgeMods`·`veiledMods`·`crucibleMods`

**결백 확인**: `hashAt`의 위치 기반 매칭을 의심했으나 캡처 실데이터 대조 결과 `hashes[i] ↔ mods[i]` 대응이 맞다(`[hash, [n]]`의 `[n]`은 mod 인덱스가 아니다). 버그 아님

검증: 테스트 **344/344**(신규 8건), 빌드 통과. **PoB 실제 붙여넣기 사용자 확인 완료(2026-08-05)** — 군단 주얼·품질·소켓·영향력 정상 동작

### PoB 영문 복사 — 군단 주얼(무궁한 주얼) 정복자 줄 유실 + 반경 미출력 (2026-08-05, **미커밋**)

**제보 증상**: 고상한 오만(Elegant Hubris)을 PoB에 붙여넣으면 `Radius: Large`와 `Passives in radius are Conquered by the Eternal Empire`가 없다. 사용자가 **PoB가 인식한 데이터**를 정상본과 나란히 제시.

**원인 A — 두 줄짜리 mod의 뒷줄이 조용히 사라진다 (일반적 결함)**
- 거래소는 군단 주얼 mod를 **두 줄 한 덩어리**로 준다: `카스피로를 기념하기 위해 제작한 주화 20820개\n반경 내 패시브 스킬은 영원한 제국의 지배를 받음`
- 그런데 검색 stat은 `explicit.pseudo_timeless_jewel_caspiro` → `"Commissioned # coins to commemorate Caspiro"`로 **첫 줄만** 덮는다(`pseudo_` = 검색 전용 가상 스탯이라 아이템 실제 문구와 1:1이 아니다)
- `translateMod`이 `#`가 다 채워졌다는 이유로 성공 처리하고 즉시 반환 → **둘째 줄은 존재 자체가 사라졌다.** `missing[]`에도 안 남아 **Shift+클릭 제보로도 영영 안 드러나는** 조용한 유실이었다
- **폴백 사전으로는 못 고친다**: `pobUniqueModMap.poe1.json`의 군단 주얼 5종은 주얼당 정복자 1명뿐(고상한 오만은 Cadiro) — poe-i18n 소스가 "유니크 1개 = mod 1쌍"이라 Caspiro·Chitus·Victario 변형이 없다. 게다가 stat id 경로가 **성공해버려서 폴백을 보지도 않았다**
- 수정: `withTrailingLines()` 신설 — 템플릿이 덮은 줄 수보다 KR 줄이 많으면 **남은 줄을 각각 폴백 사전으로 재번역**한다. 실패한 줄은 **KR을 남기고**(줄을 버리지 않는다) `missingLines`로 반환 → `modLines`가 줄 단위로 `missing[]`에 보고. `translateMod` 반환에 `missingLines` 추가(선택 필드 — 단일 줄 mod는 종전과 완전히 동일)
- ⚠ **가정**: 템플릿이 mod의 **앞줄부터** 덮는다. 검색 stat이 mod 선두 문구에서 만들어지므로 성립하지만, 어긋나는 사례가 나오면 여기부터 의심할 것

**원인 B — `properties`를 아예 안 읽었다**
- `buildPobText`에 `properties`·`Radius`가 **한 번도 등장하지 않았다**(`src/`·`test/` 전수 확인). 주얼 반경은 PoB가 '반경 내' 효과 계산에 실제로 쓴다
- `radiusLine()` 신설 — `properties`에서 반경을 뽑아 `대형/중형/소형 → Large/Medium/Small` 변환. 인게임 Ctrl+C 순서대로 **헤더 → 반경 → Item Level → mod** 섹션에 배치. 모르는 표기는 그대로 내보내되 `missing`에 `radius:<원문>`으로 남긴다

**정복자 문구 5줄을 overrides에 추가** (`scripts/pob-mod-overrides.poe1.json`, 종전 `{}`)
- 정복자는 20명(5주얼×4)이지만 **진영은 5개**라 5줄이면 끝난다. 값은 기존 사전 항목에서 **프로그램으로 추출**해 추측이 없다(바알·카루이·마라케스·템플러·영원한 제국)
- ⚠ **`src/lib/pobUniqueModMap.poe1.json`에도 같은 5키를 직접 병합했다(464 → 469키).** 빌드 스크립트 재실행은 poe-i18n 소스 + stats 원본이 필요하고 **464키가 통째로 흔들릴 위험**이 있어 피했다. overrides 파일에 기록해 뒀으므로 다음 정식 재생성 때 그대로 보존된다(overrides는 항상 승). 충돌 0건 확인
- poe2는 군단 주얼이 없어 대상 아님

검증: 테스트 **336/336**(신규 5건 — 전체 조립·진영 5종·미지 뒷줄 보고·미지 반경 보고·반경 없는 아이템 회귀), 빌드 통과. 제보 아이템 재현 결과 `Radius: Large` + 정복자 줄 포함, `missing: []`. **PoB 실제 붙여넣기 사용자 확인 완료(2026-08-05).**

### 사용자 피드백 라운드 — 스크롤바 자리 확보 + 퍼지 검색 on/off (2026-08-05, **미커밋**)

**⚠ 아직 커밋·push 안 됐다.** 변경 파일: `panel.css`·`fuzzyPrefix.js`·`panel.js`·`test/setup.js`·`test/fuzzyPrefix.dom.test.js`.

**제보 5건이 두 제품에 걸쳐 들어왔다** — 이 repo 소관 2건은 아래(③·⑤)에서 처리했고, 나머지 3건은 seominugi-com 소관이었다.

> **✅ 2026-08-16 종결 — 아래 3건은 더 이상 미착수가 아니다.** 이 절이 오래 "미착수"로 남아 다음 세션이 반복해서 파헤쳤다. 확인 결과:
> - ① 스태시 아틀라스 "UI가 산만하다" → **종결.** 제보자에게 구체화를 되물었으나 **답변이 오지 않았다**(사용자 확인). 착수 근거가 없으므로 더 추적하지 않는다.
> - ② 스태시 아틀라스 "잡유니크도 죄다 10카" → **해결됨**(사용자 확인). 당시 원인은 프론트가 아니라 시세 백엔드(smng-poe-pricer)로 지목돼 있었다.
> - ④ 아이템 시세 웹 `[상위 10개 즐겨찾기]` 툴팁 → **코드상 해결됨.** seominugi-com develop `src/components/poe-economy-v2/PriceTableDesktop.vue:70-71` 이 `:items="top10TooltipContent.items"` 를 넘기고 있다(당시 지목된 결함은 `summary`·`note`만 넘긴다는 것이었다). **남은 것은 라이브 육안 확인 1회** — 특히 즐겨찾기가 꽉 차 문구가 "전체 삭제"로 바뀐 상태의 툴팁.

**③ 스크롤바 자리 미확보로 아이콘 배치가 흔들리던 문제** — 제보 내용이 원인까지 정확했다.
- `.ba-list`(`panel.css:276`)·`.ba-sets`(칩 줄)에 `scrollbar-gutter: stable`. 하네스 실측: **수정 전 384→374px(10px 흔들림) → 수정 후 374px 고정(0px)**
- `.ba-movelist`(폴더 이동 모달)에도 같은 구조가 있지만 좌측 정렬 텍스트 행이라 체감이 없어 손대지 않았다

**⑤ 거래소 필터칸 `~` 강제 삽입을 설정에서 끌 수 있게** — 제보자가 '-'로 적었지만 실제 문자는 `~`다(`PREFIX`).
- `uiFuzzyPrefix` 키, **기본 켬**(기존 동작 보존). 설정(⚙) 모달에 "필터 퍼지 검색 (~)" 켬/끔 세그먼트 — 기존 `.ba-seg`/`.ba-set-opt` 재사용이라 **신규 CSS 0**
- **`initFuzzyPrefix()`를 async로 만들지 않았다** — 설정을 `await`하면 페이지 로드 직후 1.2초 자동포커스 가드 창을 저장소 응답 대기에 갉아먹혀 회귀가 난다. 리스너는 즉시 붙이고 **동작만 모듈 레벨 `enabled` 플래그로 가른다** + `storage.onChanged` 구독으로 새로고침 없이 반영
- **끄더라도 '페이지 로드 시 자동 포커스 해제'는 유지한다** — 제보와 무관한 별개 편의 기능이라 한 토글에 묶으면 `~`만 끄려던 사용자가 그것까지 잃는다
- ⚠ **두 세그먼트가 `.ba-set-opt`를 공유**한다 → 기존 핸들러의 `querySelectorAll('.ba-set-opt')`를 `[data-side]`/`[data-fz]`로 갈랐다. 안 갈랐으면 퍼지 클릭이 `applySide(undefined)`를 불렀다(하네스에서 패널 위치가 안 흔들리는 것으로 확인)

**곁들여 고친 것**: `.ba-seg > *`에 누름 피드백이 없었다 — 패널의 다른 누름 요소(`.ba-set-go`·`.ba-set-del`·`.ba-toast-act`)는 전부 `:active { scale }`+reduced-motion 가드를 갖는데 세그먼트 3곳(정렬 토글·패널 위치·신규 퍼지)만 빠져 있었다. 같은 값(.16s·`--ba-ease-out`·0.97)으로 맞췄다. `review-animations` **Approve**.

**테스트 목 보강**: `test/setup.js`에 `chrome.storage.onChanged` 추가 + `__fireStorageChange(changes, area)` 수동 발화기. **`set()`이 자동 발화하지는 않는다** — 그러면 다른 테스트의 리스너가 예기치 않게 깨어난다.

검증: 테스트 **331/331**(신규 5건 — 꺼짐 시 삽입·보강·보호 미동작, 능력치칸 동반, 재켜기 즉시 반영), 빌드 통과, 하네스에서 설정 행 렌더·토글·저장값(`false`/`true`)·패널 위치 무간섭·콘솔 에러 0 확인. **거래소 실사이트에서 `~` 실동작은 미확인**(하네스엔 거래소 입력칸이 없다) — 사용자 확인 필요.

### PoB 영문 복사 — 유니크 전용 문구 KR 원문 폴백 (2026-08-02)
- **제보 증상**: poe2 유니크 `하늘의 편린`(Skysliver)에서 `물리 피해 없음` 한 줄만 한글로 남았다
- **원인 — 앞 건과 다르다**: 맵이 낡아서가 아니라 **거래소 stat id가 아이템 실제 문구와 1:1이 아니다.** 라이브 API로 확인한 이 mod의 hash는 `stat.explicit.stat_1509134228`인데 그 id의 필터 문구는 `물리 피해 #% 증가`(`#% increased Physical Damage`)다 — GGG가 유니크 전용 문구에 별도 검색 항목을 안 주고 성격이 비슷한 stat에 얹어뒀다. 채울 값이 없어 `#`가 남고 → 실패 처리 → KR 폴백. **폴백 자체는 의도대로 동작**했고 나머지 4줄이 정상인 것도 같은 이유
- **stats API만으로는 이 부류를 못 고친다** — 유니크 전용 문구는 거래소 사전에 아예 없다. 그래서 `poe-i18n` 생성기 데이터(이미 base·unique 이름 맵의 소스)의 유니크별 EN/KR mod 쌍을 2차 사전으로 쓴다
- **`scripts/build-pob-modmap.mjs` 신규** → `src/lib/pobUniqueModMap.json`(poe2 256키) / `.poe1.json`(poe1 464키). 키는 **KR 원문 정규화**(태그 제거 → 값·범위 `(80 — 120)`를 `#` → 공백 제거), 값은 EN `#`-템플릿
- ⚠ **소스의 EN/KR 배열 위치 정렬이 어긋난 항목이 있다** — 거래소 stats와 교차검증 시 poe1 97.4% / poe2 95.3% 일치. 실증: `the-sentry`는 KR "물리 피해 없음"에 EN "Adds (25 — 32) to (40 — 50) Fire Damage"가 붙어 있다. **틀린 영문은 한글로 남는 것보다 나쁘다**(PoB가 그대로 읽어 빌드 수치가 바뀐다) → 3중 필터
  - ① **아이템 단위 신뢰 판정** — 거래소 기준표와 어긋나는 줄이 하나라도 있으면 그 아이템 전체 폐기(정렬이 어긋나면 배열 전체가 밀리므로 흔한 mod 한 줄만 걸려도 통째로 잡힌다). poe1 90/1140 · poe2 38/443 폐기 → `the-sentry` 제거, `skysliver` 생존
  - ② 같은 KR 키에 서로 다른 EN → 양쪽 폐기(잔여 충돌 poe1 0 · poe2 1)
  - ③ KR 값 개수 ≠ EN `#` 개수 → 폐기 / ④ 거래소가 이미 해결하는 키는 제외(폴백에 둘 이유가 없다)
- **`scripts/pob-mod-overrides.json`(+`.poe1.json`) 수동 오버라이드** — 빌드 시 병합, **항상 승**. 교차검증이 불가능한 잔여 오류를 제보로 정정하는 자리. 현재 둘 다 `{}`(자동 사전이 제보 건을 이미 커버)
- **제보 텍스트에 KR 원문 추가** — `missing` 항목이 `explicit:explicit.stat_1509134228 — 카오스 피해 없음` 형태가 됐다. 이 부류는 **id만으론 무엇을 고칠지 알 수 없고**(id가 엉뚱한 stat을 가리킨다) 폴백 사전 키가 KR 원문 기준이라, 제보만 보고 오버라이드를 쓸 수 있어야 한다
- `translateMod(id, ko, map, modMap)` / `buildPobText(..., uniqueMap, modMap)` 4·5번째 인자 추가(둘 다 기본값 `{}` — 기존 호출부 동작 불변). `normKo` export
- 검증: 테스트 **326/326**(신규 8건), **거래소 API 실캡처 아이템**으로 전 줄 영문 + `missing: []` 확인, 제보 텍스트 → `normKo` → 오버라이드 키 왕복 확인, `npm run build` 성공(dist 3.0MB, 폴백 맵 청크 poe2 15KB · poe1 42KB — PoB 클릭 시 lazy)
- **남은 위험**: 한 아이템의 모든 줄이 거래소에 없으면서 동시에 정렬이 어긋나면 못 잡는다. 그 경우 오버라이드로 정정한다(사용자 승인된 트레이드오프 — 커버리지보다 정정 가능성 우선)

### PoB 영문 복사 — 옵션형 stat id(`id|N`) 미매핑 수정 (2026-08-02, `1e9bdba`)
- **제보 증상**: 소형 클러스터 주얼을 PoB 복사하면 `Adds 2 Passive Skills (enchant)`는 영문인데 바로 다음 줄이 `추가된 소형 패시브 스킬 효과: 방어도 15% 증가 (enchant)`로 한글로 남았다
- **원인**: 거래소 stats API는 **선택지가 있는 mod를 옵션 번호까지 붙인 별개 id**로 준다 — `enchant.stat_3948993189|42` = "Added Small Passive Skills grant: 15% increased Armour". 아이템 `extended.hashes`도 같은 형태다. 그런데 번들 맵에는 옵션 번호 없는 통합 id(`enchant.stat_3948993189` → `"...grant: #"`)만 있어 조회가 실패 → KR 폴백
  - 통합 템플릿의 `#`는 **숫자 자리가 아니라 옵션 텍스트 자리**라, 설령 조회에 성공했어도 `extractValues`가 KR 문장의 "15"를 집어 `grant: 15`라는 **조용히 틀린 결과**가 나온다. 옵션형 id 도입은 그 구조적 결함까지 없앤다
- **맵 재생성**(EN/KR stats 최신본으로 `scripts/build-pob-statmap.mjs` 재실행): poe1 14,969 → **17,684 ids**, poe2 8,086 → **8,127 ids**. 최신 stats API 대비 미매핑 poe1 2,733(그중 옵션형 1,603 · `mercenary` 그룹 534 통째로 누락) / poe2 41 → **양쪽 0건**
  - EN stats(`pathofexile.com/api/trade|trade2/data/stats`)는 **KR IP에서도 200으로 받힌다** — 스크립트 주석의 "VPN 필요"는 낡은 정보(거래소 *사이트*만 geo-redirect). 이번엔 VPN 없이 4종 다 받았다
- **`build-pob-statmap.mjs`에 레거시 병합 추가** — stats API는 "지금 검색 가능한" mod만 준다. 그대로 덮으면 삭제된 리그 mod(예: `While a Unique Enemy is in your Presence, …` 계열) 281건을 잃는데, 그런 아이템은 여전히 거래된다. 새 데이터에 없는 기존 항목은 유지하되 **옵션형으로 쪼개진 통합 id는 버린다**(위의 조용히 틀린 결과 방지). 결과적으로 유실 0
- 검증: 테스트 **318/318**(신규 3건 — 옵션형 id 변환·특성 할당 인챈트·통합 id 부재), 번들 맵으로 제보 아이템을 `buildPobText`에 통과시켜 `Added Small Passive Skills grant: 15% increased Armour (enchant)` + `missing: []` 확인, `npm run build` 성공(dist 2.9MB, poe1 맵 청크 1.50MB — PoB 버튼 클릭 시 lazy 로드라 초기 로드 무영향)
- ⚠ **기존 테스트 하나가 무의미해질 뻔했다**: `translateMod('enchant.stat_2954116742', '할당 골렘의 피')`가 `null`인지 보던 테스트는 재생성 후에도 통과하지만 이유가 "# 미치환"이 아니라 "id 자체가 사라짐"으로 바뀌었다 → 합성 맵으로 동작을 고정하고, 실데이터 케이스는 옵션형 블록으로 옮겼다
- **베이스 맵은 손대지 않았다** — `pobBaseMap*.json`은 별도 repo(poe-i18n 생성기) 입력이고 제보 아이템의 베이스는 정상 변환됐다

### 0.6.1 준비 — 조건 묶음 드래그 사용성 정비 + 칩 줄 접기 (2026-08-01, `d432957`)
- **0.6.0을 실사용한 사용자 제보**: 칩 드래그로 자리를 옮기기 어렵다. 원인이 셋이었다
  - ① 삽입이 항상 "대상 칩 앞" 고정 → 오른쪽으로 옮기려면 "옮길 자리의 **다음** 칩"을 노려야 해 결과가 어긋남 → **포인터가 칩의 어느 절반인지로 앞/뒤 결정**(선이 그어진 자리가 곧 놓이는 자리)
  - ② 조건 묶음 툴팁이 커서 놓을 자리를 가리고, 드래그 중엔 `mouseout`이 안 와 **굳어버림** → `dragstart`~`dragend` 동안 툴팁 억제(`tipSuppressed`)
  - ③ 좁은 칩 사이에서 삽입선만으로는 기준 칩이 안 읽힘 → **놓을 자리 프리뷰**(칩 줄 위 fixed 바, "〈옮기는 칩〉 → 〈어디로〉"). 이름은 textContent로만
- **드롭 강조를 라임(`--ba-drop: #d9f99d`)으로** — 패널이 보라 일색이라 보라 강조가 배경에 묻힌다는 제보. 자수정 바이올렛의 보색이면서 앰버(별)·골드(화폐)·로즈(위험)·시안(공유)과 의미가 겹치지 않는 유일한 빈자리. 삽입선도 2→3px
- **칩 줄 접기**(`uiSetsCollapsed`) — 묶음이 늘면 상단 최대 3줄을 점유해 목록을 잠식. 접어도 **라벨과 개수 배지는 남긴다**(사라지면 "클릭 1회로 조건 얹기"가 잊힌다). 폴더 접기와 같은 언어(회전 셰브론+배지). 투어 중에는 자동으로 펼쳤다 끝나면 복원
- ⚠ **하네스가 잡은 버그**: `wasCollapsed`가 이미 *패널* 접힘 상태로 쓰여 이름 충돌 → 패널 전체가 안 뜨는 SyntaxError. **테스트도 빌드도 못 잡았고** 하네스 로드에서만 드러났다 → `setsWasCollapsed`로 분리
- 검증: 테스트 315/315, 하네스에서 방향 판정·툴팁 억제·프리뷰 5개 상태·접기(71px→32px)·키보드 토글·색상 최종값 확인. **재로드 후 접힘 복원은 하네스가 매 로드 목 스토리지를 새로 시드해 확인 못 함 — 실제 확장에서 확인 필요**

### 기본 화폐 가격에도 신성한 오브 환산 칩 (2026-08-01, `667f646`)
- **발단(사용자 제보)**: 한 목록에 "350 카오스"와 "2 신성한"이 섞이면, 신성한 쪽엔 카오스 환산 칩이 붙지만 카오스 쪽엔 붙을 게 없어(이미 기본 화폐) 큰 카오스 숫자와 작은 신성한 숫자를 머릿속으로 나눠야 했다
- `currencyRates.js`: `divineFromPrice(price, rateData, game)` 신규 — `baseFromPrice`의 **반대 방향**(기본 화폐 → 신성한 오브). poe1 `chaos_per_divine`, poe2 `exalted_per_divine` 사용
- `content-main.js`: 칩에 `data-kind`(`base`|`divine`) 도입, `chipKindOf()`로 아이콘·라벨·폴백 텍스트 분기. static API에서 `divine` 아이콘도 함께 로드(`divIcon`)
- **임계값 `MIN_DIVINE_CHIP = 0.5`** — 20카오스(=0.06신성한)처럼 잔 가격에 "≈ 0.1 신성한"을 붙이면 정보가 아니라 소음이다. 비교가 필요한 큰 가격에서만 붙인다
- **"섞였을 때만" 조건은 일부러 넣지 않았다** — 무한 스크롤 부분 로드에서 섞임 판정이 흔들려 칩이 붙었다 사라지고, 섞이지 않아도 큰 카오스 가격의 신성한 환산은 그 자체로 유용하다. 원하면 조건 추가 가능
- 검증: 테스트 315/315(신규 4건), 거래소 static API 실측으로 `divine` id·한글명·이미지 존재 확인(poe1 `trade`·poe2 `trade2` 양쪽, Currency 103종). 라이브 칩 렌더는 거래소 페이지에서만 확인 가능 — **사용자 확인 필요**

### 조건 묶음 칩 — 드래그 재배치 + 상시 삭제 버튼 (2026-08-01, `d0db669` develop push 완료)
- **편집 모드(연필 토글 + 앞/뒤 화살표) 제거**. 드래그와 상시 `×`가 같은 일을 하게 되어 경로가 둘로 갈렸다
- 칩 자체가 드래그 핸들(칩이 작아 별도 그립을 넣으면 이름이 더 잘린다). 클릭=조건 얹기와는 dragstart/click으로 갈려 충돌 없음
- 드롭: 다른 칩 위 → 그 칩 **앞**, 칩 줄 빈 공간 → **맨 뒤**. 삽입 위치는 세로 보라선(왼쪽/오른쪽)으로 표시
- `store.js`: `moveConditionSetBefore(id, beforeId)` 신규(order 중간값 삽입 — 북마크 재정렬과 같은 방식), `removeConditionSet`이 **삭제 레코드 반환**, `restoreConditionSet` 신규. 인접 스왑 `moveConditionSet`은 키보드용으로 유지
- **다른 game 스코프 칩 위 드롭은 무시** — poe1/poe2는 스탯 id 체계가 달라 섞이면 검색이 깨진다
- 삭제는 확인 없이 즉시 + **실행취소 토스트**(6초). 원 설계가 "오클릭 방지"로 삭제를 숨겨뒀던 것을 상시 노출로 바꾸는 대신 둔 안전장치 — 확인 다이얼로그는 매번 마찰이지만 실행취소는 실수했을 때만 비용이 든다
- `toast(msg, action)` 확장 — 액션 버튼은 DOM API로 생성(묶음 이름이 들어가므로 innerHTML 금지)
- 키보드 대안 **Alt+←/→**(목록·폴더의 Alt+↑/↓와 같은 언어), 이동 후 포커스 복귀. `.ba-set-go`에 role=button·tabindex=0·focus-visible
- 모션: `--ba-ease-out`(0.23,1,0.32,1) 토큰 신설, 누름 스케일 0.97/0.95·160ms, hover는 `(hover: hover)` 게이팅, reduced-motion에서 transform 제거. 드래그 표시는 **의도적으로 transition 없음**(포인터보다 늦게 따라오면 안 됨). `review-animations` **Approve**
- 검증: 테스트 311/311(신규 5건), 하네스 라이브 — 드래그(원거리·맨뒤)·Alt+←/→·삭제→실행취소(순서 보존)·칩 클릭 무충돌·420px 2줄 wrap·콘솔 에러 없음
- 남은 관찰: `panel.css:150`에 기존 `transition: all .15s`(세그먼트 컨트롤) — 이번 범위 밖

### 하드코어·무자비 변형 리그명 한글화 (2026-07-31, `39931d8` develop push 완료)
- 거래소 `/data/leagues`는 영구 리그·챌린지 **본명만** 한글로 주고 변형은 영문 그대로 준다("Hardcore Allflame"·"HC Ruthless Allflame"·"Hardcore Ruthless")
- `leagueMap.js`에 `leagueDisplayName(id, text, map)` 추가 — id 앞의 변형 토큰(HC Ruthless / Hardcore Ruthless / Hardcore / HC / Ruthless)만 한글화하고 베이스는 **API가 준 한글명**으로 채운다(베이스는 직접 번역 안 함 → 신규 리그도 자동 대응)
- **표시 전용**이다: `leagueInfo`의 `isLive`/`isDead`·`inMap`과 content-main의 역변환은 거래소 원본 표기 그대로 — 여기 값을 바꾸면 그 표기로 저장된 기존 레코드가 "끝난 리그"로 오판된다(테스트로 고정)
- 적용처: 카드/섹션 리그 라벨(`leagueInfo.name`) + 설정 '내 리그' 드롭다운(`panel.js`, option value는 id 유지)
- 검증: 테스트 306/306(신규 8건), 하네스 라이브 확인 — 드롭다운 8개 리그 전부 한글(`하드코어 무자비 올플레임` 등), value는 id 그대로, 콘솔 에러 없음

### 북마크 검색 단축키 = 기존 Alt+K (2026-07-31, 미커밋)
- 사용자 요청("북마크 검색 단축키가 있으면") → **이미 `panel.js`에 Alt+K로 구현돼 있었다**(⌨ 칩 팝오버에도 표기). 사용자 확인 결과 "그게 그거였다" → 신규 구현 없이 **발견성만** 개선
- 검색창 placeholder `북마크·히스토리 검색 (이름·조건)` → `(Alt+K)`, 원래 힌트는 `data-tip`으로 이동
- 논의된 미채택 안: ↑/↓+Enter로 결과까지 키보드 선택(실익 크나 포커스 링·스크롤 추적 구현 필요), Alt+1~9 북마크 즉시 실행(암기 부담·거래소 Alt 단축키 충돌 검토 필요) — **추후 후보**

### 변형(discriminator) 아이템 북마크명이 "[object Object]"가 되던 버그 수정 (2026-07-31, `51ad667` develop push 완료)
- **원인**: 거래소는 변형이 있는 아이템(해안 교두보 등)의 `query.name`·`query.type`을 문자열이 아니라 `{option, discriminator}` **객체**로 보낸다. `parseSearchQuery`가 그대로 담아 → 저장 다이얼로그 기본 이름·목록 표시가 `[object Object]`
- `searchParser.js`에 `optionText(v)` 추가(문자열/`{option}`만 표시명으로, 알 수 없는 객체는 `null` → 기존 폴백 유지) → `parseSearchQuery`의 name·itemType에 적용
- `searchIdentity`는 `identityText`로 **discriminator까지 키에 포함** — 종전엔 변형이 다른 검색이 모두 `[object Object]`로 뭉개져 히스토리 중복 제거가 오판했다
- `suggestName`·`renderList.rowHtml`에도 `optionText` 적용 — 이미 저장된(정규화 이전) 레코드의 표시도 즉시 복구. 단 사용자가 그 이름으로 확정 저장한 북마크는 문자열 `"[object Object]"`라 코드로 못 고침(이름변경 필요)
- 검증: 테스트 **298/298** 통과(신규 4건: 파서 2·suggestName 1·렌더 1), 빌드 통과

### 0.5.0 릴리즈 게시 (2026-07-28, `13dd975` / 태그 `v0.5.0`)
- `manifest.json`·`package.json` 0.4.0 → **0.5.0** 범프 (`b491848`)
- `deploy/poe-bookmark-atlas-0.5.0.zip` 생성 (dist를 루트로 담는 구조, 29파일·726KB). deploy/는 gitignore라 커밋 안 됨 — **로컬에만 존재**
- `develop` → `main` fast-forward 머지·푸시 (`00f25f2..13dd975`, 16커밋 / 18파일). push 전 비밀값 스캔 통과
- `gh release create v0.5.0 --draft --target 13dd975...` → 사용자 요청으로 **즉시 publish**(`--draft=false --latest`) → 태그 생성, `v0.4.0`은 Latest에서 내려감
- 릴리즈 노트는 사용자 관점 5개 절(조건 묶음 / 조건 칩 / 일괄 이동 / 환산 칩 / 가이드·안내 / 버그 수정)로 작성. 원본: 스크래치패드(세션 종료 시 소실 — 재작성 필요하면 릴리즈 페이지에서 복사)
- 검증: 테스트 **294/294**, 빌드 통과, 라이브 확인 완료

### 가이드 투어에 0.5.0 기능 반영 + 토스트 재배치 (2026-07-28, `ee624f6`·`13dd975`)
- **발단**: 릴리즈 직전 사용자 질문 "가이드투어 기능도 추가했을까?" → 확인 결과 **신규 기능 3종이 투어에 하나도 없었다**. 릴리즈를 멈추고 반영
- 투어 **15 → 17스텝**: ②`#ba-sets` 조건 묶음, ⑦`.ba-cond--add` 조건 칩 추가. `⋯` 스텝 본문에 "여러 개 체크해 한 번에 이동" + "조건 묶음으로 등록" 보강
- **데모 판정을 분리**(`needsConditionSetDemo`/`seedDemoSets`/`clearDemoSets`) — `#ba-sets`는 묶음 0개면 `hidden`이라 가리킬 대상이 없다. 북마크 데모(`needsTourDemo`)와 조건을 합치면 목록이 멀쩡한 사용자에게 예시 카드가 끼어들어 안 됨
- 투어 스텝에 **`sel2` 대체 선택자** 추가 — `.ba-cond--add`는 원본 `query`가 있는 카드에만 붙어서, 옛 북마크만 가진 사용자는 1순위 대상이 없다
- 데모 북마크에 `query` 추가 — 없으면 데모 카드에서 조건 칩이 클릭 가능 형태로 살아나지 않음
- **예시 안전장치**: `__demo` 묶음·카드의 칩을 눌러도 실제 검색을 만들지 않고 안내 토스트만 (거래소가 모르는 스탯 id → 400)
- **토스트 재배치**: `.ba-root` 밖 형제로 옮기고 `position: fixed` + `top: 42%` + `translate(-50%,-50%)`, `white-space: normal`·`word-break: keep-all`, `max-width: min(560px, 100vw-48px)`
  - 원인: 패널 폭 384px + `nowrap` → 긴 안내 문구(조건 묶음 얹기 등)의 좌우가 잘림(사용자 제보). 패널 안에 두면 접힘 시 `.ba-root` transform이 fixed 좌표계를 가로채는 문제도 있음
  - 위치는 상단 고정(22px) → 사용자 요청으로 **정중앙보다 살짝 위(42%)**. 진입·퇴장 키프레임도 새 중심 기준으로 수정
- 신규 테스트 7건(데모 묶음 판정·시딩·정리·게임 스코프·중복 방지·데모 북마크 query)

### 북마크 일괄 폴더 이동 — 모달 방식으로 재설계 (2026-07-27)

**1차 설계(액션 행 `선택` 토글 + 행 체크박스)는 폐기했다.** 사용자 지적 2건:
1. **UI 깨짐** — 액션 행에 버튼이 하나 더 늘자 384px 폭을 넘겨 "선택" 글자가 세로로 쪼개졌다. 직접 원인은 `.ba-select-toggle`을 **공용 버튼 스타일 선택자 목록에 안 넣어** 브라우저 기본 버튼으로 렌더된 것(폰트·nowrap·패딩 없음). 액션 행에 `flex-wrap`도 없었다
2. **라벨 모호** — "선택"만으로는 무엇을 위한 선택인지 안 드러난다

**최종: 기존 카드 액션 `⋯ → 다른 폴더로 이동`이 여러 개 선택 모달을 연다.** 진입점이 이미 이름으로 목적을 말하고 있어 라벨 문제가 사라지고, 액션 행에 버튼을 늘리지 않아 폭 문제도 사라진다.
- `showFolderPick(current, title, multi)` 확장 — `multi`가 오면 **옮길 북마크 목록**(체크박스 + 현재 폴더 표시 + 전체/해제 + N개 선택)을 폴더 칩 위에 함께 띄운다. 누른 북마크는 **미리 체크**되어 하나만 옮길 때도 흐름이 같다
- 목록 재렌더는 목록 영역만 — 폴더 칩 선택 상태가 흔들리지 않는다. 0개 선택이면 `이동` 버튼 비활성
- 이동은 `moveBookmarks`(한 번의 저장소 쓰기, 상대 순서 유지) 그대로 사용
- 1차 설계의 `selectMode`·행 체크박스·액션 행 토글·관련 CSS는 **전량 제거**(잔재 0건 확인). `bulkMove.dom.test.js`(선택 모드 DOM 테스트 11건)도 대상이 사라져 삭제 — **모달은 panel.js 소속이라 자동 테스트 밖**이고 store 계층(`moveBookmarks` 5건)만 남는다

### (폐기된 1차 설계 기록) 북마크 일괄 폴더 이동 (2026-07-27, 사용자 제보)

**증상**: 북마크를 폴더로 정리할 때 하나씩 ⋯ → 이동을 반복해야 해서 부담이 크다.

- `store.js` `moveBookmarks(ids, folderId)` — 여러 개를 **한 번의 저장소 쓰기**로 이동(하나씩 부르면 읽기·쓰기가 반복되고 중간 상태가 노출된다). 대상 폴더 맨 뒤에 붙이되 **서로의 상대 순서는 유지**. 히스토리·없는 id는 무시하고 실제 옮긴 개수만 반환
- `renderList.js` **선택 모드** — 액션 행의 `선택` 토글로 켠다. 평소엔 체크박스를 숨겨 카드가 복잡해지지 않게 하고(드래그 손잡이 자리를 그대로 씀 → 레이아웃 흔들림 없음), 모드일 때만 노출
- 선택 바: `N개 선택 · 전체 선택 · 해제 · 폴더로 이동`. 폴더 선택은 기존 `showFolderPick` 재사용(신규 다이얼로그 0)
- **체크 시 재렌더하지 않는다** — 재렌더하면 스크롤이 맨 위로 튀어 연속 선택이 불가능해진다. 체크박스·행 강조·개수만 직접 갱신
- 선택 중에는 이름을 눌러도 검색이 열리지 않고 선택만 토글(오클릭으로 페이지가 떠나는 것 방지)
- 이동 완료 시 선택 모드 자동 해제 + `N개를 옮겼습니다` 토스트. 폴더 선택 취소 시 선택 상태 유지

**테스트가 잡은 버그 1건**: 선택 모드에서 북마크가 0개가 되면 토글 버튼은 사라지는데 **선택 바만 남아 빠져나갈 방법이 없었다.** 바를 `bookmarks.length`가 있을 때만 렌더하도록 수정.

**테스트 노트**: `selectMode`는 모듈 레벨 상태라 테스트 간에 남아 순서 의존이 생겼다(제품은 페이지 로드마다 초기화되므로 문제 없음). 테스트 헬퍼에서 리스너를 매번 떼고 선택 모드를 꺼진 상태로 정규화해 해결.

테스트 **298/298**(신규 16: `store` 5 + `bulkMove.dom` 11).
**라이브 확인 완료(2026-07-27)**: 선택 토글 → 체크박스 14개 노출·손잡이 대체, 2개 선택 시 `sameNode: true`(재렌더 없음)·행 강조 2개, 폴더 선택 다이얼로그 재사용, 이동 후 `2개를 옮겼습니다` 토스트 + 선택 모드 자동 해제 + 대상 폴더로 실제 이동 확인.


### 조건 칩 클릭으로 능력치 추가 + 빈 폴더 표시 (2026-07-27)

**① 조건 칩 클릭 → 그 검색의 능력치만 지금 검색에 추가** (사용자 제안)
조건 묶음(칩 줄)은 "등록 → 이름 → 클릭" 3단계라 자주 쓰는 소수에만 맞는다. 북마크·히스토리 카드의 **조건 칩을 직접 클릭**하면 등록 없이 그 능력치를 바로 얹는다 — 사용자 계정의 raw query 보유 레코드 68개가 즉시 재료가 된다.
- 조건 칩은 원래 클릭 동작이 없어(호버 툴팁 전용) 충돌 없음. 툴팁에 "클릭하면 이 능력치를 지금 검색에 추가" 한 줄 추가, `cursor: pointer` + hover 강조
- **유형(아이템 종류)은 일부러 제외** — 보던 검색의 유형을 덮으면 '얹기'가 아니라 '바꾸기'가 된다. (칩 줄의 조건 묶음은 완성형 의도라 유형 포함 유지)
- **매번 레코드의 원본 query를 새로 읽는다** → 저장 스냅샷이 아니라서 "옛 묶음엔 그룹이 없다" 같은 마이그레이션 문제가 원천적으로 없다
- `applyAndGo()`로 실행 경로를 공통화(칩 줄·조건 칩이 같은 병합·POST·토스트·연타 차단을 공유)
- raw query가 없거나 능력치가 0개인 레코드는 클릭 불가(클래스 미부여)

**② 폴더 추가가 안 되던 문제** (사용자 제보)
`renderList`가 **빈 폴더를 전부 건너뛰고 있었다**(`if (!items.length && !(isCurrent && g.id === null)) continue`). 새로 만든 폴더는 당연히 비어 있으므로 **만들어도 화면에 안 나타나고**, 드래그해 넣을 대상조차 없어 폴더 기능을 시작할 수가 없었다. 저장은 정상이었고 렌더만의 문제.
- 현재 리그 섹션은 **빈 폴더도 표시**(드롭 타깃 "여기로 드래그"까지). 현재가 아닌 리그 섹션은 기존대로 숨김 — 조작 대상이 아니고 목록만 길어진다

테스트 **282/282**.
**라이브 확인 완료(2026-07-27)**: 조건 칩 클릭 → 능력치만 추가되고 `"… 능력치"을(를) 보던 검색에 얹었어요` 토스트, `cursor: pointer`·툴팁 안내 확인.


### 조건 묶음 — 능력치 그룹 보존 (2026-07-27, 사용자 제보)

**증상**: '숫자(count)' 그룹으로 나눠 만든 검색을 조건 묶음으로 등록하면 그룹이 저장되지 않는다.

**심각도가 보이는 것보다 높다 — 검색 의미가 바뀐다**: `count` 그룹은 "이 중 N개만 만족하면 됨"이다. 평탄화해 `and` 하나로 합치면 **전부 만족해야 하는** 완전히 다른(훨씬 좁은) 검색이 된다. 단순 누락이 아니라 조용한 오작동.

- `conditionSet.js` — `extractConditionSet`이 `groups: [{type, value, filters}]`를 함께 반환(그룹 타입 + 그룹 값 `숫자 ≥N` + `weight` 값까지 보존). 평탄 `stats`는 칩 개수 표시·하위호환용으로 유지
- `mergeConditionSet` 병합 규칙을 그룹 타입별로 분기:
  - `and` → 현재 검색의 `and` 그룹에 얹기(중복 id는 갱신). "전부 만족"이라 합쳐도 의미가 같다
  - `count`·`weight` 등 → **통째로 새 그룹으로 추가**. 그룹 자체가 하나의 조건이라 합치면 깨진다
- `store.js` — `addConditionSet`이 `groups`를 실제로 보관(안 그러면 추출해도 저장 단계에서 버려짐)
- 하위호환: `groups` 없는 기존 묶음은 `stats`를 `and` 그룹 하나로 취급

**검증**: 테스트 **273/273**. 스크린샷 시나리오(숫자≥1 그룹 + 숫자≥2 그룹)를 픽스처로 고정 — 그룹 2개가 `and`로 합쳐지지 않고 각자 `value.min`까지 유지되는지, 현재 검색의 기존 그룹은 안 건드리는지 확인.
**라이브 검증 (2026-07-27)**: 그룹 나뉜 북마크("목걸이 - 초반")를 새로 등록 → 얹기 → 필터 UI에 `숫자 (1)`·`숫자 (2)` 그룹이 각자 값과 소속 조건까지 그대로 재현됨(스크린샷 확인).

**라이브에서 이어서 발견 — 같은 그룹 중복 추가**: 이미 그 그룹을 가진 검색에 얹으면 동일한 `숫자 1` 그룹이 두 개가 됐다. `and`는 stat id로 중복을 걸렀지만 `count`·`weight`는 무조건 append했기 때문. `groupKey()`(타입 + 그룹 값 + 조건 집합, 순서 무관)로 동일 그룹이면 건너뛴다. 같은 묶음을 두 번 얹어도 그룹이 안 늘어난다. 비활성 그룹과는 비교하지 않음(검색에 적용 안 되므로).
**라이브 확인 완료(2026-07-27)**: 같은 조건을 두 번 얹어도 그룹 블록 수 4개 → 4개로 동일. **URL 해시조차 그대로**(`aL5PGwvMSe`)였는데, 병합 결과가 완전히 같아 거래소가 같은 검색을 돌려준 것 — 멱등성의 직접 증거.


### 환산 칩 — 큐레이션 밖 화폐까지 확장 (2026-07-27)

**배경(사용자 제보)**: 카오스 오브 외 화폐(색채의 오브 등)로 등록된 매물에 환산 칩이 안 뜬다.

**원인 — 조용한 회귀**: 경제 API `items`의 키가 **Metadata 경로**(`Metadata/Items/Currency/...`)인데 `itemsRate()`는 거래소 화폐 id(`chrome`)로 조회하고 있었다. 실측 결과 818개 키 중 짧은 id는 **0개** — 이 폴백은 **한 번도 매칭되지 않았다**. 즉 환산 칩은 큐레이션 4종(엑잘·디바인·미러)에서만 동작했고 나머지는 전부 조용히 빠져 있었다. 핸드오프의 "63개 화폐 커버"(2026-07-04) 기록은 BE 스키마 변경 이후 **낡은 정보**였다.
**게다가 기존 테스트가 짧은 키 형태(`{chrome:{...}}`)를 가정하고 있어 프로덕션이 깨진 채로 계속 green이었다** — 픽스처가 실제 응답과 어긋나면 테스트가 회귀를 못 잡는다는 사례.

**수정 — 한글 이름으로 조인**: 두 데이터를 잇는 유일한 공통 키가 한글명이다(거래소 static API의 화폐 `text` == 경제 API의 `ko_name`).
- `currencyRates.js` — `indexItemsByName(items)` 신설(ko_name 색인, 818개 전부 보유·**중복 0** 실측). `itemsRate(index, name, base)`로 시그니처 변경. 색인은 rates 응답당 1회만(WeakMap 캐시 — 800+ 항목을 결과 행마다 훑지 않게)
- `content-main.js` — `ensureCurIcon` → `ensureCurrencyStatic`으로 확장: 기본 화폐 아이콘 + **화폐 id→한글명 맵**을 한 번에 만든다(이미 부르던 API라 요청 추가 없음). `baseFromPrice(..., curNames)`로 전달
- 이름 맵 로드 전에는 기존 큐레이션 4종으로 폴백 → 회귀 없음

**실측 검증 (2026-07-27, 공개 API 직접 대조)**
- 거래소 화폐 45종 표본 → **38종 환산 가능**(기존 3종에서 대폭 확대). `divine`은 미색인이지만 큐레이션이 커버하므로 실질 39종
- **색채의 오브 20개 = 28.01 카오스** — 제보 스크린샷 사례 재현
- 미커버 6종(기술자의 오브·지도제작자의 끌·지평의 오브·선구자의 오브·영원의 오브·주입된 기술자의 오브)은 경제 데이터 자체에 없음 → 설계대로 조용히 미표시
- Domain Fidelity: 원본 엔트리 대조 — 색채의 오브 `primary_currency: chaos`이므로 직접관찰 `chaos_ask(1.40026)` 채택(cross 계산값 아님). 응답 `primary_unit: chaos`, `generated_at` 확인
- 테스트 **264/264**(currencyRates 28, 실제 Metadata 키 형태로 픽스처 교체)

**라이브 확인 완료(2026-07-27, 사용자 직접)**: 실제 매물 화면에서 카오스 외 화폐 매물에 환산 칩이 표시되는 것 확인


### 조건 묶음 — 자주 쓰는 조건을 클릭 1회로 얹기 (2026-07-27, `c58e200`+`efa9011` 푸시 완료)

**배경(사용자 피드백)**: 경쟁사 [Poe Trade Plus](https://chromewebstore.google.com/detail/poe-trade-plus/igofmcebdienfacijkhdppcfiglcbffb)를 지목하며 "자주 검색하는 조건을 템플릿으로" 요청. **핵심은 입력 최소화**(사용자 정정).

**경쟁사 분석 결과**: 스토어 리스팅 전문·GitHub·Firefox 리스팅 대조. 실제 기능은 `Quick Filter Presets` 한 줄뿐이고, 원형은 **조건 1개씩 넣는 버튼**(총 생명력·저항·이속·화폐). "완성된 검색을 한 번에"는 경쟁사에도 없음. 그쪽은 UI를 조작하는 방식이라 **조건 1개가 구조적 상한**. 우리는 검색 바디를 만들어 POST할 수 있어(리그 이관에서 확보) 묶음을 통째로 얹을 수 있다 — 따라하기가 아니라 다른 물건.

**입력 횟수**: 조건 7개 기준 거래소 맨손 ~35회 / 경쟁사 ~21회 / **우리 1회**.

**설계 판단 2건 (데이터 근거)**
- **자동 발견 기각** — "같은 구조를 반복 검색하면 템플릿으로 승격" 가설을 사용자 실데이터로 측정. 반복 구조 8개 중 `서로다른날 ≥ 2`는 1개뿐이고 그마저 **조건수 0**(파라미터화 불가). 지배적 패턴은 **하루 안 3~4회 반복**(= 한 세션 수치 조율, 장기 재사용 아님). 따라서 **수동 등록**만 제공
- **앱 제공 기본 묶음 제외** — 메타 종속 콘텐츠는 리그마다 갱신해야 하는 영구 부채. 사용자 결정으로 제외. 콜드 스타트가 문제되면 내보내기/가져오기만 열어 커뮤니티가 배포하게 하면 됨(우리가 관리 주체가 되지 않음)

**구현**
- `src/lib/conditionSet.js` **신규** — `extractConditionSet`(raw query → 스탯+유형, 비활성 조건 제외, 가격 등 상황 의존 필터는 **의도적으로 미포함**), `mergeConditionSet`(현재 검색에 얹기 — 관계없는 설정 보존·중복 id는 묶음 값으로 갱신·활성 `and` 그룹에만·**원본 불변**), `conditionSetSummary`
- `store.js` — `conditionSets` 별도 키(`folders`와 같은 패턴). `listConditionSets`/`addConditionSet`/`removeConditionSet`/`renameConditionSet`/`moveConditionSet`. **게임별 엄격 분리**(poe1/poe2 스탯 id 체계가 달라 섞이면 검색이 깨짐)
- `content-main.js` — `applyConditionSet(set)`: `lastQuery`에 병합 → `sanitizeQuery` → POST → id·URL 검증 → `{url, merged}` 반환
- `panel.js` — 리스트 위 **칩 줄**(묶음 없으면 hidden), 클릭=얹기, 편집 모드 토글(삭제·순서는 편집 중에만 노출 — 오클릭 방지), `storage.onChanged` 구독(탭 간 동기화)
- `renderList.js` — 북마크·히스토리 ⋯ 에 "조건 묶음으로 등록"(raw query 있는 것만)
- **사후 안내**: 얹기 성공 시 `baSetApplied` 플래그를 남기고 이동 → 새 페이지에서 1회 토스트로 "보던 검색에 얹었어요 — 되돌리려면 뒤로가기". 이동 전 토스트는 페이지가 바뀌며 사라지므로 이 경로가 필요. 15초 지난 흔적은 무시

**검증 중 잡은 버그 2건**
1. `moveConditionSet`이 **배열 인덱스**로 이웃을 찾는데 표시는 `order` 정렬 — 한 번 스왑하면 이후 이동이 먹지 않음(테스트가 적발). 정렬된 순서에서 이웃을 찾도록 수정. `moveFolder`는 배열 순서로 표시하므로 같은 코드가 맞다 — 두 모델을 섞은 게 원인
2. `ui` 객체 리터럴이 `registerConditionSet`을 선언 전에 참조 → **TDZ ReferenceError**로 패널 마운트가 통째로 실패. 기존 `ui.saveCurrentSearch = doSave` 지연 배선 패턴으로 수정

**라이브 검증 (2026-07-27, 실제 거래소 로그인 세션)**
- 등록: ⋯ → 조건 묶음으로 등록 → 실제 북마크에서 **조건 7개 정확 추출**(한글 스탯명 포함), 칩 생성
- 얹기: "투구 - 저렴이"(결과 10건) 위에 목걸이 묶음 → **투구 조건과 목걸이 조건이 함께** 들어가고 해시 변경. 결과 0건은 정상(상충 조건의 `and` 병합)
- 사후 토스트: "…보던 검색에 얹었어요 — 되돌리려면 뒤로가기" 정상
- **리그 이관도 함께 확인**(0.4.0 미검증분 해소) — "장화 - 초반" → 이동 속도 조건 살아있는 검색, 결과 10건
- 부수 발견: **거래소가 페이지 로드 후 검색을 재생성해 URL을 갈아끼운다**(저장된 해시가 살아 있어도 URL 유지 안 됨). 우리 코드와 무관 — 원본 해시 직접 이동만으로도 재현됨

**라이브에서 잡은 버그 — 늦은 ui 배선 (사용자 제보: "조건 묶음으로 등록이 작동 안 한다")**
- 증상: `조건 묶음으로 등록`이 **간헐적 무반응**(에러·토스트 없음). 추적 중 **기존 기능인 `+ 이 폴더에 현재 검색 저장`도 함께 죽어 있음**을 발견 — 둘 다 `ui` 객체에 **나중에 붙이는** 콜백(`ui.registerConditionSet` / `ui.saveCurrentSearch`)에 의존. 그 배선 지점까지 실행이 도달하지 못하면 `if (ui.xxx)` 가드가 조용히 건너뛴다
- 대조 근거: 같은 상태에서 `이름 변경`(ui 리터럴에 있는 콜백)은 정상, 저 둘만 무반응
- 수정: 두 콜백을 **ui 리터럴 안에서 화살표로 감싸 호출 시점에 해석**하도록 변경(`registerConditionSet: (id) => registerConditionSet(id)`). 늦은 배선 의존이 사라져 이 버그 부류가 제거됨. 기존 `saveCurrentSearch`의 같은 취약점도 함께 해소
- ⚠ **정확한 방아쇠는 미규명** — 신선한 로드에서는 매번 정상이고 특정 내비게이션 이후에만 재현됐다. 이번 수정은 원인 제거가 아니라 **그 원인의 영향을 받지 않게** 만든 것
- 재검증: 새 빌드로 팝오버 열림/닫힘 × 북마크/히스토리 × 칩 얹기 직후 내비게이션(이전에 죽던 지점) 전부 통과

**하네스 실측**: 등록(⋯ → 이름 → 칩 생성·저장 내용 확인) / 칩 클릭 → `applyConditionSet` 1회 호출 / **연타 3회 → 요청 1회**(429 방지) / 편집 중 칩 클릭 무시 / 마지막 묶음 삭제 시 칩 줄 자동 숨김 / 칩 12개에서 높이 105px 상한·목록 밀림 없음 / **XSS 시도(`<img onerror>`·`<script>`)가 리터럴로 이스케이프**되고 실행 0

### 아이템 검색칸 완전 삭제 불가 버그 수정 (2026-07-22, `c58e200`에 포함·푸시 완료)

**증상(사용자 제보)**: 거래소 "아이템 검색"(Alt+F) 입력칸에 이미 검색어가 있을 때, Backspace·Ctrl+A+Delete 등 무엇으로 지워도 검색어가 지워지지 않는다.

**원인**: 우리 확장의 `fuzzyPrefix.js`(퍼지 검색 기본화를 위해 검색칸 맨 앞에 "~"를 항상 유지·자동 복구하는 로직)가 지나치게 공격적이었다 — ① `input` 리스너가 값이 빈 문자열이 되는 즉시 "~"를 다시 채워 넣고 ② `keydown` 가드가 "~" 한 글자만 남았을 때도 Backspace를 막아, **어떤 방법으로도 검색칸을 진짜로 비울 수 없었다**(최소 "~" 하나는 항상 남음). Alt+F 단축키 자체의 문제는 아니고, 페이지에 상시 주입되는 이 안전망 로직이 원인.

- `src/content/fuzzyPrefix.js` — `input`/`compositionend` 핸들러에 `value === ''`면 보강하지 않는 예외 추가, `keydown` Backspace 가드에 `el.value.length > PREFIX.length` 조건 추가("~"만 남았을 때는 막지 않음). 뒤에 다른 글자가 있을 때의 기존 보호(오타로 "~"만 지워지는 것 방지)는 그대로 유지
- `test/fuzzyPrefix.dom.test.js` 신규 9케이스 — jsdom이 `document.execCommand`를 구현하지 않아 실제 브라우저의 insertText 동작(선택 구간에 텍스트 삽입 + input 이벤트 동기 발생)을 그대로 흉내내는 모킹을 두고 검증. 회귀 재현(전체 삭제·마지막 글자 Backspace) + 기존 동작 보존(뒤에 글자 있으면 여전히 보호·빈 칸 클릭 시 자동 삽입·붙여넣기 보강) 모두 커버
- `vitest.config.js`에 `environmentOptions.jsdom.pretendToBeVisual: true` 전역 추가 — 기본값 false면 jsdom의 `el.focus()`가 조용히 no-op해 `document.activeElement`가 갱신되지 않아(mock execCommand가 activeElement로 대상을 찾음) 테스트 자체가 성립 안 함. 다른 jsdom 테스트는 focus 미사용이라 영향 없음
- 테스트 **227/227**(신규 9) · 빌드 통과
- **함정 기록**: `fuzzyPrefix`는 `navigator.userActivation`으로 "실제 클릭 vs 사이트 자동 포커스"를 구분한다. jsdom엔 이 API가 아예 없어 테스트에서 직접 정의해야 하고, 안 하면 모든 focus가 자동 포커스로 오인돼 즉시 blur된다(테스트가 통째로 무동작)
- **라이브 확인 완료(2026-07-27, 사용자 직접)** — 실제 키보드 입력이 필요한 검증이라(`keydown` 가드 + `navigator.userActivation` 판별이 핵심이므로 합성 이벤트로는 무의미) 사용자가 직접 확인함

### 리그 이관(저장 조건 → 현재 리그 재검색) + 가이드 투어 예시 요소 (2026-07-22, `develop` 푸시 완료)

**핵심 판단 — 미완료 6번의 전제 교정**: "자동 채움 = 거래소 Vue 필터 UI를 클릭·타이핑으로 흉내내야 함(고리스크)"라는 기존 전제가 틀렸다. `cross-site-receiver.js`가 이미 쓰던 **공식 검색 생성 API**(`POST /api/trade2/search/poe2/<리그>`)에 저장된 raw query를 그대로 다시 제출하면, 반환된 해시 URL로 이동하는 것만으로 거래소가 **필터 UI까지 조건대로 채워** 렌더한다. UI 자동화 없이 (a)안이 노리던 결과를 그대로 얻는다. 유일한 걸림돌은 "raw query를 저장하지 않았다"였고, 그건 필드 추가로 해결.

- `src/lib/tradeSearch.js` **신규** — 순수 헬퍼 4종. `searchApiPath`/`searchResultPath`(게임별 경로·리그 인코딩), `isSafeSearchId`(응답 id를 URL에 이어 붙이므로 `[A-Za-z0-9_-]{1,64}`만 허용 — 경로 탈출 차단), `sanitizeQuery`(최상위 키를 query·sort로 화이트리스트 + 20KB 상한 + JSON 왕복). 헤더에 "왜 UI 자동 채움이 아닌가" 근거 기록
- `store.js` — 레코드에 `query`(raw 검색 조건) 추가. `backfillQuery(dedupeKey, game, query)`: query 도입 전 저장한 **구 북마크는 사용자가 같은 조건을 재검색하는 순간 자동으로 채워져** 이관 가능해짐(있는 값은 안 덮음). `migrateBookmarkLeague(id, url, league)`: url·league·lastUsedAt만 교체(이름·폴더·순서·메모·id·query 보존). `overwriteBookmark`·`markUsedByUrl`도 query 승계. `importBookmarksJSON`은 남의 query를 `sanitizeQuery` 통과시킨 뒤에만 채택(불합격이면 조건만 버리고 북마크는 유지)
- `content-main.js` — 히스토리 저장 시 `query` 동봉 + `backfillQuery` 호출. `migrateSearch(query, league)`: same-origin POST → 429/401·403/기타 HTTP를 각각 다른 사유로 반환 → id·URL 검증 후 결과 URL 반환. **일괄 이관은 의도적으로 미제공**(요청 폭주 시 GGG rate limit에 걸리면 거래소 검색 자체가 막힘)
- `renderList.js` — 진입점 2개: ① **지난 리그 북마크 이름 클릭 시 제안 팝오버**(기존 `showConflict` 재사용 — 신규 UI 요소 0): 조건이 있으면 [그대로 열기 / 현재 리그로 다시 검색], 없으면 **저장된 조건 요약을 보여주고** [그대로 열기](= 검토했던 "참고용 재구성 도우미"안의 가치를 여기서 흡수, 110자 초과 시 말줄임) ② ⋯ 팝오버의 `현재 리그로 다시 검색`(에메랄드 `.ba-act.relg`, query 있는 북마크만 노출 — 현재 리그 북마크도 해시 만료 복구용으로 사용 가능). Ctrl/⌘ 클릭(새 탭)은 기존대로 원본 열기
- **테스트가 잡은 버그 2건**: ① 중복 클릭 가드를 첫 `await` **뒤**에 둬서 연타 3회가 전부 통과 → 요청 3번(rate limit 유발). 동기 시점으로 이동 ② URL 허용 도메인 검증이 **이동 시점에만** 있어, 이상한 URL이 북마크에 먼저 기록됨 → 저장 전으로 검증 이동
- **가이드 투어 예시 요소** — 투어 2·3스텝(PoB 버튼·환산 칩)은 검색 결과가 없으면 가리킬 대상이 없어 스포트라이트가 통째로 사라졌다(사용자 스크린샷 제보). `content-main.js`의 `showTourDemo/hideTourDemo`가 **실제와 같은 클래스**(`.ba-pob-btn`/`.ba-exr-chip`)를 가진 '예시' 카드를 패널 반대편 자유 영역(패널 폭 412px 회피)에 놓고, `panel.js` 투어가 대상 없을 때만 호출→종료·다른 스텝에서 제거. 오인 방지: 점선 테두리 + "예시" 배지 + `pointer-events:none`. **진입 모션은 opacity만**(transform을 주면 투어가 삽입 직후 동기로 재는 rect가 어긋나 스포트라이트가 밀린다), 160ms `cubic-bezier(0.23,1,0.32,1)`, reduced-motion 대응
- **후속 수정 — "현재 리그"의 진실 소스 교정 (같은 날, 사용자 제보: "처음에 settlers로 리그명이 설정돼 있다")**
  - 증상: 오래된 북마크 링크로 들어오면 URL이 이미 끝난 리그(예: `Settlers`)라서 **패널이 그 죽은 리그를 "현재"로 표시**하고 진짜 현재 리그가 "지난"으로 뒤바뀐다. 그 상태에서 이관하면 **죽은 리그에 검색을 만든다**
  - 원인: '현재 리그' 판정을 `leagueFromUrl()`(페이지 URL)에만 의존 — 2026-07-04 리그 배지 때부터 있던 문제이고, 이관 기능이 그 위에 얹히면서 실해를 끼치게 됨
  - 수정: `renderList.js`에 `leagueInfo(leagueMap)` 신설 — **거래소 리그 목록 API(현재 열려 있는 리그만 반환)를 진실 소스로** 삼는다. poe1 카카오는 URL이 표시명("허상")·맵 키가 id("Mirage")라 양쪽 형태를 모두 인정. 목록을 못 받았으면 판정 보류(성급한 경고 방지)
  - 파급: ① "지난" 판정 = **끝난 리그**일 때만(스탠다드↔하드코어처럼 둘 다 살아있으면 안 깨진 것 — 기존엔 전부 "지난"으로 오탐) ② 섹션 접힘 기본값 = 끝난 리그만 접힘 ③ 살아있는 다른 리그 섹션은 배지 없음 ④ **이관 대상 리그**는 페이지 리그가 살아있을 때만 그것, 아니면 최근 히스토리의 살아있는 리그, 그것도 없으면 요청하지 않고 안내
  - `rowHtml`/`folderHtml`의 `currentLeague` 인자는 이 변경으로 고아가 돼 제거(대신 `lg` 전달 — 부수적으로 북마크 툴팁도 리그 **표시명**으로 나오게 됨, 기존엔 id 노출)
- **'내 리그' 설정 신설 (사용자 제안: "그냥 리그 정보를 유저들이 입력하게 하는 건 어떨까?")**
  - 설정 모달(Alt+O)에 **내 리그** 셀렉트 — 옵션은 거래소 리그 목록(살아있는 리그) + "자동". 게임별로 따로 저장(`uiLeague = {poe1, poe2}`), 다른 탭과 `storage.onChanged` 동기화
  - 우선순위: **설정 → 페이지 URL 리그 → 최근 검색 리그** (각 후보는 살아있을 때만 채택). `resolveCurrentLeague()`로 분리 — 이 값 하나가 섹션 '현재' 배지와 이관 대상을 **동시에** 결정한다(따로 계산하면 화면 표시와 실제 대상이 어긋난다). `bindAll`은 렌더가 정한 값을 그대로 받아 쓴다
  - 기본값은 여전히 자동 — 설정을 안 건드려도 대부분 맞고, 자동이 흔들리는 상황(끝난 리그 링크로 진입)에서만 사용자가 못 박으면 된다
  - 설정 모달 정리(사용자 지적): **패널 위치 세그먼트를 `왼쪽 | 오른쪽` 순으로** — 버튼 위치가 그 뜻과 같은 쪽에 있어야 라벨을 안 읽고도 고른다(기존은 `오른쪽 | 왼쪽`으로 반대). '내 리그' 행은 리그 목록을 못 받았을 때도 항상 렌더(저장값이 목록에 없으면 그 값도 선택지로 유지 — 설정이 사라진 것처럼 보이지 않게)
- **이관을 2단으로 재구성 — 조건 미저장 구 북마크까지 복구 (사용자 확인: "리그 단위로 검색 조건 유지가 되는 것 같아")**
  - 확인된 사실: **검색 해시는 조건만 담고 리그는 URL 경로가 정한다**(`/trade/search/Hardcore/EBo8vB8LC5` ↔ `/Standard/EBo8vB8LC5`). 따라서 리그 세그먼트만 바꾸면 같은 조건이 그 리그에서 검색된다
  - `migrateSearch(rec, league)` — ① 저장된 URL에서 해시 추출 → `GET /api/trade{2}/search/<목표리그>/<해시>`로 살아있는지 확인 → OK면 그 URL로 이동(**새 검색 생성 없음**) ② 해시가 만료됐으면 저장된 조건으로 POST 재생성 ③ 둘 다 안 되면 사유별 안내(`expired` 등)
  - **효과: query 도입 전 저장한 옛 북마크도 전부 이관 대상**(현재 구현의 유일한 사각지대였음). `migratable(rec)` = 해시 ‖ 조건
  - `searchHashFromUrl(url, game)` 신설(도메인·경로·해시 형태 검증). `isAllowedTradeUrl`은 이 모듈이 URL 조립에도 써야 해서 **정본을 `lib/tradeSearch.js`로 옮기고 `store.js`가 재수출**(순환 import 방지, 기존 import 경로 전부 그대로)
  - ⋯ 액션 라벨을 **"내 리그로 다시 검색"** 으로 통일(설정 이름과 일치). 이제 모든 북마크에 노출 — 리그 이동뿐 아니라 **만료된 링크 복구** 경로이기도 하다. 투어 ⋯ 스텝 문구도 갱신
- **투어 데모 조건 교정 (사용자 제보: 5·6·8스텝도 하이라이팅이 안 된다)**
  - 원인: 데모 주입 조건이 `isStoreEmpty`(저장소가 통째로 비었나) — **히스토리만 쌓이고 북마크는 0개**인 흔한 상태에선 false라 데모가 안 깔렸다. 그 상태에서 북마크 이름(5)·가격 필(6)·폴더 아이콘(8) 스텝은 가리킬 요소가 아예 없다(2·3스텝의 페이지 요소 문제와 같은 성격, 원인만 다름)
  - `needsTourDemo(game)`로 교체 — **북마크 0 ‖ 실폴더 0 ‖ 가격 스냅샷 가진 북마크 0** 중 하나라도 해당하면 데모를 띄운다(데모가 폴더 1 + 가격 있는 북마크 2를 넣어 셋을 한 번에 메움). `isStoreEmpty`는 이 변경으로 고아가 돼 제거
  - 투어 렌더에서 **접힌 리그 섹션도** 임시로 펼친다(기존엔 접힌 폴더만) — 끝난 리그가 기본 접힘이라 북마크가 지난 리그에만 있으면 같은 공백이 생긴다
  - 데모 링크를 게임별 경로로(`poe1 → /trade/search/`) — 데모가 더 자주 뜨게 됐으므로 잘못 클릭 시 엉뚱한 게임 404로 가지 않게
  - 하네스 실측(히스토리 3·북마크 0·폴더 0 재현): 4~8스텝 스포트라이트 전부 실제 크기로 잡힘, 투어 종료 후 북마크 0·폴더 0으로 원복(저장소 무오염)
  - **데모 리그명 교정(사용자 지적: 투어에 'Settlers'가 뜬다)**: 데모를 페이지 URL 리그로 심으면, 옛 북마크 링크로 들어온 상태에선 그 URL이 이미 끝난 리그라 예시 데이터가 `Settlers` 섹션(지난·접힘)에 들어갔다. 이제 `resolveCurrentLeague`(설정 → 페이지(살아있을 때) → 최근 검색)로 심고, **하나도 못 정하면 실제 리그명 대신 `예전 리그`** 로 적는다. 하네스 실측 — 정상: `Runes of Aldur:현재` / 끝난 리그 페이지: `예전 리그:지난`. 하네스에 `__leagueMap` 오버라이드 추가(끝난 리그 상황 재현용)
- 테스트 **218/218**(신규 53: `tradeSearch` 22 + `store` 12 + `leagueMigration.dom` 19) · 빌드 통과
- 하네스(`test-harness/harness.mjs`)에 지난 리그 북마크 2종(조건 有/無)·`migrateSearch` 목(`__migrateResult`로 성공·실패 전환)·투어 예시 스텁 추가

### CLAUDE.md 신설 — 전역 지침 감사 후속 (2026-07-07)
- 루트 `CLAUDE.md` 신규(최소형): 멀티 페르소나 도메인 선언(Domain-Specific Tool) + geo-redirect 제약·'POE 브라우저' 검증 관례 명시
- 전역 `~/.claude/CLAUDE.md` §14.4 매핑에 본 프로젝트 등록 — 4역할 검증 자동 트리거 정상화
- main 커밋·push 완료 (`99c32a5`, 2026-07-07)

### 소개 영상 대본 작성 (2026-07-06)
- `docs/영상-소개-대본.md` 신규 — 장면(🎬)+내레이션(🎙) 형식, 3분 30초~4분 30초 구성
- 구성: 훅 → 소개 → 핵심 기능 4꼭지 → **피드백 3건 업데이트 스토리**(저장 3지선다·패널 좌/우·PoB 영문 복사+Shift+클릭 제보) → "피드백으로 함께 개선" 마무리(사용자 지정 멘트)
- 부록: 촬영 체크리스트(⚠️ 0.3.0 기능은 스토어 심사 통과 후 공개 권장)·제목 후보·설명란 초안(디스코드 링크 포함)
- **미커밋 상태** — 사용자 검토 후 커밋 여부 결정

### 0.2.0 릴리즈 (2026-07-01)
- 커밋: `592d2c6` → `db73b33` → `ae71972` (origin/main 푸시 완료)
- 배포: `deploy/poe-bookmark-atlas-0.2.0.zip` 업로드 + **스토어 심사 제출 완료** (0.1.0 zip 보관)
- 주요 변경 (db73b33 + ae71972):
  - 북마크 **간략(한 줄) 보기** 토글 — `renderList.js`, `panel.css .ba-oneline` (content-visibility 대신 실측 높이)
  - 폴더별 **"현재 검색 저장" 칩** — 폴더 본문 최상단 고정 (`renderList` folderHtml `.ba-folder-savechip`)
  - 메모를 **조건 칩 우측 한 행**으로 통합 (`.ba-meta-row`) + 이름 클릭영역 테두리 (`.ba-open`)
  - **가격 호버 툴팁**(검색 시점·매물수·p25 빠른 판매가), 히스토리 **조건 칩**(필터 포함 카운트)·대표 이미지
  - **액션 버튼 의미색**(복사=청록·갱신=하늘·편집=바이올렛·이동=앰버·삭제=로즈) + 폴더 버튼
  - **정렬 기본값 recent** + 새 북마크를 폴더 맨 앞 저장 (`store.js` minBookmarkOrder)
  - **schemaVersion 도입** (`store.js` ensureSchema/MIGRATIONS, `content-main.js` 호출) — 향후 데이터 마이그레이션 진입점
  - **가이드 투어 박스(스포트라이트 구멍) 방식 전면 재설계** (`panel.js` startTour, `.ba-tour-spot`): 12스텝, 이전 버튼, 숨쉬는 테두리, 패딩 대칭 보정, 동기 즉시 배치(비활성 탭 대응), 빈 화면용 **데모 데이터** 임시 주입/제거 (`store.js` seedDemoData/clearDemoData/isStoreEmpty)
  - "모든 폴더 접기" 용어 통일

### 검색 통합·리그 접이식 (592d2c6)
- 북마크·히스토리 통합 검색, 리그별 접이식 섹션(현재 펼침/지난 접힘), 정렬 토글 헤더 이동

### 피드백 작업1·2 + 버그픽스 (2026-07-02~03)

- **작업1 — 저장 충돌 판정·UX 재설계** (`searchParser.js`, `renderList.js`, `panel.js`)
  - `filterParts()`: 비-능력치 필터(경로석 확률·등급 등)를 searchIdentity에 포함 → 수치만 바꿔도 "이미 저장됨" 오판 해결
  - `structuralIdentity()`/`findNearDuplicate()`: 수치만 다른 near-dup 감지 → **취소/새로 만들기/덮어쓰기 3지선다** (`resolveSaveConflict` + `showConflict` 팝오버)
  - 조건 칩에 입력 수치 표기(≥/≤/~) + '조건 N개' 앰버 배지, 북마크 열람 시 검색 필드 업그레이드(`markUsedByUrl` 4번째 인자)
- **스포트라이트 focus 재설계**: opacity 디밍이 패널 opacity transition들과 충돌해 무력화 → **투어식 hole-punch 오버레이**(`.ba-focus-spot`)로 교체. 대화 중 **재렌더 레이스**(행 교체 → 강조·스크롤 소실, "취소해야 스크롤됨") 해결: 행 재조회 + MutationObserver 재적용 + center 스크롤 + 휠 잠금 + 접힌 폴더 자동 펼침
- **카드 2줄 구성 + ⋯ 액션 팝오버**: 액션 5종 팝오버 통합(간략/상세 공통). shadow retargeting(`composedPath` 필수), hover transform containing-block, content-visibility, **flip 기준 리스트 가시영역 바닥**(하단 잘림) 픽스
- **작업2 — 패널 좌/우 설정**: 기어 → 설정 모달(`showSettings`), `uiPanelSide` storage, `[data-side="left"]` 미러링, 핸들 `left` transition
- **팝업 정리**: 영문거래소·시세/동향 버튼 제거
- **E2E 하네스** (`test-harness/`, `vite.harness.config.js`, `.claude/launch.json`): 목 chrome + 시드로 패널 마운트, Preview MCP(`preview_eval`)로 자동 검증. `__triggerConflict`/`__saveUnique`/`__dumpBookmarks` 헬퍼. **주의: 스크린샷·rAF는 이 환경에서 멈춤 → eval+setTimeout만**

### 작업3 — 영문 PoB 복사 (poe1·poe2 종단 완료)

- **선결 2건 해소**: ① `item.extended.hashes` 라이브 캡처 확인 ② EN stat 취득 — **이 개발 환경은 pathofexile.com geo-block 안 걸림**(curl 통과, Node fetch는 Cloudflare 차단)
- **맵 생성 스크립트** — poe1·poe2 각각 3종(총 6개 JSON, `src/lib/pob{Stat,Base,Unique}Map[.poe1].json`):
  - `scripts/build-pob-statmap.mjs`: EN+KR trade stats 페어링. **id별 k번째 등장 매칭**으로 개선(단순 인덱스 페어링은 그룹 entry 수가 1개라도 어긋나면 그 이후 전부 오염 — poe1 enchant EN 1465/KR 1464 케이스로 발견). poe2 8086 id(다중변형 80) · poe1 14969 id(다중변형 229, 미페어링 1)
  - `scripts/build-pob-basemap.mjs`: poe-i18n 생성기 base_types+unique_items → base/유니크 KR→EN. poe2 base 3172·유니크 444 / poe1 base 3102·유니크 1201
- `src/lib/pobExport.js` — `buildPobText()`: Item Class/Rarity/이름/base EN/ilvl/**enchant**(implicit 옆 별도 섹션)/**implicit**/**fractured+explicit+crafted**(한 섹션, 인게임 순서)/Corrupted. 희귀 이름은 KR↔EN 데이터 없어(+PoB 폰트에 한글 없음) `seominugi-bookmark-item-<classId 슬러그>` ASCII 치환, 유니크는 uniqueMap으로 EN 실명. 26개 TDD(캡처 실아이템 포함)
- `src/lib/currencyRates.js` — `baseCurrencyOf(game)`(poe2=exalted/poe1=chaos), `baseFromPrice(price, ex, game)`(카오스·디바인·미러→게임 기본화폐, poe1은 `chaos_per_exalted` 등 다른 키셋), `fmtCurAmount`. 16 TDD
- `src/content/content-main.js` — 종단 통합:
  - 모든 fetch(스크롤 포함)에서 `pobItems`(id→item)·`pobPrices`(id→listing.price) 누적, 새 검색 시 초기화
  - poe1 리그명 버그: URL엔 KR 표시명("허상")인데 BE는 EN id("Mirage") 요구 → **leagueMap 역변환** 추가(부수 효과로 poe1 북마크 가격 스냅샷도 복구)
  - PoB 버튼: **'인증 완료' 배지 아래**(자연 높이, 텍스트 앵커 — 이미지 로딩 타이밍 무관) 1순위 → 이미지 오른쪽(이미지 로딩 전이면 다음 패스로 스킵) → 우측 버튼 줄 아래. 2줄 라벨(PoB/영문 복사), 바이올렛 글래스모피즘, lazy-load(클릭 시 게임별 맵 3종 동적 import)
  - 엑잘/카오스 환산 칩: `findPriceHost`(텍스트 앵커 '제시 가격'/'정가')에 주입, **GGG 공식 static API**(`api/trade[2]/data/static`)의 CDN 화폐 이미지 사용(확장 내부 URL은 페이지 CSP로 깨짐), 제시가 화폐 아이콘 실측 크기에 맞춰 칩 아이콘·폰트 스케일(`!important`로 사이트 전역 img 규칙 무력화), 텍스트 폴백→아이콘 도착 시 업그레이드
  - body 전역 MutationObserver(디바운스 100ms) + 타이머 5단계 이중 안전망으로 행 주입(컨테이너 탐색·재부착 의존 제거)
- `src/content/panel/panel.js` — 설정 단축키 **Alt+O** 추가(Alt+S는 검색저장 선점) — kbd 안내 팝오버·기어 툴팁 반영
- 문서: `docs/영문-pob-복사-선행조사.md` (캡처 결과·아키텍처 C 확정)

### 작업3 이후 다듬기 라운드 (2026-07-04, 미커밋)

- **PoB 번역 정확도 버그 2건**:
  - `(Local)`/`(Global)` 접미사(거래소 필터 UI 전용 표시, 인게임 텍스트엔 없음) 미제거 → PoB가 mod 인식 못 함. `translateMod`에서 `TRADE_ONLY_SUFFIX` 정규식으로 제거
  - `fillValues` 결과에 `#`가 안 채워진 채 남는 케이스(클러스터 주얼류 "Allocates #" 같은 텍스트형 옵션 — `#` 자리가 숫자가 아니라 특성 이름) → `missing[]` 집계에도 안 잡히던 사각지대. 이제 `#` 잔존 시 `en=null`(실패) 처리해 KR 폴백 + 집계
  - **알려진 한계**: poe1 탈리스만(부적류 장신구) 베이스가 poe-i18n 데이터셋에 통째로 없음(Talisman 리그 아이템 미포함) — 확인됨, 대안 데이터소스 필요시 별도 조사
- **엑잘/카오스 환산 칩 커버리지 확장**: 큐레이션 4종(엑잘·디바인·미러) 밖 BE `items` 맵(개별 화폐 시세, poe1 341개/poe2 608개) 폴백 추가 — `itemsRate()`. 거래소 103개 화폐 id 중 63개 커버(스크린샷 재현: 쥬얼러 오브·색채의 오브 등). `primary_currency` 일치 시 직접관찰 `_ask`, 아니면 cross 계산값 사용(둘이 갈리는 사례 발견해 구분)
- **히스토리 카드 재설계 (여러 라운드)**:
  - 리그 통합(모든 리그 단일 섹션, 북마크는 리그별 유지) — `listByKind`가 이미 시간순 정렬이라 재파티션만 제거
  - 카드 UI를 북마크와 동일한 **조건칩+⋯팝오버** 언어로 통일 — 액션(북마크로 저장·링크복사·삭제)을 `.ba-actions-pop`으로 이동(신규 JS 불필요, 기존 범용 핸들러 재사용)
  - 리그 표시는 별도 칩(가변 길이 → 말줄임 문제) 대신 **조건칩(없으면 날짜칩) 툴팁 맨 위**로 이동 — `[리그] 《...》` 마커를 tooltip 렌더러가 시안색 span으로 치환(`.ba-tip-accent`, 기존 `────────`→`<hr>` 패턴과 동일 메커니즘 확장)
  - 날짜는 항상 연월일시분 전체(`fmtTime`), 히스토리 전용 시안(cyan) 색 칩(`.ba-hist-price`/`.ba-hist-when`)으로 북마크 골드 칩과 구분
  - **버그 2건 — 북마크에도 있던 잠재 버그**: ① 가격 없을 때 빈 필(pill) 렌더(`.ba-price-pill`/`.ba-hist-price` 둘 다 무조건 래핑하던 게 원인) → 가격 있을 때만 렌더로 수정. ② ⋯ 버튼이 행 우측 끝에 고정 안 됨(칩이 짧으면 남는 공간이 ⋯ 뒤에 생김, 북마크는 조건 텍스트가 보통 길어 우연히 안 보이던 문제) → `.ba-more`에 `margin-left:auto`
- **헤더 레이아웃**: `.ba-brand` 행에 로고·⌨·서미누기제작·⚙·♥ 5개 고정폭 요소가 몰려 타이틀 줄바꿈 → 설정(⚙) 버튼을 푸터로 이동("사용법 가이드 다시 보기" 옆), 아이콘 전용→"설정" 텍스트 병기로 직관성 개선
- **가이드 투어**: 죽은 `.ba-rowfoot` 셀렉터(카드 리스트럭처링으로 고아됨) → `.ba-more`로 교체, 설정 스텝 신규 추가, 투어 엔진이 페이지 바깥(PoB 버튼·환산 칩) 요소도 스포트라이트 가능하도록 확장(`global:true`, shadow root가 `.ba-root`의 transform과 무관해 좌표 안전)
- **Shift+클릭 수동 제보**: PoB 버튼에 미변환 mod 있으면 Shift+클릭으로 제보 텍스트를 클립보드에 복사 + 기존 공개 Discord 초대 링크(`discord.gg/kEm2G2qcZQ`)를 새 탭으로 오픈. **웹훅 직접 연동 안 함**(클라이언트에 webhook 시크릿 노출 시 악용 위험 — 확장은 언패킹된 JS라 누구나 추출 가능). seominugi.com 백엔드(smng-poe-pricer, 별도 레포) 쪽에 제보 수신 엔드포인트 생기면 `reportMissing()` 내부만 fetch(POST)로 교체하면 됨, UI·트리거는 안 바꿔도 됨

### 가이드 투어 화살표·키보드 네비·리그 노출 라운드 (2026-07-04, 이어서)

- **가이드 투어 스포트라이트↔카드 연결 화살표** (`panel.js` startTour) — 사용자 스크린샷 피드백 기반 여러 라운드 반복:
  - SVG 베지어 곡선(`.ba-tour-arrow`)으로 연결. `edgeAnchor()`가 내 상하좌우 네 변의 가운데 점 중 상대 중심에 **실제로 더 가까운 곳**을 직접 거리 비교로 선택(중심점 dx/dy 비율 방식은 옆으로 넓적한 박스에서 엉뚱한 변을 골라 폐기)
  - 여백(pad)이 실제 간격보다 크면 시작·끝점이 서로 앞질러 곡선이 카드 밑으로 접혀 들어가는 버그 → 여백 없는 기준점 사이 실측 간격(rawGap)에 맞춰 pad를 동적으로 축소
  - `.ba-tour-spot`은 top/left/width/height에 CSS transition이 걸려 있어 스타일 변경 직후 동기 `getBoundingClientRect()`로 되읽으면 트랜지션 시작 전(한 스텝 전) 값이 나옴 → `place()`가 계산값을 직접 반환, 화살표는 그 값을 씀(실측 DOM 재조회 안 함)
  - 화살촉 마커 확대(10×9→14×16) + `stroke-linecap: round`→`butt`(마커 밑동에 둥근 돌기 생기던 SVG 흔한 문제 해결)
  - 패널이 접힌 상태에서 투어 시작 시 `.ba-root` 슬라이드인(.26s transform) 도중 첫 스텝을 측정해 스포트라이트가 어긋나는 버그 → 접힌 상태였으면 280ms 대기 후 측정
- **가이드 투어 ← → 방향키 네비게이션** — 버튼 클릭과 동일한 `goNext`/`goPrev` 공유(중복 로직 없음), 입력창·`<select>` 포커스 시 무시, 키 반복(길게 누름) 무시. 카드에 `← →` 힌트 표시(`.ba-tour-kbdhint`, 기존 `.ba-kbd-pop kbd` 스타일 공유)
- **리그 정보 노출** (`renderList.js` rowHtml) — 조사 결과 폴더는 이미 게임 단위로 전역 공유(리그 무관, `addFolder`에 league 필드 없음)라 같은 폴더가 리그 섹션 수만큼 반복 렌더되는 구조적 어색함 확인(리그 섹션 자체를 없애는 안은 논의만 하고 보류 — 미완료 7번 참조):
  - 리그 섹션 헤더 "지난" 배지를 경고 톤(앰버, `.ba-league-badge.past`)으로 강화 + 툴팁("링크가 깨졌을 수 있음") — 행 단위 아님(섹션 헤더가 이미 리그를 말해주므로 중복 방지, AskUserQuestion으로 범위 확정)
  - 북마크 조건 칩 툴팁에도 `[리그] 《...》` 저장 당시 리그 표시(히스토리와 동일 마커·메커니즘 재사용) — `leagueLine`/`condTipWithLeague`를 히스토리·북마크 공용으로 한 번만 계산하도록 정리(히스토리 쪽 중복 코드 제거)
  - 조건 0개인 북마크는 조건 칩 자체가 안 떠서 리그 정보 걸 곳이 없던 문제 → 조건 칩 항상 렌더로 변경("조건 0개" 텍스트 + 리그만 담긴 툴팁)

## 미완료 / 다음 단계

1. 사용자가 poe1·poe2 각각에서 PoB 복사 → 실제 PoB(1/2)에 Ctrl+V import 최종 확인 — 아직 미확인(현재 "미완벽 인정, 추후 보완" 방침이라 급하지 않음. Shift+클릭 제보로 사용자 피드백 루프 대체)
2. poe1 탈리스만 베이스 데이터 소스 부재(위 참조) — 필요시 별도 조사
3. poe1 환산 칩은 BE items 맵에 있는 63개 화폐만 지원 — 제왕의 오브·축복의 오브 등 일부 흔한 화폐는 BE 데이터 자체에 없음(이 리그 스냅샷 한정일 수 있음)
4. poe1 장비 품질/소켓/링크는 MVP 미포함(PoB에서 수동 지정 필요)
5. 완료 선언 전 Designer·QA 페르소나(Phase 3·4) 정리 권장(§14, 아직 미실시)
6. **[해소 완료 2026-07-27 — 라이브 확인됨]** 리그 변경으로 깨진 북마크 복구 — 위 2026-07-22 항목 참조. **남은 것은 실제 거래소에서의 1회 확인**: 지난 리그 북마크 → "현재 리그로 다시 검색" → 새 검색이 생성되고 **필터 UI에 조건이 채워져** 뜨는지. 개발 환경에서는 로그인 세션이 필요해 POST를 실행해보지 못했다(단위·DOM 테스트와 하네스는 목으로 검증). 실패 시 확인 지점: ① 응답 JSON에 `id`가 오는지 ② poe1 리그 경로가 URL 표시명("허상")으로 맞는지(BE 환율 API만 EN id를 요구하고 거래소 API는 URL 표기 그대로일 것으로 판단) ③ 429가 뜨면 잠시 후 재시도
6-b. **[반영 완료 / 잔여 확인 1건] URL 리그 세그먼트 치환** — 사용자 확인(2026-07-22)으로 2단 이관 반영(위 항목). **남은 확인**: *오래된* 해시(예: Settlers 시절)가 GGG 서버에 얼마나 오래 살아있는지 — 만료돼 있으면 1순위가 실패하고 저장된 조건으로 폴백한다(조건도 없으면 `expired` 안내). 라이브에서 옛 북마크로 한 번 눌러보면 어느 경로를 탔는지 콘솔 로그(`[BA] 리그 이관 — 기존 해시 재사용` vs `해시 만료 추정, 조건 재생성으로 폴백`)로 바로 보인다

7. ~~**0.5.0 스토어 제출**~~ — **종료(2026-07-28, 사용자 확인)**. `deploy/poe-bookmark-atlas-0.5.0.zip` 제출 완료, 태그 `v0.5.0` 게시 완료. 심사 결과는 사용자가 직접 확인(0.4.0까지 전부 통과 이력)
8. **[사용자 검토 대기] `docs/영상-소개-대본.md`** — 미커밋 신규 문서. 0.5.0의 조건 묶음·일괄 이동·투어 개편이 대본에 반영돼야 하는지 사용자 확인 필요
9. **[정리]** 개발·테스트 중 사용자 실데이터에 만들어진 조건 묶음 `그룹테스트`(및 `목걸이 - 초반`·`투구 - 저렴이` — 이쪽은 사용자가 쓸 수도 있음). 묶음 줄 ✏ → ✕ 로 삭제 가능. 사용자 판단 사항

10. **[논의만, 미결정] 리그별 폴더 섹션 구조 제거** — 폴더가 이미 게임 전역 공유임을 확인, 히스토리처럼 리그 섹션을 없애고 북마크도 통합하는 안. **6번 구현으로 판단 근거가 바뀌었다**: 북마크가 "특정 리그의 링크"가 아니라 "리그 무관한 조건 + 언제든 재생성 가능한 링크"가 되었으므로 리그는 더 이상 1차 조직 축이 아니다 → 섹션 제거 쪽에 무게. 진행 시 리그 표시는 행 단위(조건 칩 툴팁의 `[리그]` 표기, 이미 있음) + `data-past` 기반 표식으로 내리고, 섹션 헤더의 "지난" 배지는 없어진다. **단 6번 라이브 검증 후에 착수**(검증 범위가 두 배가 되는 것 방지)

## 현재 상태

- **브랜치 전략 (2026-07-22 신설)**: 통합 브랜치 `develop`, `main`은 릴리즈 전용(직접 커밋 금지). 정본은 루트 `CLAUDE.md`. 릴리즈 시 `develop` → `main` 머지 후 태그.
- 브랜치(2026-08-02): `main`·`develop` 둘 다 `49fb1b4` 이상, origin 동기화 `0 0`. develop tip은 이 핸드오프 커밋
- 미커밋: `docs/영상-소개-대본.md`(신규, 사용자 검토 대기 — 0.5.0 조건 묶음·일괄 이동·투어 개편 반영 여부 확인 필요)
- 테스트: **318/318**(vitest, jsdom 포함) · 빌드 통과
- **라이브 확인 전부 완료(2026-07-28)**: 이동 모달(목록 렌더·미리 체크·2건 이동·복원) ✓ / 저장 다이얼로그 ✓ / 투어 17스텝 전 구간 ✓(신규 2·7스텝 실제 요소 스포트라이트 확인) / 토스트 화면 중앙·줄바꿈 ✓ — **대기 중인 라이브 검증 없음**
- 배포: **0.6.2 릴리즈 게시 + 스토어 제출 완료**(태그 `v0.6.2`, `49fb1b4`, 現 `Latest`) — 심사 결과 대기 중. 스토어에 게시 중인 버전은 심사 통과 전까지 이전 승인분
- **GitHub 릴리즈·태그**: `v0.1.0`~`v0.5.0`(`13dd975`)·`v0.6.0`(`cd5dbc4`)·`v0.6.1`(`8bcd0b9`)·**`v0.6.2`(`49fb1b4`, 2026-08-02 publish, 現 `Latest`)** 전부 공개. 로컬·origin 태그 동기 확인 완료. **릴리즈 절차**: ① 릴리즈 커밋에서 버전 범프+빌드+zip ② `gh release create vX.Y.Z --target <full-sha> --title "vX.Y.Z" --notes-file <md>`(전체 SHA 필수 — 단축 SHA는 target_commitish 거부. draft로 만들 거면 `--draft`, 즉시 게시면 `--latest`) ③ **`git fetch origin --tags`** — `gh release create`는 태그를 원격에만 만들어서 안 하면 로컬 태그가 뒤처진다 ④ draft였다면 publish(`gh release edit vX.Y.Z --draft=false --latest`). 노트는 사용자 관점 기능 중심으로 묶어 작성(개발 커밋 나열 X). **publish 시점은 사용자 판단** — 0.4.0까지는 "스토어 심사 통과 후", 0.5.0부터는 제출 전 즉시 게시를 택하고 있다. 물어보고 정할 것. **이전 버전 draft가 게시 안 됐으면 그것부터 publish**(v0.3.0 사례 — 스토어엔 나갔는데 태그 없이 19일 방치됨) 후 새 버전 릴리즈 진행
- **zip 생성**: Git Bash에 `zip`이 없다. `powershell.exe -NoProfile -Command "Compress-Archive -Path dist\* -DestinationPath deploy\poe-bookmark-atlas-X.Y.Z.zip -Force"` (Claude Code에서 실행됨, 2026-08-02 확인). `dist/*`여야 manifest.json이 zip 루트에 온다
- 빌드: `npm run build` → dist/ (해시 변경 시 확장 리로드+F5). dist/·deploy/ gitignore.
- 하네스: `.claude/launch.json`의 `harness`(포트 5199), `test-harness/harness.mjs` 참조. content-main.js(페이지 주입 로직·PoB 버튼·환산 칩)는 하네스가 실행 안 함 — 이 부분은 `preview_eval`로 실제 로직을 인라인 재현해 모의 검증(라이브 로그인 세션 접근 불가)
- 검증 제약: 거래소는 **로그인 세션 탭에서만** 패널 마운트 → 라이브 검증은 확장 리로드+F5 수동. 확장은 **dist 폴더** 로드.
- 커밋 작성자: `git -c user.name="서민욱" -c user.email="alsdnr0712@gmail.com"`, Co-Authored-By 금지.
- **함정 메모**: 패널은 open shadow root — document 레벨 리스너에서 `e.target.closest` 금지, **`e.composedPath()` 사용** (누적 3회 물림)
- **하네스 검증 함정 3건(2026-07-04)**: ① `import('../src/store/store.js')`를 `preview_eval`에서 동적 import하면 실패(harness.mjs 자체 top-level import는 정상) — `chrome.storage.local.get/set('records')`을 직접 조작해 우회. ② renderList.js/panel.js 등을 Edit하면 Vite HMR이 **풀 페이지 리로드**를 트리거해 harness.mjs가 재실행되고 `mem` Map(목 storage)이 초기화됨 — eval로 주입한 테스트 데이터가 사라지므로 Edit 직후엔 재주입 필요. ③ 이 세션에서 `preview_screenshot`이 투어 유무·코드 변경과 무관하게 지속 타임아웃(환경 이슈로 추정) — `preview_eval`로 좌표·속성·컴퓨티드 스타일을 직접 읽어 대체 검증
- **맵 재생성 절차**: `scripts/build-pob-statmap.mjs <en.json> <kr.json> [출력명]`은 비-KR 환경(VPN)에서 curl로 받은 raw JSON 필요(EN pathofexile.com geo-block, Node fetch는 Cloudflare 차단 — curl만 통과). `scripts/build-pob-basemap.mjs [poe-i18n루트] [base출력] [unique출력]`은 poe-i18n-json-data-generator-dev 레포 최신 상태 의존
